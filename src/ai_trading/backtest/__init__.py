"""Pure backtest transforms (asof, chain, candidates, costs, barriers, replay,
stats, walkforward, reports, runner): MT5-free by design; import submodules
directly — this file stays a bare marker for the whole of Phase 3.
"""
