"""Pure ML transforms (features, audit, train, artifact, scorer, heuristic,
evaluate, runner): MT5-free by design; import submodules directly — this file
stays a bare marker for the whole of Phase 4.
"""
