---
phase: 05-llm-narrative-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - config.toml
  - config.local.toml
  - src/ai_trading/config.py
  - src/ai_trading/llm/__init__.py
  - src/ai_trading/llm/schema.py
  - src/ai_trading/llm/provider.py
  - src/ai_trading/llm/prompt.py
  - tests/unit/_llm_fixtures.py
  - tests/unit/test_llm_schema.py
  - tests/unit/test_llm_provider.py
  - tests/unit/test_llm_prompt.py
  - tests/unit/test_backtest_config.py
  - tests/unit/test_normalize_and_config.py
autonomous: false
requirements: [AI-05]
user_setup: []
must_haves:
  truths:
    - "With a configured local vLLM llm_base_url/llm_model, the provider builds a chat.completions request carrying model, messages, response_format (strict json_schema derived from the LLMNarrative pydantic model), and a generous max_tokens (>= llm_max_tokens, default 2048) so the reasoning model's content=None truncation is handled, not crashed on."
    - "LLMNarrative validates verdict in {confirm, refute}, confidence in [0,1], reasoning non-empty, citations a list of str; FORBIDDEN_LEVEL_KEYS names the level fields that must never survive as LLM-emitted citations (D-02)."
    - "Config carries the llm_* knobs (enabled, base_url, model, timeout, max_tokens, top_n_contributors, structured_mode, agree_min_confidence, max_retries) with fail-fast validation; llm_api_key lives only in gitignored config.local.toml and is never repr'd/config-dumped to committed config."
    - "The LLM-provider path is testable fully offline: a duck-typed FakeLLMProvider implements the same interface as the real provider, and the default suite runs without any live vLLM endpoint."
    - "The prompt is built only from the evidence object's fields (D-03) and contains no raw OHLC/candle series and no recomputed levels."
  artifacts:
    - src/ai_trading/llm/__init__.py
    - src/ai_trading/llm/schema.py
    - src/ai_trading/llm/provider.py
    - src/ai_trading/llm/prompt.py
    - tests/unit/_llm_fixtures.py
    - tests/unit/test_llm_schema.py
    - tests/unit/test_llm_provider.py
    - tests/unit/test_llm_prompt.py
  key_links:
    - "schema.py -> provider.py: the response_format the provider sends is the strict json_schema derived from LLMNarrative; the returned JSON is re-validated against the same model (single source of truth, no drift)."
    - "config.py -> provider.py: llm_base_url/llm_api_key/llm_timeout_ms feed the OpenAI client constructor; llm_max_tokens >= 2048 is the reasoning-model content=None guard."
    - "provider.py -> prompt.py: prompt.py builds the system/user message list from the evidence object; the provider sends them (never injects OHLC or levels)."
---

<objective>
Build the LLM provider interface plus the structured-output contract that the Phase 5 narrative pipeline (plan 05-02) depends on.

Purpose: This plan lands the separable, testable seam between the LLM endpoint and the codebase — an abstract provider (local vLLM via the OpenAI-compatible SDK; any OpenAI-compatible endpoint works through the same interface), a pydantic-validated structured-output contract (D-04), a schema-level level-field guard (D-02/SC1), and a prompt builder that is strictly the evidence-object projection (D-03). Everything here must be provable offline (AI-07/SC3) via a duck-typed fake provider, so the default suite never touches a live endpoint.

Output: The `ai_trading.llm` package skeleton (schema, provider, prompt), the extended frozen `Config` with `llm_*` knobs, the `openai` dependency + `llm` pytest marker, and the offline unit tests proving the contract and the provider's request mapping.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llm-narrative-layer/05-CONTEXT.md
@.planning/phases/05-llm-narrative-layer/05-RESEARCH.md
@.planning/phases/05-llm-narrative-layer/05-PATTERNS.md
@src/ai_trading/config.py
@src/ai_trading/ml/scorer.py
@src/ai_trading/backtest/candidates.py
@tests/unit/_ml_fixtures.py
@tests/unit/test_backtest_config.py
@tests/unit/test_normalize_and_config.py

# Contracts this plan must honor (locked decisions, non-negotiable)
- D-01/D-02/D-03/D-04/D-05 (05-CONTEXT.md): full structured evidence object; structural SL/TP/entry reference-only with a schema-level guard that DROPS any level field the LLM emits; strictly the structured evidence object (no OHLC); structured JSON {verdict, confidence, reasoning, citations}; strict citation check.
- RESEARCH.md "Live vLLM caveat": the local vLLM (192.168.5.178:8000/v1, model deepseek-v4-flash-vision-exp) is a reasoning model — content can be None with finish_reason="length" on small max_tokens; the adapter MUST set a generous max_tokens (>= 2048) and treat content=None as a retry/fallback trigger (LLMTruncatedError), never a parse crash.
- RESEARCH.md Package Legitimacy Audit: `openai` is [SUS] (official SDK, high confidence, data-availability artifact) — a blocking-human legitimacy checkpoint is mandated before `uv add`.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the openai package legitimacy before install</name>
  <what-built>Nothing yet — this is the pre-install package-legitimacy checkpoint mandated by the RESEARCH Package legitimacy audit (openai is flagged [SUS] — an official OpenAI SDK with a data-availability false-positive).</what-built>
  <action>Blocking human confirmation that the `openai` package on PyPI is the legitimate official OpenAI SDK before the Task 2 `uv add "openai>=3.8.0"` install runs. Approve by responding as directed; do not proceed to install until approved.</action>
  <how-to-verify>
    1. Open https://pypi.org/project/openai/ in a browser and confirm it is the official OpenAI Python SDK (project_urls points at github.com/openai/openai-python).
    2. Confirm the current release version is >= 3.8.0 (research pinned 3.8.0, published 2026-09-03).
    3. Confirm no suspicious postinstall/install script is declared on the package page.
    Expected: you confirm this is the legitimate official SDK and approve installation into the project venv.
  </how-to-verify>
  <resume-signal>Type "approved" (or state any concern) to proceed to the scaffold/install in Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wave 0 scaffold — openai dependency, llm marker, and llm_* config knobs</name>
  <files>pyproject.toml, config.toml, config.local.toml, src/ai_trading/config.py, tests/unit/test_backtest_config.py, tests/unit/test_normalize_and_config.py</files>
  <read_first>
    @src/ai_trading/config.py
    @tests/unit/test_backtest_config.py
    @tests/unit/test_normalize_and_config.py
    @pyproject.toml
    @D:\Git\AI-Trading\.planning\phases\05-llm-narrative-layer\05-RESEARCH.md
    @D:\Git\AI-Trading\.planning\phases\05-llm-narrative-layer\05-PATTERNS.md
  </read_first>
  <action>
    Per RESEARCH.md "Marker change required (Wave 0)", "Open Question 4", and 05-PATTERNS.md "pyproject.toml / config.toml / config.local.toml (MODIFIED):"

    1. pyproject.toml: add `"openai>=3.8.0"` to `[project].dependencies`; register a new `llm` marker in `markers` (text describing opt-in live-endpoint tests); change `addopts` from `-m "not mt5"` to `-m "not mt5 and not llm"` so the live-endpoint integration test is excluded by default and the suite stays offline-clean.

    2. config.py — extend the frozen Config with the llm_* knob block in ALL FOUR places the ml_* block occupies (see config.py lines 59-74 _REQUIRED_KEYS, 110-126 fields+defaults, 316-367 _validate, 147-188 load_config construction):
       - _REQUIRED_KEYS: add llm_enabled, llm_base_url, llm_model, llm_api_key, llm_timeout_ms, llm_max_tokens, llm_top_n_contributors, llm_structured_mode, llm_agree_min_confidence, llm_max_retries.
       - frozen Config fields with defaults: llm_enabled: bool = False; llm_base_url: str = "http://192.168.5.178:8000/v1"; llm_model: str = "deepseek-v4-flash-vision-exp"; llm_api_key: str = ""; llm_timeout_ms: int = 8000; llm_max_tokens: int = 2048; llm_top_n_contributors: int = 5; llm_structured_mode: str = "json_schema"; llm_agree_min_confidence: float = 0.6; llm_max_retries: int = 1.
       - _validate: llm_structured_mode must be in {"json_schema", "json_object", "none"}; llm_timeout_ms must be integer > 0; llm_max_tokens must be integer >= 2048 (reasoning-model budget, Pitfall 1); 0 <= llm_agree_min_confidence <= 1; llm_top_n_contributors must be integer >= 1. Reuse the existing _is_int/_is_number discipline so a bool passed as int is rejected; raise ValueError naming the offending field (match the ml_* message style).
       - load_config: construct every llm_* field from raw. llm_api_key is a credential — it must never be written to committed config.toml nor appear in any repr/log (config.py module docstring already enforces Config never repr'd; keep that guarantee).
       - This is an ADDITIVE change: existing prior-phase config behavior (collector/backtest/ml_* keys) must not change.

    3. config.toml (committed, no secrets): add llm_enabled=false, llm_base_url="http://192.168.5.178:8000/v1", llm_model="deepseek-v4-flash-vision-exp", llm_timeout_ms=8000, llm_max_tokens=2048, llm_top_n_contributors=5, llm_structured_mode="json_schema", llm_agree_min_confidence=0.6, llm_max_retries=1. Do NOT add llm_api_key here.
    4. config.local.toml (gitignored): add llm_api_key (a dummy key works for the local vLLM; research notes any value is accepted).

    5. Because adding llm_* to _REQUIRED_KEYS makes load_config refuse any config lacking them, update the shared `_base_values(...)` config-dict helper in BOTH tests/unit/test_backtest_config.py and tests/unit/test_normalize_and_config.py to include the same llm_* keys (matching the config.toml defaults). Do NOT change any existing assertion; only add the required keys so load_config keeps loading.

    6. Install the dependency (after the Task 1 checkpoint approved): `uv add "openai>=3.8.0"` then confirm the import surface with `uv run python -c "import openai; print(openai.__version__)"` (expect >= 3.8.0).
  </action>
  <acceptance_criteria>
    - pyproject.toml lists "openai>=3.8.0" in [project].dependencies, registers the `llm` marker, and addopts is exactly `-m "not mt5 and not llm"`.
    - `uv add "openai>=3.8.0"` succeeds and `uv run python -c "import openai"` imports (version >= 3.8.0).
    - Config gains all 10 llm_* fields with the defaults above and fails fast (ValueError naming the field) on: llm_structured_mode outside {json_schema,json_object,none}, llm_timeout_ms <= 0, llm_max_tokens < 2048, llm_agree_min_confidence outside [0,1], llm_top_n_contributors < 1.
    - config.toml contains the public llm_* defaults (no llm_api_key); config.local.toml contains llm_api_key.
    - Existing config unit tests still pass after the two `_base_values` helpers are extended.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import openai; assert openai.__version__ >= '3.8.0'" ; uv run pytest -q tests/unit/test_normalize_and_config.py tests/unit/test_backtest_config.py -q</automated>
  </verify>
  <done>
    openai 3.8.0 installed; the `llm` marker + addopts change in pyproject.toml; Config validates all llm_* knobs fail-fast; config.toml/config.local.toml hold the correct (secret-split) values; both prior config test files still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Structured-output contract (LLMNarrative) + provider interface + prompt builder + offline fixtures</name>
  <files>src/ai_trading/llm/__init__.py, src/ai_trading/llm/schema.py, src/ai_trading/llm/provider.py, src/ai_trading/llm/prompt.py, tests/unit/_llm_fixtures.py, tests/unit/test_llm_schema.py, tests/unit/test_llm_provider.py, tests/unit/test_llm_prompt.py</files>
  <read_first>
    @src/ai_trading/ml/scorer.py
    @src/ai_trading/ml/artifact.py
    @src/ai_trading/backtest/candidates.py
    @src/ai_trading/mt5_client.py
    @src/ai_trading/ml/features.py
    @tests/unit/_ml_fixtures.py
    @D:\Git\AI-Trading\.planning\phases\05-llm-narrative-layer\05-RESEARCH.md
    @D:\Git\AI-Trading\.planning\phases\05-llm-narrative-layer\05-PATTERNS.md
  </read_first>
  <action>
    Per 05-PATTERNS.md (copy-from vs adapt assignments) and RESEARCH.md Patterns 1-3:

    1. src/ai_trading/llm/__init__.py — bare package marker mirroring ml/__init__.py (one-line docstring naming the package; import submodules directly; optional re-exports of LLMProvider/OpenAICompatProvider).

    2. schema.py — the structured-output contract (adapt of ml/artifact.py schema-lock + ml/features.py pinned-enum discipline):
       - Define pydantic `LLMNarrative` per D-04: `verdict: Literal["confirm", "refute"]`; `confidence: Annotated[float, Field(ge=0.0, le=1.0)]`; `reasoning: str = Field(min_length=1)`; `citations: list[str]`.
       - Module constants (research lines 272): `FORBIDDEN_LEVEL_KEYS` frozenset = {"entry","sl","tp","sl_price","tp_price","entry_price","take_profit","stop_loss"} (D-02 guard — the schema-level whitelist that drops any level field the LLM emits). `ALLOWED_CITATION_KEYS` = the set of evidence-object field keys that are legal citation targets (excluding the forbidden level fields — e.g. symbol, timeframe, direction, zone_id, zone_state, event_id, pool_id, sweep_side, bias_h1, bias_h4, p_win, score_source, artifact_version).
       - A builder that returns the strict JSON-schema response_format for the openai SDK from the model (research Pattern 2/Common Operation 2 uses openai's `type_to_response_format_param(LLMNarrative)` → `{"type":"json_schema","json_schema":{...strict:true}}`). Also expose `model_json_schema()`/the model itself so the provider's single source of truth is the pydantic model, not a hand-written dict.
       - Fail-fast validation on construction: reject a Narrative whose verdict is outside the enum or confidence outside [0,1] naming the field.

    3. provider.py — the adapter tier (copy mt5_client.py "only module that imports the SDK" discipline; research Pattern 1 + Common Operation 1):
       - Error taxonomy as RuntimeError subclasses: `LLMProviderError`, and `LLMTruncatedError` (raised when `content is None` on a reasoning model — research Pitfall 1).
       - `LLMProvider` as a typing.Protocol defining `build_narrative(self, evidence: dict, response_schema, *, cfg) -> str` (returns the raw JSON string).
       - `OpenAICompatProvider` implementing it: construct the OpenAI client ONCE in __init__ from cfg (api_key=cfg.llm_api_key, base_url=cfg.llm_base_url, timeout=cfg.llm_timeout_ms/1000, max_retries=cfg.llm_max_retries). `build_narrative` calls `self._client.chat.completions.create(model=cfg.llm_model, messages=build_prompt(evidence), max_tokens=cfg.llm_max_tokens, response_format=response_schema)`, reads `resp.choices[0].message.content`, and if it is None raises LLMTruncatedError (never parse-crash). The openai SDK is imported exactly once, here.
       - Keep `response_format` behind `cfg.llm_structured_mode`: for "json_schema" pass the strict schema; for "json_object" pass {"type":"json_object"}; for "none" omit it (research Pitfall 4 — some stacks reject json_schema; pydantic re-validation in plan 05-02 is the authority).

    4. prompt.py — build_prompt (pure, adapt of the ml/features pure-function convention; RESEARCH D-03 rule):
       - `build_prompt(evidence: dict) -> tuple[list[dict], list[dict]]` returning system + user message dicts (the system message instructs the model to reason only from the provided structured evidence and return only the D-04 JSON; the user message embeds the evidence object's fields).
       - Hard rule (D-03): the prompt contains ONLY the evidence object's field projection — no raw OHLC/candle series, no recomputed SL/TP/entry/R:R values, no raw feature arrays. Never reach for any data outside the passed evidence dict.

    5. tests/unit/_llm_fixtures.py — copy the FakeMT5Client duck-typed fake + _ml_fixtures.ml_cfg config-constructor convention (research Pattern/Common Operation 4):
       - `FakeLLMProvider`: constructor takes `responses` (FIFO of raw JSON strings) and `errors` (FIFO of exceptions e.g. TimeoutError), records `calls` as list of (evidence, response_schema), and `build_narrative(evidence, response_schema, *, cfg)` pops from the FIFOs (raising the next error when present; AssertionError when no scripted response remains).
       - `llm_cfg(**overrides)` — a frozen Config carrying the llm_* defaults via the existing `bt_cfg`/`ml_cfg` constructor chain (never touches load_config).
       - `make_evidence(...)` — a deterministic sample evidence object dict (keys matching ALLOWED_CITATION_KEYS plus the reference-only level fields) for the prompt/citation tests.

    6. Unit tests (each `@pytest.mark.unit`, imports from `_llm_fixtures` directly, no network / no MT5):
       - test_llm_schema.py: LLMNarrative accepts verdict in {confirm,refute}; rejects a verdict outside the enum naming the field; rejects confidence outside [0,1]; requires non-empty reasoning; FORBIDDEN_LEVEL_KEYS contains entry/sl/tp/sl_price/tp_price/entry_price; the json-schema response_format builder yields a dict with type=="json_schema" and a strict schema matching LLMNarrative.
       - test_llm_provider.py: monkeypatch the real OpenAI client's `chat.completions.create` (no network) and assert `OpenAICompatProvider.build_narrative` maps cfg → model/messages/max_tokens/response_format correctly; assert a `content=None` response raises LLMTruncatedError (reasoning-model truncation); assert the client is constructed with base_url/timeout/max_retries from cfg.
       - test_llm_prompt.py: build_prompt contains the evidence fields; asserts the prompt text contains NO raw OHLC/candle keys and NO level values (a forbidden-input guard, mirroring ml/features `_validate_bars` style).
  </action>
  <acceptance_criteria>
    - `LLMNarrative` validates the D-04 contract; FORBIDDEN_LEVEL_KEYS drops any level field; the json_schema response_format builder returns a strict schema matching the pydantic model.
    - `OpenAICompatProvider.build_narrative` maps cfg to the correct openai chat.completions params and raises LLMTruncatedError on content=None; the client is constructed once with base_url/timeout/max_retries.
    - `build_prompt(evidence)` produces system+user messages containing only the evidence object projection (no OHLC / no recomputed levels).
    - `FakeLLMProvider` records calls and can script responses and TimeoutError; `llm_cfg`/`make_evidence` are available and deterministic.
    - All three test modules pass offline (no live endpoint).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/unit/test_llm_schema.py tests/unit/test_llm_provider.py tests/unit/test_llm_prompt.py</automated>
  </verify>
  <done>
    The ai_trading.llm package (schema/provider/prompt) is importable; the contract enforces verdict/confidence/citations and drops level fields; the provider maps config to the openai request and handles content=None via LLMTruncatedError; the prompt is evidence-only; the suite is offline-green.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

**New source modules (src/ai_trading/llm/):**
- `__init__.py` — bare package marker.
- `schema.py` — `LLMNarrative` (pydantic), `FORBIDDEN_LEVEL_KEYS` (frozenset), `ALLOWED_CITATION_KEYS` (set), `json_schema`-response_format builder (e.g. `narrative_response_format()`).
- `provider.py` — `LLMProvider` (Protocol), `OpenAICompatProvider`, `LLMProviderError`, `LLMTruncatedError`.
- `prompt.py` — `build_prompt(evidence) -> (system_messages, user_messages)`.

**Extended source:**
- `config.py` — 10 new frozen-Config fields + `_REQUIRED_KEYS` entries + `_validate` checks: `llm_enabled`, `llm_base_url`, `llm_model`, `llm_api_key`, `llm_timeout_ms`, `llm_max_tokens`, `llm_top_n_contributors`, `llm_structured_mode`, `llm_agree_min_confidence`, `llm_max_retries`.
- `pyproject.toml` — `openai>=3.8.0` dependency, `llm` marker, `addopts = '-m "not mt5 and not llm"'`.
- `config.toml` (public llm_* defaults), `config.local.toml` (`llm_api_key`).
- `tests/unit/test_backtest_config.py` and `tests/unit/test_normalize_and_config.py` — `_base_values` helper gains the llm_* keys.

**New test files (tests/unit/):**
- `_llm_fixtures.py` — `FakeLLMProvider`, `llm_cfg`, `make_evidence`.
- `test_llm_schema.py`, `test_llm_provider.py`, `test_llm_prompt.py`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → LLM endpoint | The model is untrusted: it can return malformed JSON, emit fabricated level fields, or return content=None. Nothing it returns is trusted; the schema guard + citation check (D-02/D-05) reject it mechanically. |
| config.toml (committed) | Public defaults only — no credentials may live here (ASVS V14). |
| pip/uv install | Third-party dependency (openai) is installed into the venv. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing | OpenAICompatProvider returns model-originated content | high | mitigate | Schema-level guard (D-02) drops any level field via FORBIDDEN_LEVEL_KEYS; returned JSON re-validated against pydantic LLMNarrative; never trust prompt adherence. |
| T-05-02 | Repudiation | No record of which narrative came from the LLM vs ML | medium | mitigate | score_source/narrative_status/reason are recorded on the narrative record in plan 05-02; provenance is data, not prose. |
| T-05-03 | Information Disclosure | llm_api_key leaked into committed config/logs | high | mitigate | llm_api_key only in gitignored config.local.toml; Config is never repr'd (existing V14 hygiene); committed config.toml carries no secret. |
| T-05-04 | Denial of Service | Slow/hanging LLM endpoint blocks setup emission | medium | mitigate | llm_timeout_ms + llm_max_retries bound the provider call; plan 05-02 adds a try/except fallback emitting ML-only setups (AI-07/SC3). |
| T-05-05 | Tampering | Model returns content=None (reasoning model) causing a hard crash | medium | mitigate | Generous llm_max_tokens >= 2048 + LLMTruncatedError read as retry/fallback, never a parse crash (Pitfall 1). |
| T-05-06 | Tampering | Narrative artifact written non-atomically (partial/corrupt file) | medium | mitigate | plan 05-02 writer uses tmp+os.replace atomic write (existing reports.py discipline). |
| T-05-SC | Tampering | pip install of openai (supply chain) | high | mitigate | blocking-human legitimacy checkpoint (Task 1) verifies the official OpenAI SDK before `uv add` (openai is [SUS] per the package audit). |
</threat_model>

<verification>
Run `uv run pytest -q -m "not mt5 and not llm" tests/unit` after Task 3; the whole plan must be green offline. Confirm the `llm`-marked live test (added in plan 05-02) is excluded by the default addopts. Confirm `uv run python -c "import ai_trading.llm.schema, ai_trading.llm.provider, ai_trading.llm.prompt"` imports cleanly.

> Scope-sanity note (accepted): this plan touches 13 files (the config/seam scaffold plus the llm package); the file count is accepted here because the tasks are 3 cohesive vertical steps with per-task commit discipline, as in prior phases. No restructuring.
</verification>

<success_criteria>
- `openai>=3.8.0` installed; `llm` marker + `addopts` change present; default suite runs offline and green.
- Config validates all 10 llm_* knobs fail-fast; config.toml/config.local.toml secret-split is correct.
- LLMNarrative + FORBIDDEN_LEVEL_KEYS + json_schema response_format builder meet D-04/D-02.
- OpenAICompatProvider maps config to the openai request and handles content=None via LLMTruncatedError; client constructed once.
- build_prompt emits only the evidence projection (D-03).
- FakeLLMProvider + llm_cfg + make_evidence are available; the three test modules pass offline.
</success_criteria>

<output>
Create `.planning/phases/05-llm-narrative-layer/05-01-SUMMARY.md` when done
</output>
