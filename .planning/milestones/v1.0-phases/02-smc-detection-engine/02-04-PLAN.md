---
phase: 02-smc-detection-engine
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/ai_trading/detectors/mtf.py
  - tests/unit/test_mtf_join.py
  - tests/unit/test_detector_integration.py
autonomous: true
requirements: [SMC-06]

must_haves:
  truths:
    - "An M15 bar at time T sees only HTF zone state whose confirmation happened strictly before T: merge_asof with direction='backward' and allow_exact_matches=False; an HTF confirmation at exactly T is NOT visible (D-15 — the #1 lookahead trap)"
    - "Every joined row satisfies joined-zone created_at < time_utc — asserted as an invariant over the full payload (Pitfall 9)"
    - "Bias from the M15 close's position inside the latest live HTF zone range: close above equilibrium -> bearish (premium), close below equilibrium -> bullish (discount), close exactly at equilibrium -> neutral (A9); no live HTF range -> bias NA with NaN range fields and empty zone IDs (D-13)"
    - "Lean payload per HTF (H1 AND H4) per M15 bar: bias, HTF range high/low/equilibrium, signed distance-to-equilibrium in M15 ATR(14) units (A1), and IDs of live (not-yet-invalidated as of T) HTF zones whose range contains the M15 close (A2); no HTF pool/sweep payload (D-14)"
    - "If the latest strictly-before HTF zone is already invalidated as of T, payload fields are NA/NaN/empty until the next leg completes a new zone (planner pin — no stale bias from a broken range)"
    - "DST continuity: joins key on stored time_utc values; H1/H4 context does not jump at a broker-offset +3 -> +2 transition (Pitfall 8)"
    - "Full chain swings -> zigzag -> pools -> zones -> MTF over synthetic multi-TF frames produces consistent payloads: no zone before leg completion; no non-NA payload before the first HTF confirmation; input frames bit-identical before/after every detector call (SC5)"
  artifacts:
    - "src/ai_trading/detectors/mtf.py — htf_context(m15, htf_zones_h1, htf_zones_h4) -> pd.DataFrame"
    - "Payload schema (wide, one row per M15 bar): (symbol, time_utc, bias_h1, htf_range_high_h1, htf_range_low_h1, htf_equilibrium_h1, htf_dist_to_eq_atr_h1, htf_zone_ids_h1, bias_h4, htf_range_high_h4, htf_range_low_h4, htf_equilibrium_h4, htf_dist_to_eq_atr_h4, htf_zone_ids_h4); bias in {bullish, bearish, neutral} or NA; htf_zone_ids object lists sorted by (created_at, zone_id), empty list when none"
    - "tests/unit/test_mtf_join.py — SMC-06 join suite incl. DST anchor + strictly-before invariants"
    - "tests/unit/test_detector_integration.py — end-to-end chain + purity + schema assertions (SC5)"
  key_links:
    - "htf_context consumes derive_zones output frames (plan 02-03) keyed on confirmation timestamps — never HTF bar-open time, never a latest-row lookup (D-15, Pitfall 1)"
    - "Both merge_asof inputs explicitly sorted immediately before the join (Pitfall 9); by='symbol' for multi-symbol frames"
    - "Distance units from wilders_atr on the M15 decision bars (plan 02-01 ATR, A1)"
    - "Zero adapter-tier imports, zero file I/O, input frames never mutated (SC5)"
---

<objective>
Implement the multi-timeframe point-in-time context join: HTF zone-state timelines keyed on confirmation timestamps, strictly-before as-of joins onto each M15 decision bar (D-13/D-14/D-15), plus the end-to-end detector integration suite proving the whole chain (swings -> zigzag -> pools -> zones -> MTF) is pure, schema-stable, and lookahead-safe — closing SMC-06 and ROADMAP SC4/SC5 for the phase.

Purpose: the confirmation-time as-of rule is the single most dangerous lookahead trap in the project (an H4 bar stamped 12:00 contains information through 15:59; a 12:15 M15 bar must not see it). This plan makes D-15 a tested invariant, not a convention.
Output: src/ai_trading/detectors/mtf.py + tests/unit/{test_mtf_join,test_detector_integration}.py — all unit-green, ruff-clean.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-smc-detection-engine/02-CONTEXT.md
@.planning/phases/02-smc-detection-engine/02-PATTERNS.md
@src/ai_trading/normalize.py
@tests/conftest.py
@src/ai_trading/detectors/swings.py
@src/ai_trading/detectors/atr.py
@src/ai_trading/detectors/zigzag.py
@src/ai_trading/detectors/pools.py
@src/ai_trading/detectors/zones.py
@tests/unit/_detector_fixtures.py
</context>

## Artifacts this phase produces (plan 04 scope)

- Function: `htf_context(m15: pd.DataFrame, htf_zones_h1: pd.DataFrame, htf_zones_h4: pd.DataFrame) -> pd.DataFrame` in `src/ai_trading/detectors/mtf.py`
- Pinned wide payload schema (one row per M15 decision bar): `(symbol, time_utc, bias_h1, htf_range_high_h1, htf_range_low_h1, htf_equilibrium_h1, htf_dist_to_eq_atr_h1, htf_zone_ids_h1, bias_h4, htf_range_high_h4, htf_range_low_h4, htf_equilibrium_h4, htf_dist_to_eq_atr_h4, htf_zone_ids_h4)`; `bias_*` in {bullish, bearish, neutral} or NA; `htf_zone_ids_*` object columns of Python lists (sorted by (created_at, zone_id)); float fields NaN when no live HTF range
- Test modules: `tests/unit/test_mtf_join.py`, `tests/unit/test_detector_integration.py` (module-local multi-TF sculptors; do NOT modify tests/unit/_detector_fixtures.py or root conftest.py)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mtf.py — confirmation-time as-of join + lean MTF payload</name>
  <files>src/ai_trading/detectors/mtf.py</files>
  <read_first>
    - .planning/phases/02-smc-detection-engine/02-PATTERNS.md — mtf.py section: copy-ready join core (Pattern 6 excerpt with merge_asof direction='backward', allow_exact_matches=False, by='symbol') + pinned copy rules; collector.py point-in-time discipline excerpt
    - .planning/phases/02-smc-detection-engine/02-RESEARCH.md — Pattern 6 (lines 344–359), Pitfall 1 (the lookahead trap), Pitfall 8 (DST anchor), Pitfall 9 (sorted merge_asof inputs), Assumptions A1/A2/A9
    - .planning/phases/02-smc-detection-engine/02-CONTEXT.md — locked decisions D-13, D-14, D-15
    - src/ai_trading/detectors/zones.py — zones frame contract from plan 02-03 (created_at/invalidated_at/range/equilibrium/zone_id)
    - src/ai_trading/detectors/atr.py — wilders_atr for the M15 distance units
    - src/ai_trading/normalize.py — style-of-record for guards
  </read_first>
  <behavior>
    - HTF zone created strictly before T is visible to the M15 bar at T; a zone created at exactly T is NOT visible (allow_exact_matches=False lock, D-15)
    - Every payload row satisfies joined created_at < time_utc (invariant, Pitfall 9 warning sign)
    - M15 close above the joined HTF equilibrium -> bias bearish (premium); below -> bullish (discount); exactly at -> neutral (A9)
    - Payload carries range high/low/equilibrium of the latest strictly-before HTF zone per HTF timeframe
    - Distance-to-equilibrium = (M15 close - equilibrium) / M15 ATR(14) at the decision bar, signed (A1)
    - zone_ids lists every live zone (created_at < T and not invalidated at or before T — an invalidation stamped at T is not yet visible) whose range contains the M15 close (range_low <= close <= range_high), sorted by (created_at, zone_id) (A2)
    - A zone invalidated strictly before T is excluded from both the bias range (if it was the latest) and containment IDs; payload goes NA/NaN/empty until the next leg completes
    - Before the first HTF confirmation on a timeframe: bias NA, range fields NaN, dist NaN, zone_ids empty
    - Both H1 and H4 payloads present on every row (D-14); no pool/sweep fields anywhere
    - Multi-symbol frames join correctly per symbol (by='symbol')
    - Empty M15 -> empty payload with the full schema; empty HTF zones -> all-NA/NaN/empty payload columns with rows preserved
  </behavior>
  <action>
    Create src/ai_trading/detectors/mtf.py with the module docstring citing D-13..D-15 and RESEARCH Pattern 6; from __future__ import annotations; imports: pandas, from ai_trading.detectors.atr import wilders_atr. Zero adapter-tier vendor imports, zero file I/O, no grid re-flooring.

    Public function `htf_context(m15, htf_zones_h1, htf_zones_h4) -> pd.DataFrame`. Guards: m15 must contain symbol, time_utc, open, high, low, close with strictly-increasing unique time_utc and finite OHLC; zone frames must match the pinned plan 02-03 schema; violations raise ValueError naming the invariant. Empty/short inputs follow the pinned empty contracts (Pitfall 10).

    Implementation per HTF timeframe (run once for H1 zones, once for H4 zones, then merge the two payloads side-by-side on (symbol, time_utc)):
    1. Build the zone timeline from the zones frame: columns (created_at, zone_id, range_high, range_low, equilibrium, invalidated_at, symbol); explicitly sort_values by created_at immediately before the join (Pitfall 9).
    2. Sort m15 by time_utc; pd.merge_asof(left=m15, right=timeline, left_on="time_utc", right_on="created_at", by="symbol", direction="backward", allow_exact_matches=False) — the verified strictly-before join (D-15).
    3. Liveness: the joined zone is live as of T when its invalidated_at is NA or >= T (an invalidation at exactly T is a confirmation at T — not yet visible). If the joined zone is not live (or none joined), bias/fields are NA/NaN and zone_ids empty (planner pin — no stale bias from a broken range).
    4. Bias: close > equilibrium -> "bearish" (premium), close < equilibrium -> "bullish" (discount), close == equilibrium -> "neutral" (A9, D-13).
    5. Distance: (close - equilibrium) / wilders_atr(m15) value at the decision bar, signed (A1); NaN when no live range.
    6. Containment: cross-join the M15 bars against the HTF zones frame (small volumes — no interval structures), filter to live zones as of T with range_low <= close <= range_high, collect zone_ids per bar sorted by (created_at, zone_id) (A2).
    7. Assemble the wide payload with the exact pinned column names; suffix-merge the H1 and H4 payloads on (symbol, time_utc); return a fresh frame sorted by (symbol, time_utc).
    Never assign into m15 or the zone frames; naive datetimes throughout; consume stored time_utc values as-is (DST safety comes from both frames shifting together — Pitfall 8).
  </action>
  <verify>
    <automated>uv run python -c "from ai_trading.detectors.mtf import htf_context; print('mtf-import-ok')" && uv run pytest tests/unit/test_mtf_join.py -q</automated>
  </verify>
  <acceptance_criteria>
    - [source] src/ai_trading/detectors/mtf.py defines htf_context(m15, htf_zones_h1, htf_zones_h4) -> pd.DataFrame with the exact pinned 14-column payload schema
    - [source] the merge_asof call uses direction="backward" and allow_exact_matches=False with by="symbol"
    - [source] rg -c "floor_to_timeframe" src/ai_trading/detectors/mtf.py returns no matches
    - [test-command] uv run pytest tests/unit/test_mtf_join.py -q exits 0
    - [behavior] exact-timestamp exclusion test passes (HTF confirmation at exactly T is invisible)
    - [behavior] bias mapping covers premium/discount/neutral including the exact-equilibrium case
  </acceptance_criteria>
  <done>htf_context implements D-13..D-15: strictly-before confirmation joins, bias from live HTF range position, lean per-HTF payload with ATR-scaled distance and live-zone containment IDs, deterministic NA/NaN/empty contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_mtf_join.py — strictly-before invariants, bias mapping, DST anchor continuity</name>
  <files>tests/unit/test_mtf_join.py</files>
  <read_first>
    - tests/unit/_detector_fixtures.py — reuse sculptors (read-only)
    - src/ai_trading/detectors/mtf.py — implementation under test (Task 1)
    - tests/unit/test_timezone_dst.py — DST contract-test style-of-record (offset-flip fixtures with server-wall time arrays)
    - .planning/phases/02-smc-detection-engine/02-RESEARCH.md — Pitfall 1 warning signs, Pitfall 8 test design, Pitfall 9 invariant
    - .planning/phases/02-smc-detection-engine/02-CONTEXT.md — D-15 specifics + CONTEXT DST caveat (offset +3 summer, +2 after US DST end)
  </read_first>
  <behavior>
    - Confirmation-at-exact-T exclusion: HTF zone created_at == M15 bar time_utc -> that bar does NOT see it; the next M15 bar does
    - Strictly-before invariant: for a full sculpted frame, every payload row's effective HTF created_at < time_utc
    - Bias mapping: three M15 bars closing above / below / exactly at the HTF equilibrium -> bearish / bullish / neutral
    - Payload completeness: all 14 pinned columns present; before first HTF confirmation the fields are NA/NaN/empty
    - Containment: zone IDs list only live zones containing the close; a zone invalidated strictly before T is excluded; a zone invalidated at exactly T is still included (invalidation not yet visible)
    - Distance sign: close above equilibrium -> positive dist, below -> negative, magnitudes scale with 1/ATR
    - DST continuity: build M15 + H4 frames around a broker-offset flip from +3 to +2 (server-wall arrays per the test_timezone_dst pattern; H4 on the server-midnight lattice); H4 payload values progress continuously across the flip — no hour-gap jump, no dropped coverage
    - Point-in-time stability: appending later M15 bars or adding later-created HTF zones does not alter earlier rows' payloads
    - Empty M15 / empty zone frames -> pinned empty contracts
    - m15/zone inputs bit-identical before/after htf_context
  </behavior>
  <action>
    tests/unit/test_mtf_join.py — module docstring cites SMC-06 + ROADMAP SC4 + D-15 (the #1 lookahead trap, pinned explicitly) and states no adapter-tier imports anywhere; @pytest.mark.unit on every test. Build zone fixtures through the real chain (detect_swings -> build_zigzag -> derive_zones on sculpted HTF bars) so created_at/invalidated_at semantics are genuine; sculpt M15 decision bars with known closes relative to the HTF range. One named test per behavior bullet (names indicative): test_htf_confirmation_at_exact_bar_time_excluded, test_joined_rows_strictly_before_invariant, test_bias_premium_discount_neutral_mapping, test_payload_field_completeness_and_pre_confirmation_na, test_zone_ids_containment_live_only, test_zone_invalidated_before_t_excluded_at_t_boundary_visible, test_distance_to_equilibrium_signed_atr_units, test_dst_offset_flip_h4_anchor_continuity, test_appended_bars_and_zones_leave_earlier_rows_unchanged, test_empty_frame_contracts, test_input_frames_not_mutated. Write tests first, then align mtf.py to GREEN. The DST fixture mirrors tests/unit/test_timezone_dst.py's offset-array construction (naive server-wall times, per-bar offsets, time_utc = time - offset) — reuse that pattern module-locally, do not import test modules across files.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mtf_join.py -q && git diff --exit-code tests/conftest.py tests/unit/_detector_fixtures.py</automated>
  </verify>
  <acceptance_criteria>
    - [test-command] uv run pytest tests/unit/test_mtf_join.py -q exits 0 (>= 11 named tests)
    - [behavior] test_htf_confirmation_at_exact_bar_time_excluded proves D-15's strictly-before rule at the exact-match boundary
    - [behavior] test_dst_offset_flip_h4_anchor_continuity proves no context jump across a +3 -> +2 broker-offset flip (SC4 DST contract)
    - [behavior] test_appended_bars_and_zones_leave_earlier_rows_unchanged proves point-in-time stability of the join
    - [source] git diff --exit-code tests/conftest.py tests/unit/_detector_fixtures.py passes
  </acceptance_criteria>
  <done>SMC-06 join semantics are locked: strictly-before confirmation visibility, bias mapping with neutral pin, lean payload completeness, live-zone containment, DST anchor continuity, and point-in-time stability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_detector_integration.py — end-to-end chain, purity, schema locks (SC5)</name>
  <files>tests/unit/test_detector_integration.py</files>
  <read_first>
    - All five detector modules: src/ai_trading/detectors/{swings,zigzag,pools,zones,mtf}.py — the chain under test and their pinned schemas
    - tests/unit/_detector_fixtures.py — sculptors (read-only)
    - tests/unit/test_idempotent_store.py — cross-module chain-test style-of-record and assert_frame_equal usage
    - .planning/phases/02-smc-detection-engine/02-RESEARCH.md — Validation Architecture SC5 row, Security Domain (schema drift + mutation threats), Pattern excerpts for expected chain semantics
    - .planning/phases/02-smc-detection-engine/02-PATTERNS.md — shared patterns section (purity, non-mutation, empty contracts)
  </read_first>
  <behavior>
    - Full chain over sculpted multi-TF frames (M15 + H1 + H4, at least two symbols): detect_swings -> build_zigzag -> detect_pools -> derive_zones -> htf_context runs end-to-end and yields schema-correct non-empty artifacts at every tier
    - No zone record has created_at before its completing leg's second point confirmation (no zone before leg completion)
    - No payload row before the first HTF confirmation carries non-NA bias
    - Every joined payload row satisfies the strictly-before invariant
    - Chain determinism: running the whole chain twice on identical inputs produces byte-identical outputs (assert_frame_equal check_exact)
    - Purity: every input frame (bars M15/H1/H4, swings) is bit-identical before/after each detector call in the chain
    - Schema locks: each of the five output frames (swings, zigzag, pools, events, zones, payload) matches its pinned columns exactly — including empty-frame schemas
    - Sortedness invariants: swings sorted by confirmed_at; zones sorted by (symbol, created_at); payload sorted by (symbol, time_utc)
    - Purity-by-source: no module under src/ai_trading/detectors/ contains an adapter-tier vendor-package import statement (file-content assertion inside the test)
    - Multi-symbol by='symbol' join correctness: symbol A's payload never incorporates symbol B's HTF zones
  </behavior>
  <action>
    tests/unit/test_detector_integration.py — module docstring cites ROADMAP SC4 + SC5 + BT-01 (Phase 3 replays these exact functions) and states no adapter-tier imports anywhere; @pytest.mark.unit on every test; module-local multi-TF sculptors over _detector_fixtures helpers: build an H4 frame with two clean legs, an H1 frame with one leg, and an M15 frame whose closes sweep through premium/discount/equilibrium positions and pierce a sculpted equal-high pool; use two symbols to exercise by='symbol' (only one symbol needs full sculpting; the second can carry minimal bars proving join isolation). One named test per behavior bullet (names indicative): test_full_chain_end_to_end_over_multi_tf_frames, test_no_zone_before_leg_completion, test_no_payload_before_first_htf_confirmation, test_payload_strictly_before_invariant_full_chain, test_chain_deterministic_on_rerun, test_all_inputs_unmutated_through_chain, test_output_schemas_pinned_including_empty_frames, test_sortedness_invariants_on_all_outputs, test_no_vendor_imports_in_detector_sources (reads each file under src/ai_trading/detectors/ and asserts neither vendor import statement form appears), test_mtf_join_isolated_per_symbol. Write tests first, then fix any integration gaps in the detector modules (schema mismatches, sort order, empty contracts) — schemas are pinned by the plans, so implementations move, not tests.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_detector_integration.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - [test-command] uv run pytest tests/unit/test_detector_integration.py -q exits 0 (>= 10 named tests)
    - [behavior] test_full_chain_end_to_end_over_multi_tf_frames proves swings -> zigzag -> pools -> zones -> MTF composes over synthetic multi-TF frames (SC5)
    - [behavior] test_all_inputs_unmutated_through_chain proves non-mutation across every tier
    - [behavior] test_no_vendor_imports_in_detector_sources proves detector-tier purity by file-content assertion
    - [test-command] uv run pytest -q (entire unit suite) exits 0
    - [test-command] uv run ruff check . exits 0
  </acceptance_criteria>
  <done>The full detector chain is proven pure, deterministic, schema-locked, and lookahead-safe end-to-end; the phase's unit suite is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTF zone state -> M15 payload | Information from higher timeframes crosses into decision-bar context — the project's primary lookahead surface |
| multi-symbol frames -> by-symbol join | Cross-symbol contamination would silently corrupt per-instrument evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering/Repudiation (lookahead bias) | htf_context join | high | mitigate | merge_asof direction='backward' with allow_exact_matches=False on confirmation timestamps; strictly-before invariant tested over full chains; DST continuity test; appended-data stability test — all build-breaking |
| T-2-02 | Tampering (NaN/inf poisoning) | htf_context entry | medium | mitigate | V5 guards on m15 and zone frames; pinned empty contracts for missing/empty HTF zones |
| T-2-03 | Tampering (schema drift) | payload frame | medium | mitigate | 14-column payload schema pinned; schema-asserting tests here; Phase 4 feature builder and Phase 6 evidence traces consume exactly these columns |
| T-2-04 | Tampering (input-frame mutation) | htf_context + chain | medium | mitigate | Fresh frames throughout; chain-wide input-equality test |
| T-2-05 | Tampering (cross-symbol contamination) | merge_asof by='symbol' | medium | mitigate | by='symbol' join; dedicated two-symbol isolation test |
| T-2-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No packages installed in Phase 2 (Package Legitimacy Audit: zero adoptions) |
</threat_model>

<verification>
- uv run pytest -q — full unit suite green (all four plans)
- uv run pytest -q -m "unit or mt5" — full suite (no terminal needed for Phase 2 tests)
- uv run ruff check . — clean
- rg -c "import MetaTrader5|from MetaTrader5" src/ai_trading/detectors/ — no matches (SC5)
</verification>

<success_criteria>
- Every M15 decision bar joins H1+H4 zone state whose confirmation is strictly earlier than the bar's time (SMC-06, D-15, ROADMAP SC4)
- Lean payload carries bias, HTF range high/low/equilibrium, ATR-scaled distance-to-equilibrium, and live-zone containment IDs for both HTFs (D-13/D-14)
- The complete chain swings -> zigzag -> pools -> zones -> MTF is proven pure (no vendor imports, no input mutation), deterministic, and schema-locked over synthetic multi-TF frames (SC5) — the exact functions Phase 3's backtester will replay bar-by-bar (BT-01)
- Zero vendor-package imports; conftest.py and _detector_fixtures.py untouched
</success_criteria>

<output>
Create `.planning/phases/02-smc-detection-engine/02-04-SUMMARY.md` when done
</output>
