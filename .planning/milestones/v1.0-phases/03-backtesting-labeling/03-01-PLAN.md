---
phase: 03-backtesting-labeling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai_trading/backtest/__init__.py
  - src/ai_trading/backtest/asof.py
  - src/ai_trading/backtest/chain.py
  - src/ai_trading/backtest/costs.py
  - src/ai_trading/backtest/candidates.py
  - src/ai_trading/backtest/replay.py
  - src/ai_trading/config.py
  - config.toml
  - tests/unit/_backtest_fixtures.py
  - tests/unit/test_backtest_config.py
  - tests/unit/test_asof.py
  - tests/unit/test_costs.py
  - tests/unit/test_candidates.py
  - tests/unit/test_replay.py
  - tests/unit/test_replay_repaint.py
  - tests/unit/test_detector_integration.py
  - tests/unit/test_normalize_and_config.py
autonomous: true
requirements: [BT-01, BT-02]
user_setup:
  - service: "human-decision"
    why: "Confirm the bid-side cost-asymmetry convention (A1: long pays spread at entry, short pays at exit) at phase verification. Pinned by a named test (test_long_short_cost_symmetry); 10-second human eyeball only, non-blocking for implementation."
must_haves:
  truths:
    - "The replay imports and calls the exact Phase 2 detector functions (detect_swings, build_zigzag, detect_pools, derive_zones, htf_context) and never re-implements detection (BT-01)."
    - "Point-in-time visibility uses per-tier stamp semantics (close-time anchors for confirmed_at/created_at/activated_at; bar-time anchors for resolved_at/pierced_at/mitigated_at/invalidated_at); the MTF payload row is consumed as-is (D-15 Phase 2)."
    - "Decision-path prefix stability: candidate/entry/fill decisions are prefix-stable — with the constant fake resolver, replay(bars.iloc[:k]) labels equal the visible subset of replay(full) by entry_time < prefix close time (strict — a D-04 fill from a decision at bar k-1 has entry_time == t_k == prefix_close_time, and that fill cannot exist in the prefix run), check_exact=True, over 1-by-1 and chunked appends (no look-ahead). Outcome evaluation legitimately uses the post-entry window: with a real barrier-style resolver (wired in 03-03) the equality comparison holds for fully-resolved labels (entry_idx + time_barrier_bars <= k); boundary-window labels (post-entry window crossing the prefix boundary) are expected to truncate to TIMEOUT in the prefix run — asserted explicitly as expected behavior, not a leak."
    - "Cost model converts spread POINTS to price via point_size (pip/10), falls back to the per-symbol default when the recorded spread is 0/absent (D-15), and applies the fixed per-symbol slippage pips on BOTH fills (D-14), direction-aware (long crosses spread at entry, short at exit)."
    - "Entry candidate = sweep (SMC-03) + mitigated PD-zone tap on M15 only (D-01, D-06), HTF bias agreement required (D-02), one-at-a-time suppression while a position is open (D-05), silent warmup skip (D-07), structural raw SL/TP (D-08/D-09/D-13), min-R:R discard at fill (D-12)."
    - "New config knobs validate fail-fast in the frozen Config and the backtest package is MT5-free (vendor-import content assertion extended over src/ai_trading/backtest/*.py)."
  artifacts:
    - src/ai_trading/backtest/__init__.py (bare marker, mirror detectors/__init__.py)
    - src/ai_trading/backtest/asof.py (visible_mask, STAMP_CLOSE/STAMP_BAR)
    - src/ai_trading/backtest/chain.py (run_chain)
    - src/ai_trading/backtest/costs.py (pip_size, point_size, effective_spread_points, effective_slippage_pips, entry_fill_price, exit_fill_price)
    - src/ai_trading/backtest/candidates.py (Candidate, candidate_at_bar, bias_agrees, compute_rr)
    - src/ai_trading/backtest/replay.py (Position, LABEL_COLUMNS, auto_warmup_bars, check_history_gate, assert_offset_uniform, replay_symbol)
    - src/ai_trading/config.py (12 new frozen Config fields + _REQUIRED_KEYS + _validate rules)
    - config.toml (backtest knobs section)
    - tests/unit/_backtest_fixtures.py, test_backtest_config.py, test_asof.py, test_costs.py, test_candidates.py, test_replay.py, test_replay_repaint.py
  key_links:
    - chain.run_chain -> detectors.* (same exported functions; no second implementation)
    - replay.replay_symbol -> asof.visible_mask (per-tier anchors) + candidates.candidate_at_bar + costs.entry_fill_price
    - costs spread fallback -> config.default_spread_points(_by_symbol); slippage -> config.slippage_pips(_by_symbol)
    - New Config fields carry dataclass defaults so tests/conftest.py _make_cfg keeps constructing Config unchanged (conftest untouched)
---

<objective>
Build the MT5-free replay engine core for Phase 3: the shared-code-path chain runner (BT-01), the point-in-time as-of slicer, the cost model (BT-02), the D-01..D-13 entry-candidate pure functions (owned here so Phase 6 setup assembly imports the exact same functions), and the forward-pass replay state machine with the D-21 history gate.

Purpose: BT-01 is the phase's core constraint — the backtester must run the identical detector pipeline as live analysis. This plan creates the only code that touches detector output as a service, plus the cost model that makes outcomes truthful (spread in POINTS + slippage, direction-aware).

Output: src/ai_trading/backtest/ package (asof, chain, costs, candidates, replay), extended frozen Config with 12 backtest knobs, config.toml extension, _backtest_fixtures.py test helper, and 7 test modules including the anti-lookahead prefix-equivalence suite.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtesting-labeling/03-CONTEXT.md
@.planning/phases/03-backtesting-labeling/03-RESEARCH.md
@.planning/phases/03-backtesting-labeling/03-PATTERNS.md
@.planning/phases/02-smc-detection-engine/02-VERIFICATION.md

# Prior plan SUMMARYs are NOT needed: this plan is the phase's first code; the
# P2 VERIFICATION and 03-PATTERNS carry the contracts it must preserve.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backtest package scaffold + config knobs + test fixtures (Wave 0)</name>
  <files>src/ai_trading/backtest/__init__.py, src/ai_trading/config.py, config.toml, tests/unit/_backtest_fixtures.py, tests/unit/test_backtest_config.py, tests/unit/test_normalize_and_config.py</files>
  <read_first>
    src/ai_trading/detectors/__init__.py (bare-marker style to mirror, 9 lines)
    src/ai_trading/config.py (full file: _REQUIRED_KEYS, frozen Config, _is_int, _validate)
    config.toml (existing flat-key layout to append to)
    tests/conftest.py (make_bars L26-60 with spread=list(range(count)) zeros; _make_cfg L63-84)
    tests/unit/_detector_fixtures.py (flat_bars L17-31, prefix_close_time L89-93 — reuse, do not duplicate)
    tests/unit/test_normalize_and_config.py (L48-52 _write_config, L55-77 _base_values, parametrized rejection style)
  </read_first>
  <action>
    Create src/ai_trading/backtest/__init__.py as a bare marker: module docstring only ("Pure backtest transforms (asof, chain, candidates, costs, barriers, replay, stats, walkforward, reports, runner): MT5-free by design; import submodules directly — this file stays a bare marker for the whole of Phase 3."), zero imports, zero code, no __all__, no re-exports (mirror detectors/__init__.py verbatim structure).

    Extend src/ai_trading/config.py in place (do NOT create a second config module):
    1. Add `from dataclasses import field` to imports.
    2. Append to _REQUIRED_KEYS: slippage_pips, slippage_pips_by_symbol, default_spread_points, default_spread_points_by_symbol, pip_size, min_rr, time_barrier_bars, wf_train_days, wf_test_days, min_history_days, warmup_bars, htf_warmup_days (fail-fast: a typo'd or missing key refuses load).
    3. Append to the frozen Config dataclass (fields WITH defaults so direct construction, e.g. tests/conftest.py _make_cfg's Config(**defaults), keeps working and conftest stays untouched):
       - slippage_pips: float = 0.5
       - slippage_pips_by_symbol: dict[str, float] = field(default_factory=dict)
       - default_spread_points: int = 20
       - default_spread_points_by_symbol: dict[str, int] = field(default_factory=dict)
       - pip_size: dict[str, float] = field(default_factory=lambda: {"EURUSD": 0.0001, "GBPUSD": 0.0001, "USDJPY": 0.01})
       - min_rr: float = 1.0
       - time_barrier_bars: int = 96
       - wf_train_days: int = 180
       - wf_test_days: int = 30
       - min_history_days: int = 30
       - warmup_bars: int = 0
       - htf_warmup_days: int = 30
    4. In load_config, pass all 12 raw values into the Config(...) constructor (slippage_pips_by_symbol and default_spread_points_by_symbol as dict objects; keep existing behavior otherwise).
    5. In _validate, add after the existing checks, in the existing ValueError(f"{name} must be ...") style:
       - slippage_pips must be a float >= 0 (reject bools); min_rr must be a float > 0.
       - default_spread_points: _is_int and >= 0.
       - pip_size must be a dict; every symbol in cfg.symbols must appear as a key with a numeric value > 0; raise ValueError naming missing symbols.
       - slippage_pips_by_symbol and default_spread_points_by_symbol: every key must be in cfg.symbols and values numerically >= 0.
       - time_barrier_bars, wf_train_days, wf_test_days, min_history_days, htf_warmup_days: _is_int and > 0 (reuse the existing _is_int loop style); warmup_bars: _is_int and >= 0 (0 = auto-compute).

    Append to config.toml a commented "# Backtest / labeling knobs (Phase 3)" block with: slippage_pips = 0.5, default_spread_points = 20, pip_size = { EURUSD = 0.0001, GBPUSD = 0.0001, USDJPY = 0.01 }, min_rr = 1.0, time_barrier_bars = 96, wf_train_days = 180, wf_test_days = 30, min_history_days = 30, warmup_bars = 0, htf_warmup_days = 30, slippage_pips_by_symbol = {}, default_spread_points_by_symbol = {} (inline TOML tables; existing comment style with # documenting defaults).

    Create tests/unit/_backtest_fixtures.py (NEW local helper; MUST NOT mutate tests/conftest.py per the Phase 1 contract and 03-RESEARCH Wave 0 Gaps):
    - Reuse via direct imports: `from conftest import make_bars, _make_cfg` and `from _detector_fixtures import flat_bars, prefix_close_time` (same direct-import convention as Phase 2 test modules).
    - Add set_spreads(bars, values) -> DataFrame: returns a copy with the "spread" column replaced by the given sequence (length must equal len(bars), else ValueError); used to make "recorded spread > 0" and "recorded spread == 0" scenarios deterministic (D-15 fallback tests).
    - Add bt_cfg(**overrides) -> Config: thin wrapper over conftest _make_cfg with Phase-3 defaults (slippage_pips=0.5, default_spread_points=20, pip_size 3 majors, min_rr=1.0, time_barrier_bars=96, wf_train_days=180, wf_test_days=30, min_history_days=30, warmup_bars=0, htf_warmup_days=30) so tests never depend on load_config/files.
    - Add write_bars_parquet(bars, path) -> Path: writes via bar_store.merge_and_write(new=bars, path=path, timeframe=<inferred from cols>) so runner tests reuse the single write path (tmp + os.replace discipline).

    Create tests/unit/test_backtest_config.py mirroring test_normalize_and_config.py structure (_write_config + _base_values with the 12 new keys added, parametrized rejection matrix):
    - Happy path: load_config with all keys returns a Config carrying the exact values (incl. per-symbol override dicts).
    - Rejection cases (each pytest.raises(ValueError, match="<key>")):
      slippage_pips = -0.1; min_rr = 0; default_spread_points = -1; pip_size missing a configured symbol; pip_size value 0; slippage_pips_by_symbol with key outside cfg.symbols; default_spread_points_by_symbol with negative value; time_barrier_bars = 0; wf_train_days = 0; wf_test_days = -5; min_history_days = 0; warmup_bars = -1; missing required key (a _base_values dict with one new key removed -> missing-required-key error).
    - Direct-construction path: Config(**{**fifteen old keys}) uses dataclass defaults for all 12 new fields (proves tests/conftest.py _make_cfg remains valid WITHOUT modifying conftest).

    Modify tests/unit/test_normalize_and_config.py minimally: add the 12 new keys to _base_values (same values as config.toml) so the existing load_config happy-path tests keep passing now that the keys are required. No other change to that file.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_backtest_config.py tests/unit/test_normalize_and_config.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/config.py tests/unit/_backtest_fixtures.py tests/unit/test_backtest_config.py -q</automated>
    <automated>git diff --exit-code tests/conftest.py</automated>
    <automated>git diff --exit-code tests/unit/_detector_fixtures.py</automated>
  </verify>
  <acceptance_criteria>
    - backtest/__init__.py contains zero code: no import/def/class statements (docstring only) — validated by reading the file.
    - config.py Config has exactly the 12 new fields with the given defaults; _REQUIRED_KEYS contains all 12 new names; _validate raises ValueError naming the offending key for each rejection case.
    - config.toml contains every new key with the given values.
    - _backtest_fixtures.py exposes set_spreads, bt_cfg, write_bars_parquet and imports only from conftest/_detector_fixtures/bar_store (no MetaTrader5).
    - `uv run pytest tests/unit -q` is fully green (existing 174 + new phase tests, no regressions), git diff on tests/conftest.py is clean, and ruff on the new files passes.
  </acceptance_criteria>
  <done>
    Frozen Config carries the 12 backtest knobs with fail-fast validation; committed config.toml carries the same keys with documented defaults; backtest package marker exists; _backtest_fixtures.py provides spread sculpting + bt_cfg; the existing suite stays green with conftest untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: As-of slicer + chain runner + cost model (BT-01/BT-02 core)</name>
  <files>src/ai_trading/backtest/asof.py, src/ai_trading/backtest/chain.py, src/ai_trading/backtest/costs.py, tests/unit/test_asof.py, tests/unit/test_costs.py, tests/unit/test_detector_integration.py</files>
  <read_first>
    src/ai_trading/detectors/mtf.py (_is_live L115-118, strictly-before as-of discipline, bias mapping L174-178: close>eq -> bearish / <eq -> bullish / == eq -> neutral)
    src/ai_trading/detectors/atr.py (L23-50 validate-then-compute pure-fn style + "invariant violated" ValueError)
    src/ai_trading/normalize.py (COLUMNS L26-39 incl. "spread"; TIMEFRAME_MINUTES L39)
    tests/unit/test_detector_integration.py (run_chain L118-145 exact composition; test_no_vendor_imports_in_detector_sources L380-390)
    tests/unit/test_mtf_join.py (as-of visibility test conventions)
  </read_first>
  <action>
    Create src/ai_trading/backtest/asof.py — the column-aware point-in-time visibility helper (the single anti-lookahead choke point):
    - Module constants STAMP_CLOSE = "close" and STAMP_BAR = "bar".
    - close_time_of(bar_t, timeframe) -> pd.Timestamp: bar_t + pd.Timedelta(minutes=TIMEFRAME_MINUTES[timeframe]) (import TIMEFRAME_MINUTES from ai_trading.normalize — single source of truth).
    - visible_mask(df, col, stamp_kind, bar_t, close_t) -> pd.Series: anchor = close_t if stamp_kind is STAMP_CLOSE else bar_t; return df[col].notna() & (df[col] <= anchor) (NaT stamps excluded — never happened).
    - Docstring must pin the heterogeneous stamp table: close-time stamps (swings.confirmed_at, zigzag.confirmed_at, zones.created_at, pools.activated_at) use the close anchor; bar-time stamps (pools.pierced_at, pools.resolved_at, zones.mitigated_at, zones.invalidated_at) use the bar anchor; MTF payload rows are consumed as-is and NEVER re-anchored. Add the note that a single <= t_S filter over close-time stamps is off by one bar.
    - Missing column -> ValueError(f"visible_mask invariant violated: {col!r} not in frame columns"). Never mutate the input frame; keep dtype-neutral.

    Create src/ai_trading/backtest/chain.py — the BT-01 single-pass chain runner:
    - run_chain(m15, h1, h4) -> dict[str, pd.DataFrame]: exactly the composition of test_detector_integration.run_chain (L118-145): detect_swings(m15, "M15") -> build_zigzag -> detect_pools(m15, swings15) -> (pools15, events15); detect_swings(h1, "H1") -> build_zigzag -> derive_zones(zigzag_h1, h1); same for H4; htf_context(m15, zones_h1, zones_h4) -> payload. Return dict keys: swings15, zigzag15, pools15, events15, swings_h1, zigzag_h1, zones_h1, swings_h4, zigzag_h4, zones_h4, payload.
    - Import style: direct submodule imports (e.g. from ai_trading.detectors.pools import detect_pools; from ai_trading.detectors.zones import derive_zones; from ai_trading.detectors.mtf import htf_context) — mirror detectors/pools.py L47-48 convention. Zero file I/O, zero MetaTrader5 imports. Note in docstring: BT-01 requires these exact exported functions; never re-implement detection for speed.

    Create src/ai_trading/backtest/costs.py — pure cost arithmetic, validate-then-compute (atr.py style):
    - pip_size(cfg, symbol) -> float: cfg.pip_size[symbol]; raise ValueError naming the symbol if absent.
    - point_size(cfg, symbol) -> float: pip_size / 10 (5-digit: 1 point = pip/10; USDJPY 0.001).
    - effective_spread_points(cfg, symbol, bar_spread) -> int: bar_spread if bar_spread > 0 else cfg.default_spread_points_by_symbol.get(symbol, cfg.default_spread_points) (D-15 fallback; recorded spread 0/absent is the dominant path in stored data).
    - effective_slippage_pips(cfg, symbol) -> float: cfg.slippage_pips_by_symbol.get(symbol, cfg.slippage_pips) (D-14 per-symbol override).
    - entry_fill_price(direction, bar_open, bar_spread, cfg, symbol, apply_slippage=True) -> float (Pattern 4): long = bar_open + spread_px + (slip if apply_slippage else 0); short = bar_open - (slip if apply_slippage else 0). spread_px = effective_spread_points * point_size; slip = effective_slippage_pips * pip_size.
    - exit_fill_price(direction, level, bar_spread, cfg, symbol, apply_slippage=True) -> float: long = level - (slip if apply_slippage else 0); short = level + spread_px + (slip if apply_slippage else 0).
    - Docstrings MUST state units explicitly (Pitfall 2): stored spread is POINTS (MqlRates semantics; 5-digit quote 1 pip = 10 points), never pips and never price; pip size is 0.0001 for EURUSD/GBPUSD and 0.01 for USDJPY. Direction-aware asymmetry: bars are bid-side, long crosses the spread at entry, short at exit (A1).
    - Validators: direction must be "long"/"short" else ValueError with an "invariant violated" message; bar_spread negative -> ValueError.

    Create tests/unit/test_asof.py (mirror test_mtf_join.py conventions, @pytest.mark.unit, direct imports of _backtest_fixtures):
    - close-kind stamps: stamp == close_t visible; stamp == bar_t (one TF earlier) NOT visible; stamp after close_t NOT visible.
    - bar-kind stamps: stamp == bar_t visible (bar-time events are known at bar close); stamp == close_t NOT visible at bar_t.
    - NaT/None stamps excluded (never-happened events).
    - close_time_of uses TIMEFRAME_MINUTES (M15 +15min; H4 +240min) and accepts float/bool timestamp inputs unchanged.
    - visible_mask with a missing column raises ValueError matching "invariant violated".

    Create tests/unit/test_costs.py (pure-math style like test_normalize_and_config.py; literal expected floats):
    - points->price: EURUSD spread 20 -> 0.0002; spread 42 -> 0.00042; USDJPY spread 20 -> 0.020; point_size EURUSD 0.00001, USDJPY 0.001.
    - D-15 fallback: bar_spread 0 -> default_spread_points (20 -> 0.0002); bar_spread 18 -> 18 (no fallback); default_spread_points_by_symbol override wins over scalar default.
    - D-14: effective_slippage_pips returns per-symbol override when present, scalar default otherwise.
    - Fill math literal: long entry = open + spread_px + slip; short entry = open - slip; long exit = level - slip; short exit = level + spread_px + slip (assert exact float equality with hand-computed values, e.g. EURUSD open 1.10000, spread 20 pt, slip 0.5 pip -> long entry 1.10020 + 0.00005 = 1.10025).
    - test_long_short_cost_symmetry: identical bar paths long vs short -> round-trip net cost = spread_px + 2*slip for BOTH directions (Pitfall 8: no double-charged spread).
    - raw vs net (D-16): round-trip RAW cost = spread_px only; NET = spread_px + 2*slip_px; delta = exactly 2*slip_px (PRICE units — the R-unit expression of this delta, -2x slippage/structural risk per convention (a), is specified in 03-02's barrier R math).
    - Wave-0 sanity: for EURUSD/GBPUSD/USDJPY sampled spreads, spread_pips = points/10 lies in [0.1, 10] (Pitfall 2).
    - apply_slippage=False returns the raw (spread-only) fill; invalid direction raises ValueError.

    Extend tests/unit/test_detector_integration.py: in test_no_vendor_imports_in_detector_sources, also glob src/ai_trading/backtest/*.py and assert MetaTrader5 never appears as a module import in those files (same content-scan style as L380-390; prose docstring mentions are allowed, imports are not).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_asof.py tests/unit/test_costs.py tests/unit/test_detector_integration.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest tests/unit/test_asof.py tests/unit/test_costs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - asof.visible_mask returns True exactly for rows whose stamp is not NaT and <= the right anchor (close vs bar per stamp_kind); test_asof.py covers both kinds at boundary equality.
    - chain.run_chain returns the 11-key dict and calls ONLY detector exports (no backtest-side detection code); vendor-import assertion covers the backtest package.
    - costs.py: all six functions exist with the stated signatures; every test_costs.py literal-float assertion passes including the symmetry test and the raw/net delta of exactly 2x slippage (PRICE units; the R-unit form per convention (a) is specified in 03-02).
    - Full unit suite green; ruff clean.
  </acceptance_criteria>
  <done>
    BT-01 pipeline (chain once) + as-of slicer + BT-02 cost model are implemented as pure MT5-free functions with per-tier visibility tests, literal cost-math tests, and an extended vendor-purity assertion; the D-15 fallback branch and both fill directions are pinned.
  </done>
</task>

<task type="auto">
  <name>Task 3: Entry-candidate pure functions + forward-pass replay + anti-lookahead proof</name>
  <files>src/ai_trading/backtest/candidates.py, src/ai_trading/backtest/replay.py, tests/unit/test_candidates.py, tests/unit/test_replay.py, tests/unit/test_replay_repaint.py</files>
  <read_first>
    src/ai_trading/detectors/pools.py (POOL_COLUMNS L50-61, EVENT_COLUMNS L62-74, _detect_for_symbol L174-284 event-emission/loop-local state style, _emit L160-172)
    src/ai_trading/detectors/swings.py (SWING columns incl. price + confirmed_at close-time stamp — the D-09 prior-confirmed-swing TP source)
    src/ai_trading/detectors/zones.py (ZONE_COLUMNS L36-51, _zones_for_symbol per-bar walk L159-176)
    src/ai_trading/detectors/mtf.py (PAYLOAD_COLUMNS L41-56, bias semantics L174-178)
    tests/unit/_detector_fixtures.py (assert_point_in_time_prefix_equality L96-109, prefix_close_time L89-93)
    tests/unit/test_repaint.py (T1 prefix-equality conventions)
    .planning/phases/03-backtesting-labeling/03-RESEARCH.md (Pattern 2 L270-288, Pattern 5 L314-329; Code Example 1 for barrier shape only — barrier walk itself ships in plan 03-02)
  </read_first>
  <action>
    Create src/ai_trading/backtest/candidates.py — the D-01..D-04, D-08..D-13 entry rules as PURE functions (owned here; Phase 6 setup assembly imports these exact functions per D-03):
    - Candidate frozen dataclass fields: symbol, timeframe ("M15"), direction ("long"/"short"), sl_price, tp_price, entry_bar_idx, pool_id, event_id, zone_id, bias_h1, bias_h4.
    - bias_agrees(bias_h1, bias_h4, direction) -> bool: want = "bullish" if direction == "long" else "bearish"; True iff bias_h1 == want or bias_h4 == want (D-02: at least one HTF lean agrees; the other may be neutral/absent, never needed).
    - candidate_at_bar(state, cfg) -> Candidate | None where state bundles the as-of slices: m15_bars (DecisionBar context), events15 (visible sweep events: event_type, pool_id, side, level, resolved_at), zones15 (M15 zones: zone_id, range_high, range_low, equilibrium, state, mitigated_at, leg_direction), pools15 (pool level/index for SL/TP), swings15 (prior confirmed M15 swing levels: price, confirmed_at — close-time stamps; D-09 prior-confirmed-swing TP source), payload_row (Series with bias_h1, bias_h4). Semantics pinned (planner composition per RESEARCH Open Question 4 — M15 zones for the tap, HTF payload for bias only):
      1. Latest visible sweep: events15 where event_type == "sweep" and resolved_at <= bar_t (bar anchor); take max resolved_at. None -> None.
      2. Tap trigger: zones15 rows with state == "mitigated" and mitigated_at == bar_t (the zone is mitigated ON this decision bar — each tap emits once). None -> None.
      3. Direction: long if the mitigation bar close <= zone.equilibrium (discount) AND the sweep's pool side == "low"; short if close >= zone.equilibrium (premium) AND pool side == "high"; close == equilibrium or side mismatch -> None (D-01 "right side": discount for longs, premium for shorts).
      4. HTF bias: bias_agrees(payload_row["bias_h1"], payload_row["bias_h4"], direction) must be True, else None (D-02); bias values are recorded on the candidate as-is.
      5. SL (D-08 structural raw, no buffer): long = min(sweep_pool.level, zone.range_low); short = max(sweep_pool.level, zone.range_high) — beyond BOTH the swept pool level and the zone far boundary.
      6. TP (D-09 structural, R:R varies, no fixed R:R, no cap): NEAREST structural target strictly beyond the zone's far boundary, considering BOTH D-09 alternatives. Liveness convention (D-09 is silent on it): opposite pools must still be LIVE at the decision — a pool already resolved/swept before the decision provides no remaining liquidity, so resolved pools are EXCLUDED from TP candidates. long: candidates = {opposite-side pools with side == "high", activated_at <= close_t (close-time anchor), level > zone.range_high, AND (resolved_at.isna() | (resolved_at > bar_t)) (bar-time anchor per Pattern 1)} ∪ {prior confirmed swings from swings15 with confirmed_at <= close_t (close-time anchor) and price > zone.range_high}; if the candidate union is non-empty -> TP = the minimum level in the union (nearest structural target beyond the zone), else TP = zone.range_high (the far side of the PD zone). short mirrored: side == "low" pools with level < zone.range_low AND live ((resolved_at.isna() | (resolved_at > bar_t))), prior confirmed swings with price < zone.range_low -> TP = the maximum such level, else zone.range_low. Same priority for both alternatives — whichever target is nearest beyond the zone wins; ties between pool and swing resolve to the pool target. Never cap R:R. Docstring states the liveness decision explicitly (resolved pools not selectable; D-09 unpinned -> planner convention, module-documented).
      7. entry_bar_idx = index of the next bar (S+1) — the caller passes the index; no fill math here (D-04).
    - compute_rr(direction, entry, sl, tp) -> float: long (tp - entry)/(entry - sl); short (entry - tp)/(sl - entry). D-12 filtering happens at fill (replay) because the entry price is the next bar's open.
    - Docstring cites D-01..D-04, D-08..D-13 and states Phase 6 imports these functions (BT-01/D-03).

    Create src/ai_trading/backtest/replay.py — the forward-pass state machine (pools.py _detect_for_symbol sequential-loop style, deliberately not vectorized):
    - Position frozen dataclass: symbol, timeframe, direction, entry_open (structural next-bar open), entry_spread_points (effective, recorded-or-defaulted), entry_price (net filled), sl_price, tp_price, entry_idx, entry_time, evidence: dict (pool_id/event_id/zone_id/bias_h1/bias_h4), rr.
    - LABEL_COLUMNS pinned tuple (the single label schema): ("symbol","timeframe","direction","entry_time","entry_price","sl_price","tp_price","rr","pool_id","event_id","zone_id","bias_h1","bias_h4","outcome","exit_time","exit_price","exit_idx","r_gross","r_raw","r_net").
    - auto_warmup_bars(cfg) -> int: cfg.warmup_bars if > 0 else 28 (ATR(14) NaN head + 2-bar fractal confirmation + one completed zigzag leg margin); docstring states D-07 (silent skip, never an error).
    - check_history_gate(days_stored, min_history_days, symbol, timeframe) -> None (D-21): if days_stored < min_history_days raise RuntimeError with an actionable message containing the symbol, timeframe, both day counts and the remedy text "extend collection (Phase 1 purge + backfill) or pass --min-history-days override"; never a silent empty report.
    - assert_offset_uniform(bars) -> None (Pitfall 10): compute bars["time"] - bars["time_utc"]; if more than one distinct Timedelta -> RuntimeError naming the symbol and the mixed offsets (DST/offset mixing must not silently reach labels).
    - replay_symbol(m15_bars, chain, cfg, resolver) -> pd.DataFrame with exactly LABEL_COLUMNS:
      - Validators (per-symbol, never global): required columns present, time_utc monotonic and unique, else ValueError with "replay_symbol invariant violated" (mirror pools.py _validate_bars).
      - resolver: Callable[[Position, pd.DataFrame, Config], dict] returning {outcome, exit_time, exit_price, exit_idx, r_gross, r_raw, r_net} — the interface contract implemented by plan 03-02 (walk_barriers); runner wires it in plan 03-03. Document that this is the interface-first seam.
      - Loop i over bars: decision at close of bar i (bar_t = time_utc[i], close_t = bar_t + TF delta). Warmup: i < auto_warmup_bars(cfg) -> continue silently (D-07).
      - D-05: if a position is open (not yet resolved) -> skip generation. Slot-release timing: the resolver is invoked ONCE at fill time and returns the complete outcome including exit_idx; the label row is appended at fill time with those outcome/exit fields from that single resolver call, but the slot remains held until the loop advances past the resolver's exit_idx — only after i > exit_idx does the slot free (a naive next-bar release would allow overlapping trades, violating D-05).
       - State assembly per bar: as-of slices via asof.visible_mask with the correct per-tier anchors (close-kind vs bar-kind) — swings15 uses STAMP_CLOSE on confirmed_at, pools15 uses STAMP_CLOSE on activated_at (and the slice must expose both levels and any bar-time lifecycle stamps), events15 uses STAMP_BAR on resolved_at, zones15 uses STAMP_BAR on mitigated_at; payload row = chain["payload"] row where time_utc == bar_t, used as-is (never re-anchored; D-15 Phase 2).
      - cand = candidate_at_bar(...); None -> continue.
      - entry_idx = i + 1; if entry_idx >= len(m15_bars) -> break (no fill without a next bar, D-04).
      - Fill (D-04/D-14/D-15): entry_open = bars.iloc[entry_idx]["open"]; spread = effective_spread_points(cfg, symbol, bars.iloc[entry_idx]["spread"]); entry_price = costs.entry_fill_price(direction, entry_open, ...); rr = compute_rr(direction, entry_open, sl, tp); if rr < cfg.min_rr -> discard, no position, no label (D-12).
      - label = resolver(position, m15_bars, cfg); append assembled row (dtype-pinned: StringDtype for strings, datetime64[us] for times, float64 for prices/R).
      - Empty result must be a schema-correct empty frame with LABEL_COLUMNS and pinned dtypes (detector empty-frame contract); input frames never mutated.
    - Docstring cites D-04, D-05, D-07, D-12, D-21 and states MT5-free.

    Create tests/unit/test_candidates.py (sculpted worlds via _backtest_fixtures + _detector_fixtures; hand-derivable expected values):
    - sweep (pool side "low") then mitigation bar in discount -> Candidate long with exact SL = min(pool.level, zone.range_low), exact TP (zone.range_high and opposite-pool override case).
    - D-09 prior-confirmed-swing TP: no opposite pool beyond the zone but a prior confirmed swing level (swings15 confirmed_at <= close_t) beyond it -> TP = that swing level; also the nearest-wins case where BOTH an opposite pool and a prior swing exist beyond the zone (nearest target from the union, tie -> pool), and the fallback case where neither exists -> TP = zone far boundary. Mirrored short variants.
    - test_resolved_opposite_pool_not_selected_as_tp: an opposite pool beyond the zone whose resolved_at <= bar_t (already swept/resolved before the decision) is NOT a TP candidate — TP falls through to the prior-swing / far-boundary alternative; an unresolved pool (resolved_at is NaT or > bar_t) IS selectable.
    - mirrored short case (pool side "high", premium).
    - sweep-only (no mitigated zone this bar) -> None; zone-only (no sweep) -> None (D-01).
    - bias disagrees (bearish H1 + bearish H4 for long) -> None; agrees via H4 only -> Candidate with both bias values recorded (D-02).
    - side mismatch (sweep high + discount) -> None.
    - tie/edge: close == equilibrium -> None; compute_rr literal values (24 pips R:R 2:1 etc.).
    - entry_bar_idx == S+1.

    Create tests/unit/test_replay.py:
    - Warmup (Pitfall 9): bars shorter than warmup -> zero labels AND no raised errors; candidate emitted at the first eligible bar after warmup (sculpt a sweep+mitigation at the first post-warmup bar).
    - D-05 one-at-a-time: while a position's label is unresolved, a second valid tap produces no second label; after resolution the next tap labels.
    - D-04/D-14/D-15: entry_price == next bar open + spread_px + slip (long) with recorded and fallback spread variants.
    - D-12: candidate whose fill-time rr < cfg.min_rr yields no label; rr >= min_rr yields a label.
    - Labelled row schema == LABEL_COLUMNS; empty input -> empty frame with exactly LABEL_COLUMNS and pinned dtypes.
    - check_history_gate: below minimum raises RuntimeError whose message contains symbol, timeframe, stored days, min days and the remedy text; at/above minimum does not raise.
    - assert_offset_uniform: mixed offsets -> RuntimeError; uniform -> passes.
    - resolver interface: a fake resolver returning a fixed dict is accepted and its values land in the label row.

    Create tests/unit/test_replay_repaint.py — THE anti-lookahead suite (BT-01/SC1; adapt assert_point_in_time_prefix_equality from _detector_fixtures):
    - runner = lambda bars: replay_symbol(bars, chain, cfg, fake_resolver) — the same CONSTANT fake resolver as test_replay (returns a fixed dict, no barrier window, so exact equality holds over ALL labels).
    - 1-by-1 prefixes and chunked appends: for k in range(min_prefix, len): labels of replay(bars.iloc[:k]) must equal exactly the rows of replay(full) whose entry_time < prefix_close_time(bars, k) (STRICT inequality — make_bars bars are contiguous so prefix_close_time(bars, k) == t_k, and a full-run fill decided at bar k-1 has entry_time == t_k but cannot exist in replay(bars.iloc[:k]); a `<=` filter would fail the anti-lookahead test on a correct engine), reset_index, pd.testing.assert_frame_equal(..., check_exact=True) (use _detector_fixtures.prefix_close_time).
    - test_prefix_boundary_timeout_truncation (documented behavior; complements the 03-03 real-resolver wiring): runner_bound = replay_symbol(bars, chain, cfg, barrier_stub) where barrier_stub is a LOCAL test stub implementing the documented window-clamp semantics (clamps [entry_idx, entry_idx + time_barrier_bars) to len(bars); an incomplete window -> TIMEOUT at the last available bar, exactly as 03-02's walk_barriers end-of-data lens). Assert: (a) for every prefix k, every label of the prefix run with entry_idx + time_barrier_bars <= k is byte-identical to the same fully-resolved row of the full run; (b) labels whose post-entry window crosses the prefix boundary are TIMEOUT in the prefix run (truncated by design) — asserted as EXPECTED behavior, never treated as a leak; (c) the decision path (candidate -> entry -> fill fields) is prefix-stable for BOTH cases.
    - test_no_future_bar_access: every label's entry_idx and exit_idx stay within the processed prefix (spot check).
    - test_labels_deterministic: same inputs run twice -> byte-identical DataFrames.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_candidates.py tests/unit/test_replay.py tests/unit/test_replay_repaint.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest tests/unit/test_candidates.py tests/unit/test_replay.py tests/unit/test_replay_repaint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - candidates.py exposes Candidate, bias_agrees, candidate_at_bar, compute_rr with the pinned semantics; every test_candidates.py case (sweep-only None, zone-only None, bias veto, side mismatch, exact SL/TP, opposite-pool TP override, prior-confirmed-swing TP, nearest-wins TP union, rr literals) passes.
    - replay.py exposes Position, LABEL_COLUMNS, auto_warmup_bars, check_history_gate, assert_offset_uniform, replay_symbol; replay_symbol accepts the (Position, bars, Config) -> dict resolver contract; D-05 suppression, D-07 silent warmup, D-12 discard, and D-21 gate all pass their named tests.
    - test_replay_repaint.py proves decision-path prefix stability (1-by-1 AND chunked, check_exact=True) and determinism; the boundary-window truncation behavior is asserted as expected (test_prefix_boundary_timeout_truncation); no label references a bar beyond its prefix.
    - Full unit suite green; ruff clean.
  </acceptance_criteria>
  <done>
    The one shared code path is now executable: chain -> as-of slice -> candidate -> next-bar-open fill with costs -> resolver seam -> LABEL_COLUMNS rows, with the replay-level look-ahead proof green and the history gate refusing short ranges with an actionable message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored bars -> replay engine | untrusted/past data crosses into labels; correctness (not poison) is the risk |
| detector output -> as-of slicer -> candidates | the single future-data choke point; a slice bug leaks post-decision info into labels |
| config.toml -> costs/candidates | config knobs (pip_size, spread/slippage, min_rr) silently shift every cost; fail-fast validation is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | as-of slicer / replay engine (look-ahead leakage) | critical | mitigate | visible_mask is the single visibility helper with two anchors; per-tier test_asof.py; replay-level prefix-equivalence suite test_replay_repaint.py (1-by-1 + chunked, check_exact=True); detectors themselves already repaint-proven (Phase 2) |
| T-03-02 | Repudiation | cost model (spread points vs pips vs price) | high | mitigate | single costs.py conversion; units stated in module docstrings (POINTS, never pips/price); literal-float tests; Wave-0 sanity spread_pips in [0.1, 10]; raw vs net delta pinned to exactly 2x slippage in PRICE units (the R-unit form -2x slippage/structural risk lives in 03-02 per convention (a)) |
| T-03-03 | Denial of Service (of the evidence) | history gate (D-21) | high | mitigate | check_history_gate refuses below min_history_days with actionable message (symbol, TF, days, remedy); never emit empty-trade reports as success; runner wiring lands in 03-03 but the gate is unit-tested here |
| T-03-04 | Tampering | config knobs (invalid/bounds) | medium | mitigate | frozen Config + _REQUIRED_KEYS fail-fast; _validate bounds (positive ints/floats, pip_size covers every symbol, override keys subset of symbols); parametrized rejection matrix in test_backtest_config.py |
| T-03-05 | Tampering | cross-symbol contamination | medium | mitigate | per-symbol validators (monotonic unique time_utc per symbol) and per-symbol replay loop; never validate/concatenate multi-symbol frames globally |
| T-03-06 | Tampering | package installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: no installs; vectorbt/quantstats rejected by design) |
</threat_model>

<verification>
- Scope note (rubric): this plan intentionally carries 17 files_modified — above the 15-file rubric. **Accepted mitigation = per-task commit discipline**: Tasks 1-3 commit independently (task-scoped commits with co-located tests and their own green gate), so no verification scope exceeds one task's commit. Restructure rejected: the 17 files form three coherent seams (config+fixtures / asof+chain+costs / candidates+replay+repaint); splitting them would cut the BT-01 core path (chain->asof->candidate->replay) across plan boundaries.
- Per task commit: `uv run pytest tests/unit -q` (must stay green; existing 174 tests never regress).
- Per wave merge: full `uv run pytest -q` plus `uv run ruff check .`.
- Wave-0 gate: tests/conftest.py and tests/unit/_detector_fixtures.py remain untouched (`git diff --exit-code` both) — Phase 1/2 contract.
- Anti-lookahead gates: `uv run pytest tests/unit/test_replay_repaint.py -q` and `tests/unit/test_asof.py -q` are green before this plan is done.
- Human-verify note (per 03-VALIDATION manual-only row): confirm the A1 cost-asymmetry convention (long pays spread at entry, short at exit) — pinned by test_long_short_cost_symmetry; 10-second eyeball at phase verification.
</verification>

<success_criteria>
- replay engine imports and calls the same detector exports as live analysis (grep-verifiable; no detection logic in backtest/) — BT-01.
- cost model is direction-aware (spread once per round trip) with D-15 fallback and D-14 slippage, proven by literal-float tests — BT-02.
- No look-ahead: prefix-equivalence proof is green over the full replay path (candidate -> entry -> label) — SC1 preservation.
- Traveled ~15 files + config; every new module is MT5-free and ruff-clean; full unit suite ≥ 174 + new tests green.
</success_criteria>

## Artifacts this phase produces

**New modules (src/ai_trading/backtest/):**
- `__init__.py` — bare package marker
- `asof.py` — `STAMP_CLOSE`, `STAMP_BAR`, `close_time_of(bar_t, timeframe)`, `visible_mask(df, col, stamp_kind, bar_t, close_t)`
- `chain.py` — `run_chain(m15, h1, h4) -> dict[str, pd.DataFrame]` (keys swings15/zigzag15/pools15/events15/swings_h1/zigzag_h1/zones_h1/swings_h4/zigzag_h4/zones_h4/payload)
- `costs.py` — `pip_size`, `point_size`, `effective_spread_points`, `effective_slippage_pips`, `entry_fill_price(direction, bar_open, bar_spread, cfg, symbol, apply_slippage=True)`, `exit_fill_price(...)` (apply_slippage=False = raw/spread-only variant)
- `candidates.py` — `Candidate` dataclass (symbol, timeframe="M15", direction, sl_price, tp_price, entry_bar_idx, pool_id, event_id, zone_id, bias_h1, bias_h4), `bias_agrees(bias_h1, bias_h4, direction)`, `candidate_at_bar(state, cfg)`, `compute_rr(direction, entry, sl, tp)`
- `replay.py` — `Position` dataclass (symbol, timeframe, direction, entry_open, entry_spread_points, entry_price, sl_price, tp_price, entry_idx, entry_time, evidence, rr), `LABEL_COLUMNS` (20 cols: symbol, timeframe, direction, entry_time, entry_price, sl_price, tp_price, rr, pool_id, event_id, zone_id, bias_h1, bias_h4, outcome, exit_time, exit_price, exit_idx, r_gross, r_raw, r_net), `auto_warmup_bars(cfg)`, `check_history_gate(days_stored, min_history_days, symbol, timeframe)`, `assert_offset_uniform(bars)`, `replay_symbol(m15_bars, chain, cfg, resolver)` where resolver: `(Position, DataFrame, Config) -> dict`

**Extended files:**
- `src/ai_trading/config.py` — +12 frozen Config fields (slippage_pips, slippage_pips_by_symbol, default_spread_points, default_spread_points_by_symbol, pip_size, min_rr, time_barrier_bars, wf_train_days, wf_test_days, min_history_days, warmup_bars, htf_warmup_days) + _REQUIRED_KEYS + _validate rules
- `config.toml` — backtest knobs section
- `tests/unit/test_normalize_and_config.py` — _base_values gains the 12 keys (only change)
- `tests/unit/test_detector_integration.py` — vendor-purity assertion extends over backtest/*.py

**New test files:** `tests/unit/_backtest_fixtures.py` (set_spreads, bt_cfg, write_bars_parquet), `test_backtest_config.py`, `test_asof.py`, `test_costs.py`, `test_candidates.py`, `test_replay.py`, `test_replay_repaint.py`

**New config keys (config.toml):** slippage_pips, default_spread_points, pip_size, min_rr, time_barrier_bars, wf_train_days, wf_test_days, min_history_days, warmup_bars, htf_warmup_days, slippage_pips_by_symbol, default_spread_points_by_symbol

<output>
Create `.planning/phases/03-backtesting-labeling/03-01-SUMMARY.md` when done
</output>
