---
phase: 04-ml-scoring
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/ai_trading/ml/heuristic.py
  - src/ai_trading/ml/evaluate.py
  - src/ai_trading/ml/runner.py
  - src/ai_trading/ml/__main__.py
  - tests/unit/test_ml_heuristic.py
  - tests/unit/test_ml_evaluate.py
  - tests/unit/test_ml_runner.py
autonomous: true
requirements: [AI-04, AI-02]
user_setup: []

must_haves:
  truths:
    - "Every labeled candidate row in the walk-forward eval receives a probability: rows in trainable windows are ML-scored, rows in skipped/starved windows are heuristic-scored, and every score row records its score_source (ml or heuristic) — no score without provenance"
    - "Headline metrics (AUC, log loss, Brier) are computed on decided rows only; TIMEOUT rows appear in the scores artifact with their probabilities flagged excluded (D-01/D-02 score-and-flag), so no metric is contaminated and no information is lost"
    - "Windows that fail the tiny-window preflight are skipped-and-recorded with a stable reason (never raised, never fabricated); their test spans are covered by the flagged heuristic"
    - "The retrain CLI reuses the Phase 3 walk-forward harness as the only splitter, enforces the label-count gate with an actionable D-21-style refusal, writes the feature audit artifact, and follows the {2 config, 1 runtime, 0 success} exit-code contract including zero-decided refusals"
    - "Eval reports are written atomically as data/reports/ml_walkforward.parquet, ml_reliability.parquet, and ml_scores.parquet with deterministic filenames and --write gating"
  artifacts:
    - path: "src/ai_trading/ml/heuristic.py"
      provides: "WEIGHTS + heuristic_score — deterministic, versioned, [0,1] non-ML bootstrap score (Pattern 6)"
    - path: "src/ai_trading/ml/evaluate.py"
      provides: "run_walkforward_eval — per-window train-or-skip loop, decided-only headline metrics, flagged scores, reliability rows"
    - path: "src/ai_trading/ml/runner.py"
      provides: "run_ml_pipeline + main CLI (python -m ai_trading.ml) — label-count gate, full wiring, artifact save on --train, atomic report writes"
    - path: "src/ai_trading/ml/__main__.py"
      provides: "module entry mirroring backtest/__main__.py"
    - path: "tests/unit/test_ml_heuristic.py, test_ml_evaluate.py, test_ml_runner.py"
      provides: "Named tests for determinism/range/flags, decided-only headline + TIMEOUT flagging, gate refusals + exit codes + artifact layout"
  key_links:
    - from: "ml/evaluate.py"
      to: "backtest/walkforward.py build_windows + label_window_assignment"
      via: "eval windows and row assignment come from the Phase 3 harness unchanged"
      why: "AI-04/SC3: the harness is the only splitter; per-window attribution is shared with Phase 3 reports"
    - from: "ml/runner.py"
      to: "data/labels/*.parquet (Phase 3 output) → data/reports/ + data/models/"
      via: "labels loaded from the label store; features built via plan-01 builder; audit artifact written per run; bundle saved via plan-02 artifact API"
      why: "D-05 pipeline-first: the gate refuses starvation data with the backfill remedy; synthetic runs prove the chain end-to-end"
    - from: "ml/heuristic.py"
      to: "ml/features.py FEATURE_SPEC"
      via: "heuristic weights are keyed by feature names and versioned with feature_list_version"
      why: "the bootstrap scores the same feature vector the ML model sees; flagged score_source keeps it out of ML headline metrics"
---

<objective>
Deliver the walk-forward evaluation reports with decided-only headline metrics and flagged TIMEOUT scores, the deterministic heuristic-score bootstrap for unlabeled/starved periods, and the retrain CLI that wires the full Phase 4 chain behind the label-count gate (AI-04, AI-02 completeness, D-02, D-05).

Purpose: this plan is where Phase 4 becomes evidence — per-window metrics mirroring the Phase 3 report conventions, honesty flags on every score, and a runnable `python -m ai_trading.ml` whose refusals tell the user exactly what data task unlocks real training.

Output: src/ai_trading/ml/{heuristic,evaluate,runner,__main__}.py, three test modules, and the artifact/report layout under data/ proven by tests on synthetic stores.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ml-scoring/04-CONTEXT.md
@.planning/phases/04-ml-scoring/04-RESEARCH.md
@.planning/phases/04-ml-scoring/04-01-SUMMARY.md
@.planning/phases/04-ml-scoring/04-02-SUMMARY.md

# Source-of-truth code
@src/ai_trading/backtest/walkforward.py
@src/ai_trading/backtest/runner.py
@src/ai_trading/backtest/__main__.py
@src/ai_trading/backtest/reports.py
@src/ai_trading/backtest/stats.py
@src/ai_trading/ml/features.py
@src/ai_trading/ml/train.py
@src/ai_trading/ml/artifact.py
@src/ai_trading/ml/scorer.py
@tests/unit/_ml_fixtures.py
@tests/unit/test_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ml/heuristic.py — deterministic flagged non-ML bootstrap score</name>
  <files>src/ai_trading/ml/heuristic.py, tests/unit/test_ml_heuristic.py</files>
  <read_first>
    - src/ai_trading/ml/features.py (FEATURE_SPEC names + semantics — the heuristic scores the same vector)
    - src/ai_trading/config.py (ml_feature_list_version — weights versioning anchor)
    - src/ai_trading/backtest/candidates.py (D-02 bias_agrees semantics mirrored by htf_bias_agreement)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Pattern 6 heuristic design; A4 acceptance framing; Open Question 3)
  </read_first>
  <action>
    1. Implement heuristic.py as a pure module: a module-level WEIGHTS dict keyed by a fixed subset of FEATURE_SPEC numeric feature names (rr_at_decision, zone_position, htf_bias_agreement, bars_since_sweep inverted to recency favoring fresher sweeps, atr14 inverted as the regime term, sl_dist_atr) with hand-set float weights documented inline with a one-line rationale each, plus HEURISTIC_VERSION tied to cfg.ml_feature_list_version in the docstring. Implement normalize(value, feature_name) -> float applying a documented fixed linear map per feature (recency and inverse-ATR terms mapped so smaller-is-better; rr and zone_position already roughly unit-scale; agreement scaled from its 0/1/2 range) with missing values contributing zero. Implement heuristic_score(features: pd.DataFrame) -> pd.Series computing the logistic of the weight-normalized sum, clipped into [0, 1], deterministic, vectorized over rows (no per-row Python loop over the frame beyond pandas ops). Missing features contribute zero (documented); the score never reads label-side columns — it consumes the same feature frame the ML path consumes.
    2. Write tests/unit/test_ml_heuristic.py: test_heuristic_deterministic_same_input_same_output (two calls byte-identical); test_heuristic_in_unit_range (synthetic frames incl. extreme and missing values stay within [0, 1]); test_heuristic_missing_features_contribute_zero (a frame of all-missing numerics scores exactly the logistic of zero); test_heuristic_monotone_in_rr (raising rr_at_decision with all else fixed never lowers the score); test_heuristic_weights_subset_of_feature_spec (every WEIGHTS key is a FEATURE_SPEC name — keeps the bootstrap honest when the feature list evolves); test_heuristic_pure_no_io (no file writes, inputs unmutated).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_heuristic.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_heuristic.py -q` all green, including the six named tests.
    - Determinism and [0,1] range hold on seeded synthetic frames including all-missing rows.
    - Every WEIGHTS key is a FEATURE_SPEC name (test-enforced), and the module carries the feature_list_version tie in its docstring.
    - `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>Heuristic bootstrap exists: deterministic, bounded, missing-safe, spec-keyed and versioned — ready for evaluate.py to flag its rows score_source="heuristic".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ml/evaluate.py — walk-forward eval loop with decided-only headline and flagged scores</name>
  <files>src/ai_trading/ml/evaluate.py, tests/unit/test_ml_evaluate.py</files>
  <read_first>
    - src/ai_trading/backtest/walkforward.py (build_windows, label_window_assignment, Window — the only splitter)
    - src/ai_trading/ml/purge.py + folds.py + train.py (plan 02 outputs consumed here)
    - src/ai_trading/ml/heuristic.py (Task 1 output — skip-window fallback)
    - src/ai_trading/backtest/stats.py (nan-propagation and decided-denominator conventions the report must stay consistent with)
    - src/ai_trading/ml/features.py (feature frame alignment contract: row i corresponds to labels row i)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Pattern 5 skip contract; Pattern 6 heuristic coverage; Pitfall 12 TIMEOUT contamination; Code Example 5)
  </read_first>
  <action>
    1. Implement run_walkforward_eval(labels, features, windows, cfg) -> an EvalResult dataclass with fields report, reliability, scores (all pd.DataFrame). Validate first: features row count equals labels row count (the builder's alignment invariant) and both non-empty frames share row order; raise a ValueError naming the misalignment otherwise. Assign windows via walkforward.label_window_assignment (labels carry entry_time; every label lands in exactly one window per the harness contract).
    2. Per window: build the train slice from the window's train_mask restricted to decided rows (D-01: the third outcome class never trains) with the plan-02 purge applied over exit times (plan-02 purged_train_mask with cfg.ml_embargo_bars); derive calibration folds over the train slice via plan-02 calibration_folds with cfg.ml_cal_train_days/ml_cal_test_days/ml_embargo_bars; run the plan-02 preflight (cfg.ml_min_train_labels + both classes + at least two viable folds). On preflight failure: record a report row {window_id, status "skipped", reason (the stable preflight prefix), n_train_decided, n_test_decided, n_timeout_flagged, auc/log_loss/brier as NaN} and score the window's test rows through the heuristic with score_source "heuristic" (Pattern 6: skipped spans stay scoreable; AUC-style metrics never see heuristic rows).
    3. On preflight pass: subset the feature frame to exactly the FEATURE_SPEC names before any fit or score (the model matrix excludes entry_time/decision_close_time and every non-spec alignment column); fit via plan-02 fit_calibrated on the decided train slice; score ALL test rows (probabilities for every candidate row — D-02); compute headline metrics on decided test rows only with the nan guards (roc_auc_score yields NaN with reason when the test slice is single-class; log_loss always called with labels [0, 1]; brier on decided rows); record the report row {window_id, status "ok", reason NaN, n_train_decided, n_test_decided, n_timeout_flagged, auc, log_loss, brier, provenance "window_model"}. Reliability: call the plan-02 reliability_curve on the decided test probabilities and tag each row with the window_id; collect one pooled reliability set over all decided test rows across windows tagged with window_id -1 (the pooled scope marker documented in the docstring).
    4. Scores artifact rows (one per label row, label order preserved): symbol, timeframe, entry_time, direction, outcome, window_id, p_win, excluded (boolean — True exactly for the TIMEOUT outcome rows per D-02's score-and-flag; decided rows False), score_source ("ml" or "heuristic"), provenance ("window_model" or "heuristic"), artifact_version (NaN in eval — per-window models are transient; the versioned bundle is plan-02's saved artifact that the runner records on the report summary). Every row carries its provenance — a score without provenance is a bug (research anti-pattern).
    5. Write tests/unit/test_ml_evaluate.py on synthetic labels + features (seeded, roughly 100 rows over multi-day entry times so several windows pass preflight and at least the first window starves): test_decided_only_headline_metrics (TIMEOUT rows excluded from auc/log_loss/brier; n_timeout_flagged equals the TIMEOUT count; D-01); test_timeout_rows_scored_and_flagged_excluded (every TIMEOUT row has a finite p_win with excluded True and its window's score_source; D-02); test_single_class_test_window_yields_nan_metrics_no_raise; test_starved_window_skipped_and_recorded (first window status "skipped" with the stable reason prefix; no exception); test_skipped_window_span_heuristic_scored (score_source "heuristic" for those rows only); test_every_label_row_scored_exactly_once (scores row count equals label count; window coverage complete); test_reliability_rows_per_window_plus_pooled (window-tagged rows plus the -1 pooled scope; empty when no window passes preflight); test_eval_never_mutates_inputs_and_report_sorted (inputs unmutated; report sorted by window_id); test_no_shuffled_splits_used (windows parameter flows from build_windows; assert window ids ascending and assignment via label_window_assignment — the harness is the only splitter, AI-04).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_evaluate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_evaluate.py -q` all green, including the nine named tests.
    - The TIMEOUT flagging test proves D-02 mechanically: flagged rows keep finite scores and never enter headline metrics.
    - The skip test proves Pattern 5: starvation records status "skipped" with the stable reason prefix and covers the span heuristically — never raises.
    - Every score row carries score_source and provenance; scores row count equals label count.
    - `uv run pytest tests/unit -q` full suite green; `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>Walk-forward eval produces the three report shapes with decided-only headline metrics (A4-consistent, D-01), score-and-flagged TIMEOUTs (D-02), skip-and-record starvation handling (Pattern 5), and provenance on every score — all over the Phase 3 harness's windows (AI-04).</done>
</task>

<task type="auto">
  <name>Task 3: ml/runner.py + ml/__main__.py — retrain CLI with label-count gate and artifact/report writes</name>
  <files>src/ai_trading/ml/runner.py, src/ai_trading/ml/__main__.py, tests/unit/test_ml_runner.py</files>
  <read_first>
    - src/ai_trading/backtest/runner.py (THE CLI pattern to mirror: exit-code contract {2,1,0}, _output_dirs data-root guard, argparse shape, summary block, dataclasses.replace override discipline)
    - src/ai_trading/backtest/reports.py (write_labels layout data/labels/{SYMBOL}_{TF}.parquet the runner reads; config_hash; atomic writer pattern)
    - src/ai_trading/ml/features.py + audit.py (plan 01: build_feature_frame per symbol + run_feature_audit/write_feature_audit)
    - src/ai_trading/ml/train.py + artifact.py + scorer.py (plan 02: fit/preflight/reliability + save_artifact/load path)
    - src/ai_trading/ml/evaluate.py (Task 2 output)
    - src/ai_trading/stores/bar_store.py (read_bars/bar_path — the only bars input path, MT5-free)
    - tests/unit/_ml_fixtures.py + tests/unit/_backtest_fixtures.py write_bars_parquet (building a synthetic store for runner tests)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (architecture diagram steps 1-2 gate; D-05 pipeline-first resolution; Open Question 1 recommendation)
  </read_first>
  <action>
    1. Implement src/ai_trading/ml/runner.py mirroring the backtest runner's structure and contracts. CLI flags exactly: --config (default config.toml), --train (full pipeline ending in a saved versioned artifact), --eval (walk-forward eval reports without saving an artifact), --write (persist artifacts; dry-run summary when absent). Specifying neither --train nor --eval is a config error (exit 2, message naming the required flags). Module docstring states the exit-code contract verbatim in the backtest runner's shape: 2 config errors (load_config ValueError, missing mode flag), 1 runtime refusals (label-count gate, missing/unreadable label store, feature/label schema invariants), 0 success — including runs where every window skips (heuristic-only scores are a valid outcome, logged as INFO).
    2. Label-count gate (D-21 style, D-05): resolve the data root as the bars_dir parent with the same resolve-under-root guard as the backtest runner (path traversal refused); load every parquet under data/labels/ (a missing directory or zero files is an exit-1 refusal with an actionable message naming the directory, the Phase 3 backtest command that populates it, and the MT5 history-backfill prerequisite); concat into one frame; compute the decided count and class presence; refuse with exit 1 when decided rows are below cfg.ml_min_train_labels or a class is missing — the message names the observed decided/wins/losses counts, the required threshold, and the remedy (complete the Phase 3 backtest run and deepen stored history via the Phase 1 collector backfill; the heuristic bootstrap inside eval covers thin periods once the gate passes on any future store). The gate runs before any feature building or artifact write.
    3. Pipeline (both modes): per configured symbol present in the labels — read M15/H1/H4 bars via read_bars, run run_chain over the loaded ranges, build_feature_frame per symbol, concat; run the plan-01 feature audit and write feature_audit.json on --write (the audit runs on every training run per SC1). Derive windows exclusively via build_windows over the combined entry times with cfg.wf_test_days/wf_train_days; call run_walkforward_eval. On --train: build the final decided-only training set over the whole store, derive final calibration folds via plan-02 calibration_folds over all decided entry/exit times, re-run the preflight (refuse exit 1 on failure with the same actionable shape), fit via fit_calibrated, and save via save_artifact with the bundle metadata (cv_fold_summary from the final folds, training_window span, label counts incl. purged/timeout counts, config_hash, reliability_summary — pooled calibration-curve stats over the final folds' decided predictions per plan-02 build_bundle, completing the SC2 retention in the manifest alongside the ml_reliability.parquet rows written on --write).
    4. Report writes on --write (atomic, deterministic filenames, no run ids in data bytes — mirror the reports.py determinism contract): data/reports/ml_walkforward.parquet (the eval report frame sorted by window_id), data/reports/ml_reliability.parquet, data/reports/ml_scores.parquet; feature_audit.json as above. The summary block prints: symbols, decided/timeout counts, gate status, windows trained vs skipped (with reasons), pooled reliability summary, artifact version saved (or eval-only), and written paths. created_at-style run metadata stays in the summary/log layer only, never in the parquet bytes.
    5. Implement src/ai_trading/ml/__main__.py as the module entry calling runner.main (mirror backtest/__main__.py). Write tests/unit/test_ml_runner.py using a synthetic data root built with _ml_fixtures/write_bars_parquet and label parquet files written through the Phase 3 write path (never a second Parquet writer): test_missing_labels_dir_exit_1_actionable (message names the directory and both remedies); test_gate_refusal_exit_1_names_counts_and_threshold (a store with fewer decided rows than cfg.ml_min_train_labels — message carries the observed counts and threshold); test_missing_mode_flag_exit_2; test_config_error_exit_2 (bad --config path); test_full_train_run_exit_0_writes_artifacts (store seeded past the gate: pooled/v1 + LATEST.json + the three report parquets + feature_audit.json exist; exit 0); test_eval_mode_writes_reports_not_artifact (reports exist; no new version dir); test_dry_run_writes_nothing (no --write: zero artifacts created, summary still printed); test_all_windows_skipped_exit_0 (a store where every window starves: exit 0, skipped statuses recorded, heuristic-scored rows present in the scores artifact — Pattern 5 end-to-end); test_output_dir_traversal_refused (a bars_dir whose parent resolves outside the data root is refused per the backtest runner guard).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_runner.py -q && uv run pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_runner.py -q` all green, including the nine named tests; full suite green afterwards.
    - The gate refusal message names the observed decided/wins/losses counts, cfg.ml_min_train_labels, and the backtest-plus-backfill remedy (D-21-style actionable refusal, D-05).
    - test_full_train_run_exit_0_writes_artifacts proves the complete chain on synthetic data: labels → gate → features → audit artifact → windows → per-window eval → final calibrated model → versioned bundle + LATEST + three reports.
    - Exit codes observed exactly: 2 for config errors, 1 for gate/schema refusals, 0 for success including all-skipped windows.
    - No run ids or created_at-style run metadata in parquet bytes — entry_time/exit_time are data columns, not run metadata, so the deterministic-filename contract is about filenames and run ids, not data timestamps; all writes go through tmp + os.replace helpers.
    - `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>Phase 4 is runnable end-to-end: `python -m ai_trading.ml --config config.toml --train --write` enforces the label-count gate with an actionable data-task message, proves the whole chain on synthetic stores in tests, writes the SC1 audit artifact per run, saves the SC4 bundle on --train, and emits the three eval reports under data/reports/ with the Phase 3 runner's exit-code discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/labels/*.parquet → runner | stored label files cross into training/eval; schema violations must refuse loudly |
| eval report frames → data/reports/ | scored outputs become downstream evidence; provenance and flags must survive serialization |
| CLI args → pipeline | --range/--write style inputs must not escape the data root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Repudiation (metric integrity) | ml/evaluate.py headline metrics | critical | mitigate | Decided-only headline pinned by named test (D-01); TIMEOUT rows flagged excluded with scores kept (D-02); n_timeout_flagged column present; nan guards on single-class test slices |
| T-04-02 | Repudiation (evidence integrity) | eval window derivation | critical | mitigate | Windows and assignment come exclusively from build_windows + label_window_assignment (AI-04); no new splitter exists in the module; fold discipline inherited from plan 02 |
| T-04-10 | Repudiation | score provenance | high | mitigate | Every scores row carries score_source + provenance; heuristic rows are structurally barred from ML headline metrics; flagged bootstrap is visually separable for Phase 5/6 |
| T-04-06 | Information disclosure | runner output dirs | medium | mitigate | Data-root resolve-under guard copied from the backtest runner; fixed config-driven filenames only; no user-supplied filenames |
| T-04-04 | Tampering | gate + config consumption | high | mitigate | Label-count gate refuses starvation data with counts + threshold + remedy; ml knobs arrive pre-validated by the plan-01 frozen Config |
| T-04-SC | Tampering | (inherited) pinned stack | low | accept | No installs in this plan; versions verified in plan 01 Task 1 |
</threat_model>

<verification>
- `uv run pytest tests/unit -q` — full suite green
- `uv run pytest tests/unit/test_ml_heuristic.py tests/unit/test_ml_evaluate.py tests/unit/test_ml_runner.py -q` — AI-04/AI-02/D-02/D-05 named tests green
- `uv run ruff check .` — clean
- End-to-end synthetic proof: `uv run python -m ai_trading.ml --config <test config> --train --write` (in tests) exits 0 with the full artifact layout
- Vendor purity: no MetaTrader5 import under src/ai_trading/ml/ (plan-01 static test still passes)
- File-count mitigation (accepted, as in Phase 3): per-task commit discipline — one commit per task with the unit suite green before each commit — keeps every review/rollback scope to a single task
</verification>

<success_criteria>
- Every labeled row scored with provenance; decided-only headline metrics; TIMEOUTs flagged excluded with scores (D-01/D-02)
- Starved windows skip-and-record with heuristic coverage — never a crash, never a fabricated model (Pattern 5/6)
- Walk-forward eval runs exclusively on the Phase 3 harness windows (AI-04/SC3)
- Retrain CLI enforces the label-count gate with an actionable refusal and honors the {2,1,0} exit-code contract; --train saves the versioned artifact; reports land atomically under data/reports/ (D-05, SC1, SC4 wiring)
- Deep-history backfill remains documented as the human data task (Phase 3 UAT deferral) — the phase succeeds on synthetic + harness evidence
</success_criteria>

## Artifacts this phase produces

**New files:** `src/ai_trading/ml/heuristic.py`, `src/ai_trading/ml/evaluate.py`, `src/ai_trading/ml/runner.py`, `src/ai_trading/ml/__main__.py`, plus `tests/unit/test_ml_heuristic.py`, `test_ml_evaluate.py`, `test_ml_runner.py`

**Symbols in ml/heuristic.py:** `WEIGHTS` (dict keyed by FEATURE_SPEC numeric names), `HEURISTIC_VERSION`, `normalize(value, feature_name) -> float`, `heuristic_score(features: pd.DataFrame) -> pd.Series`

**Symbols in ml/evaluate.py:** `EvalResult` (dataclass: report, reliability, scores), `run_walkforward_eval(labels, features, windows, cfg) -> EvalResult`

**Symbols in ml/runner.py:** `run_ml_pipeline(cfg, mode, write) -> dict`, `main(argv) -> int`; **CLI flags:** `--config` (default config.toml), `--train`, `--eval`, `--write`; **exit codes:** 2 config / 1 runtime refusal / 0 success (mirrors backtest runner)

**Module entry:** `python -m ai_trading.ml` via `src/ai_trading/ml/__main__.py`

**On-disk layout created:** `data/reports/feature_audit.json`, `data/reports/ml_walkforward.parquet` (per-window eval rows, sorted by window_id), `data/reports/ml_reliability.parquet` (window-tagged + pooled scope), `data/reports/ml_scores.parquet` (per-row p_win, excluded flag, score_source, provenance); on `--train`: `data/models/pooled/v{N}/` + `data/models/LATEST.json` (via plan-02 save_artifact)

**Phase-wide symbol inventory (all three plans):** config fields `ml_feature_list_version` … `ml_retrain_interval_hours` (13 keys, plan 01); functions `features_at_decision`, `build_feature_frame`, `audit_prefix_equivalence`, `audit_spec_coverage`, `run_feature_audit`, `write_feature_audit`, `purged_train_mask`, `calibration_folds`, `decided_mask`, `preflight_reason`, `lgbm_params`, `fit_lightgbm`, `fit_calibrated`, `reliability_curve`, `build_bundle`, `save_artifact`, `load_artifact`, `load_scorer`, `Scorer.score`, `Scorer.contributors`, `heuristic_score`, `run_walkforward_eval`, `run_ml_pipeline`, `main`; CLI `python -m ai_trading.ml --config --train --eval --write`

<output>
Create `.planning/phases/04-ml-scoring/04-03-SUMMARY.md` when done
</output>
