---
phase: 04-ml-scoring
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/ai_trading/ml/purge.py
  - src/ai_trading/ml/folds.py
  - src/ai_trading/ml/train.py
  - src/ai_trading/ml/artifact.py
  - src/ai_trading/ml/scorer.py
  - tests/unit/test_ml_purge.py
  - tests/unit/test_ml_folds.py
  - tests/unit/test_ml_train.py
  - tests/unit/test_ml_artifact.py
  - tests/unit/test_ml_scorer.py
autonomous: true
requirements: [AI-02, AI-03]
user_setup: []

must_haves:
  truths:
    - "Train labels whose outcome resolves inside a test window are purged: exit_time at or after the window start is dropped and an exit closing exactly at the window start is kept (both boundary sides pinned by named tests); the embargo knob extends the boundary by whole bars"
    - "Every train/test and calibration fold boundary derives from walkforward.build_windows — expanding, chronological, purged, zero shuffles; the calibration wrapper refuses to run without explicit folds, and a source assertion proves no default-cv call site exists"
    - "Training is deterministic: identical inputs and seeds reproduce identical probabilities (retrain-identical named test); no imbalance reweighting flags are set because they distort the probability estimates this phase exists to produce"
    - "The artifact bundle (fitted calibrator + feature metadata + provenance) saves under data/models/pooled/v{N}/ with manifest.json + LATEST.json atomically, and the loader validates schema version, feature list version, and library versions before returning a Scorer"
    - "Scorer.score returns calibrated P(WIN) per row with categories restored from the artifact's categorical specs; Scorer.contributors returns per-row per-feature contributions plus a bias column; single-row scoring works"
  artifacts:
    - path: "src/ai_trading/ml/purge.py"
      provides: "purged_train_mask — the OQ5 obligation (Phase 3 deferred) implemented over entry/exit stamps"
    - path: "src/ai_trading/ml/folds.py"
      provides: "calibration_folds — expanding purged fold pairs derived exclusively from build_windows (SC3)"
    - path: "src/ai_trading/ml/train.py"
      provides: "decided_mask, preflight_reason, lgbm_params, fit_lightgbm, fit_calibrated, reliability_curve — pure training core (D-01 decided-only, sigmoid default, deterministic)"
    - path: "src/ai_trading/ml/artifact.py"
      provides: "ARTIFACT_SCHEMA_VERSION, build_bundle, save_artifact, load_artifact — versioned bundle + manifest (incl. SC2 reliability summary + stamped artifact_version) + LATEST pointer (SC4)"
    - path: "src/ai_trading/ml/scorer.py"
      provides: "load_scorer + Scorer.score/Scorer.contributors — the loadable, MT5-free score API Phase 6 consumes"
  key_links:
    - from: "ml/folds.py"
      to: "backtest/walkforward.py build_windows"
      via: "fold pairs are positional projections of Window train/test masks with the purge applied — never a new splitter"
      why: "SC3/AI-04: the Phase 3 harness is the only splitter of record"
    - from: "ml/train.py"
      to: "sklearn.calibration.CalibratedClassifierCV"
      via: "ensemble=True + explicit chronological fold iterable + method from cfg.ml_calibration_method (sigmoid default)"
      why: "AI-03/SC2: verified-correct calibration shape; ensemble=False is a verified partition error on expanding folds"
    - from: "ml/artifact.py"
      to: "data/models/pooled/v{N}/ + data/models/LATEST.json"
      via: "atomic joblib + JSON writes following the reports.py tmp+os.replace discipline"
      why: "SC4 versioned artifacts; Phase 6 loads through ml.scorer"
    - from: "ml/scorer.py"
      to: "04-01 FEATURE_SPEC"
      via: "feature_names + categorical_specs stored in the bundle; scorer reindexes and restores category dtypes before predict"
      why: "Pitfall 7/10: category dtype and feature-list drift between training and scoring are refused or restored, never silent"
---

<objective>
Deliver the LightGBM P(WIN) training core with chronological-fold calibration, the OQ5 boundary purge, and the versioned artifact bundle + loadable scorer (AI-02, AI-03, ROADMAP SC2/SC3/SC4).

Purpose: this plan turns 04-01's leak-free features into calibrated probabilities without ever letting time ordering break — inside training, inside calibration folds, or at the artifact boundary. The purge closes Phase 3's deferred OQ5 obligation; the artifact + scorer give Phase 6 a loadable, version-validated entry point.

Output: src/ai_trading/ml/{purge,folds,train,artifact,scorer}.py and five test modules, all pure and MT5-free.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ml-scoring/04-CONTEXT.md
@.planning/phases/04-ml-scoring/04-RESEARCH.md
@.planning/phases/04-ml-scoring/04-01-SUMMARY.md

# Source-of-truth code
@src/ai_trading/backtest/walkforward.py
@src/ai_trading/backtest/asof.py
@src/ai_trading/backtest/replay.py
@src/ai_trading/config.py
@src/ai_trading/ml/features.py
@tests/unit/_ml_fixtures.py
@src/ai_trading/backtest/reports.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Boundary purge (OQ5) + harness-derived calibration folds</name>
  <files>src/ai_trading/ml/purge.py, src/ai_trading/ml/folds.py, tests/unit/test_ml_purge.py, tests/unit/test_ml_folds.py</files>
  <read_first>
    - src/ai_trading/backtest/walkforward.py (build_windows contract, Window dataclass, module docstring's documented Phase-4 purge obligation, D-19/D-20 semantics)
    - src/ai_trading/backtest/replay.py (LABEL_COLUMNS: entry_time/exit_time stamps — exit_time is the exit bar's OPEN time; the boundary semantics follow from this)
    - src/ai_trading/normalize.py (TIMEFRAME_MINUTES for the embargo bar math)
    - src/ai_trading/config.py (ml_embargo_bars, ml_cal_train_days, ml_cal_test_days knobs from plan 01)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Pattern 4 purge/embargo spec incl. both boundary cases; Pattern 3 fold derivation; Pitfall 8)
  </read_first>
  <action>
    1. Implement src/ai_trading/ml/purge.py with purged_train_mask(train_mask, exit_times, test_start, embargo_bars=0, bar_minutes=15) -> pd.Series: boolean Series aligned to train_mask's index, True where the row is in train_mask AND exit_times is not NaT AND exit_times is strictly before (test_start − embargo_bars × bar_minutes minutes). This single keep-condition implements both mechanics: with embargo 0 it is the mandatory purge (a label with exit_time at or after test_start is dropped — its outcome resolves inside the test window; an exit_time one bar before test_start closes exactly at the window start and is kept), and embargo_bars = N extends the cut N bars earlier as a serial-correlation guard. Document in the module docstring why the default is 0 (the 96-bar barrier means purge already removes every overlapping label; embargo only guards regime continuity and costs train depth on the tiny store — revisit after deep backfill), and that exit_time being the exit bar's OPEN time is what makes both boundary cases exact. Never mutate inputs; NaT exit_times are conservatively excluded from training.
    2. Implement src/ai_trading/ml/folds.py with calibration_folds(entry_times, exit_times, test_days, train_days, embargo_bars=0) -> list of (train_positions, test_positions) pairs: derive windows exclusively via walkforward.build_windows over entry_times (raise the harness's own validation errors — no new splitter, SC3); for each window compute the purged train positions (purged_train_mask applied to the window's train_mask, converted to positional indices over the reset input order) and the test positions from the window's test_mask; keep only pairs where both sides are non-empty; return positional index lists suitable as an sklearn cv iterable over the training slice. Document that positions are 0-based positional indices into the (positionally-indexed) training slice — callers must pass reset-index frames. Empty entry_times yields an empty list (supported state, mirrors the harness's zero-label contract).
    3. Write tests/unit/test_ml_purge.py: test_purge_drops_exit_at_test_start (exit_time equal to test_start dropped); test_purge_keeps_exit_closing_at_test_start (exit_time one bar before test_start kept with embargo 0); test_purge_embargo_extends_boundary (embargo_bars=1 drops that same row; embargo_bars=2 drops two bars earlier); test_purge_preserves_mask_alignment_and_inputs (result aligned to input index, train_mask rows outside train stay False, inputs unmutated); test_purge_nat_exit_excluded (NaT exit row never trains).
    4. Write tests/unit/test_ml_folds.py: test_folds_derived_from_build_windows_expanding (fold k's train positions are a superset of fold k−1's; test blocks are sequential, non-overlapping, ascending); test_folds_purge_applied_at_every_boundary (a label whose exit_time falls at or after a fold's test_start is absent from that fold's train positions but present in earlier folds); test_folds_embargo_applied (embargo_bars=1 shrinks every fold's train positions by the expected rows); test_folds_empty_entry_times_returns_empty_list; test_folds_reject_harness_invalid_days (test_days=0 propagates the harness's ValueError). Use _ml_fixtures.make_labels entry/exit times as positional series.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_purge.py tests/unit/test_ml_folds.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_purge.py tests/unit/test_ml_folds.py -q` all green, including the ten named tests (five per module).
    - Both boundary cases of the purge are pinned by their own named tests, and the embargo test proves the knob moves the boundary by whole bars.
    - folds.py imports build_windows from backtest.walkforward and contains no other windowing logic (source review; no second splitter exists).
    - Both modules are pure: no I/O, no vendor imports, inputs unmutated (asserted via the preservation tests).
    - `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>OQ5 closed: purge drops exactly the overlapping-outcome train labels with both boundary sides pinned; embargo knob semantics pinned; calibration folds derive exclusively from build_windows with purge applied at every fold boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ml/train.py — preflight gate, deterministic LightGBM, chronological calibration, reliability helper</name>
  <files>src/ai_trading/ml/train.py, tests/unit/test_ml_train.py</files>
  <read_first>
    - src/ai_trading/ml/purge.py + src/ai_trading/ml/folds.py (Task 1 outputs — train consumes them)
    - src/ai_trading/ml/features.py (FEATURE_SPEC categorical names — the category-dtype columns)
    - src/ai_trading/config.py (ml_* knobs: ml_calibration_method, ml_random_state, ml_n_estimators, ml_num_leaves, ml_min_data_in_leaf, ml_learning_rate, ml_min_train_labels, ml_cal_train_days, ml_cal_test_days)
    - tests/unit/_ml_fixtures.py (synthetic_feature_frame + make_labels for training-shaped tests)
    - src/ai_trading/backtest/barriers.py (OUTCOME_WIN/OUTCOME_LOSS/OUTCOME_TIMEOUT constants — D-01 decided classes)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Pattern 3 calibration mechanics + verified ensemble=True trap; Pattern 5 tiny-window preflight; Pitfalls 2, 3, 5, 6, 11; Code Examples 1-3)
  </read_first>
  <action>
    1. Implement decided_mask(outcomes) -> pd.Series: True where outcome is WIN or LOSS (import the outcome constants from backtest.barriers; D-01: the training target is binary among decided trades, the third class is excluded from fitting and headline metrics). Implement preflight_reason(y_decided, min_labels, folds=None) -> str or None: return None when the decided count is at least min_labels AND both classes are present AND (when folds is given) at least two folds remain after dropping folds whose test block is single-class — otherwise return a reason string starting with one of the stable prefixes "insufficient_decided_labels", "single_class", "insufficient_calibration_folds", each carrying the observed numbers. CalibratedClassifierCV needs at least two viable folds; a single viable fold cannot calibrate and is a skip/refuse condition, never a silent uncalibrated fallback (SC2).
    2. Implement lgbm_params(cfg) -> dict returning exactly: objective "binary", n_estimators/num_leaves/min_data_in_leaf/learning_rate/random_state from the cfg.ml_* knobs, deterministic True, force_row_wise True, num_threads 1, verbose -1, boosting "gbdt". Deliberately omit the imbalance reweighting knobs (is_unbalance / scale_pos_weight): LightGBM's own docs state they damage individual class probability estimates, which is the exact quantity this phase produces; among decided trades at R:R >= 1 the imbalance is mild and calibration handles the residual mapping. Document this omission in the docstring. Implement fit_lightgbm(X, y, cfg) -> fitted LGBMClassifier: cast every FEATURE_SPEC categorical column of X to pd.category dtype (idempotent), then fit with lgbm_params. Implement fit_calibrated(X, y, folds, method, cfg) -> fitted CalibratedClassifierCV: folds is a REQUIRED positional parameter with no default (the SC3 no-default-cv rule made structural); raise ValueError on an empty or single-fold list naming the requirement; construct the calibrator with estimator=fit_lightgbm(...), method=method, cv=folds, ensemble=True — never ensemble False (verified: it routes through cross_val_predict which demands partitions, impossible under expanding chronology) and never cv=None or an int (the silent StratifiedKFold shuffle trap). Fit on the decided rows only.
    3. Implement reliability_curve(y_true, p_pred, n_bins=5) -> pd.DataFrame wrapping sklearn.calibration.calibration_curve with strategy "quantile": return columns bin (int64), prob_pred, prob_true (float64), count (int64 per bin). Guard: fewer than 2 distinct predicted values or a single-class y returns a schema-correct empty frame (mirrors the nan-with-reason convention; never raises). This helper is the SC2 reliability-data source evaluate.py persists per window.
    4. Write tests/unit/test_ml_train.py on _ml_fixtures synthetic frames (n around 100 decided rows, both classes, seeded): test_decided_mask_excludes_third_class; test_preflight_reason_insufficient_decided_labels (prefix "insufficient_decided_labels" when below cfg.ml_min_train_labels); test_preflight_reason_single_class; test_preflight_reason_insufficient_calibration_folds (folds list where only one fold's test block has both classes); test_preflight_passes_returns_none; test_fit_lightgbm_deterministic_probabilities (two independent fits on the same data produce identical predict_proba arrays); test_lgbm_params_no_imbalance_flags (the returned dict contains neither reweighting key); test_fit_calibrated_requires_explicit_folds (empty folds and single-fold raise naming the requirement; the folds parameter has no default — assert via inspect.signature); test_fit_calibrated_end_to_end_sigmoid (fit with plan-01-derived folds from folds.calibration_folds; predict_proba returns values in [0,1] for every row); test_no_default_cv_call_sites (AST scan of ml/train.py: every CalibratedClassifierCV instantiation passes cv from the folds parameter and ensemble=True — mirrors the Phase 2 source-assertion pattern); test_reliability_curve_shape_and_counts (columns and dtypes; count sums to n when non-empty; single-class input yields the empty schema-correct frame). Keep individual tests fast (tiny trees, n_estimators around 40 where determinism is not under test).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_train.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_train.py -q` all green, including the eleven named tests.
    - test_fit_lightgbm_deterministic_probabilities proves byte-identical predict_proba across refits (deterministic=True, force_row_wise=True, num_threads=1, fixed seed).
    - The AST scan test proves every calibration call site passes explicit folds with ensemble=True; inspect.signature proves folds has no default.
    - No test trains on the third outcome class (decided_mask gates every fit path; D-01).
    - `uv run ruff check .` passes; training tests keep the module under the ~30s per-file budget.
  </acceptance_criteria>
  <done>Training core is deterministic, decided-only (D-01), calibrated through CalibratedClassifierCV with structurally-required explicit chronological folds (SC3), preflight-guarded against starvation windows (Pattern 5), and exposes the SC2 reliability helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ml/artifact.py + ml/scorer.py — versioned bundle, manifest, LATEST pointer, loadable scorer</name>
  <files>src/ai_trading/ml/artifact.py, src/ai_trading/ml/scorer.py, tests/unit/test_ml_artifact.py, tests/unit/test_ml_scorer.py</files>
  <read_first>
    - src/ai_trading/ml/train.py (Task 2 output — the fitted calibrator being bundled)
    - src/ai_trading/ml/features.py (FEATURE_SPEC — feature_names, categorical_specs, feature_list_version stored in the bundle)
    - src/ai_trading/backtest/reports.py (config_hash + the atomic tmp+os.replace writer pattern to copy)
    - src/ai_trading/config.py (ml knobs + frozen Config)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Pattern 7 bundle spec + loader contract; Pitfalls 7, 10; Security Domain pickle-deserialization row)
  </read_first>
  <action>
    1. Implement src/ai_trading/ml/artifact.py: module constant ARTIFACT_SCHEMA_VERSION = 1. build_bundle(model, cfg, feature_names, categorical_specs, cv_fold_summary, training_window, label_counts, reliability_summary=None) -> dict carrying exactly the SC4 metadata: artifact_schema_version, feature_names (ordered list from FEATURE_SPEC), categorical_specs (dict name to ordered category list), feature_list_version (cfg.ml_feature_list_version), model (the fitted calibrator), method (cfg.ml_calibration_method), cv_fold_summary, seeds (cfg.ml_random_state), config_hash (reuse backtest.reports.config_hash), training_window (dict start/end), label_counts (dict decided/wins/losses/timeout counts), reliability_summary (dict — pooled calibration-curve statistics over the final training folds' decided predictions, the SC2 retention the manifest carries; empty dict when reliability cannot be computed — this satisfies SC2's "the artifact retains reliability data" wording alongside the full per-window/pooled reliability rows the runner persists at data/reports/ml_reliability.parquet, the Pattern 7 reconciliation), sklearn_version, lightgbm_version, created_at. save_artifact(bundle, models_dir) -> Path: resolve the next version N as one plus the max existing v{N} directory number under models_dir/pooled (1 when none), stamp artifact_version = N into the bundle dict before serialization (so the loaded Scorer reports it without path parsing), write pooled/v{N}/model.joblib via joblib.dump through a local atomic tmp + os.replace helper (copy the reports.py pattern — tmp sibling, unlinked on failure), then pooled/v{N}/manifest.json as human-readable JSON with the same metadata minus the pickled model (json-safe scalars only, including artifact_version and reliability_summary), then data/models/LATEST.json as the pointer {artifact_version, path, config_hash} written atomically after the version dir completes. Document the integrity contract in the module docstring: bundles are produced only by this repo's own training runs; loading third-party joblib files is outside the trust boundary (loader validation is the gate, not a sandbox).
    2. Implement load_artifact(path) -> dict: joblib.load then validate in order — artifact_schema_version equals ARTIFACT_SCHEMA_VERSION (raise ValueError naming expected vs found), feature_list_version equals the current FEATURE_SPEC version (raise naming both), installed sklearn/lightgbm major versions match the recorded ones (raise on major mismatch, log a warning on minor mismatch), and required bundle keys are present (raise naming the missing keys). Document that model bundles are not held to byte-determinism (pickle framing may vary); determinism is pinned at the probability level and manifest level per the research deviation note.
    3. Implement src/ai_trading/ml/scorer.py: load_scorer(models_root_or_path) -> Scorer accepts either a models root containing LATEST.json (resolve the pointer's path) or a version directory containing model.joblib; delegates validation to load_artifact. Scorer stores the bundle and exposes: score(features) -> pd.DataFrame reindexing the input frame to the bundle's feature_names order (missing columns raise a ValueError naming them), restoring pd.CategoricalDtype from categorical_specs for every categorical column (unseen categories become missing — LightGBM's documented native handling; log a warning when any unseen category is present), then predict_proba column 1; return columns p_win (float64), score_source (constant "ml"), artifact_version (read from the bundle's artifact_version field stamped by save_artifact; load_scorer falls back to deriving it from the resolved v{N} directory segment or LATEST.json when the field is absent). contributors(features) -> pd.DataFrame via the booster's pred_contrib prediction: shape (n_rows, n_features + 1) with columns feature_names plus "bias" — documented as raw-model contribution space paired with the calibrated p_win headline (A6). Keep the module MT5-free and importable without any runner dependencies (Phase 6's consumption surface).
    4. Write tests/unit/test_ml_artifact.py on a tiny fitted bundle from Task 2 helpers + _ml_fixtures: test_save_artifact_versions_increment_and_latest_pointer (two saves produce v1 then v2; LATEST.json points at v2 with matching config_hash); test_bundle_required_keys (every SC4 key present, including reliability_summary and the save-stamped artifact_version); test_manifest_json_is_pickle_free (manifest.json parses as JSON and contains feature_names/method/seeds/config_hash/reliability_summary but no model object); test_atomic_writes_leave_no_tmp (no .tmp siblings survive success); test_load_rejects_schema_version_mismatch (tampered bundle raises naming expected vs found); test_load_rejects_feature_list_mismatch; test_load_rejects_major_library_mismatch (monkeypatch the recorded version string to a different major → raises; same-major-different-patch → warns, loads).
    5. Write tests/unit/test_ml_scorer.py: test_load_scorer_via_latest_pointer_and_explicit_dir (both path forms resolve to equivalent Scorers); test_score_single_row_matches_training_frame (a one-row slice of the training frame scores identically to its row in the full-frame prediction — the category-restoration necessity from Pitfall 7); test_score_rejects_missing_feature_columns (ValueError naming the missing column); test_score_unseen_category_becomes_missing_with_warning (caplog captures the warning; the score is still produced); test_score_output_schema (exact columns p_win/score_source/artifact_version; score_source constant "ml"; artifact_version equals the version stamped by save_artifact and matches the v{N} directory it was loaded from); test_contributors_shape_and_bias_column (width n_features + 1; last column named bias; deterministic across calls).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_artifact.py tests/unit/test_ml_scorer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_artifact.py tests/unit/test_ml_scorer.py -q` all green, including the thirteen named tests (seven artifact + six scorer).
    - Versioned layout exists in tests' tmp dirs: data models root gains pooled/v1, pooled/v2 and LATEST.json after two saves; manifest.json is valid JSON without pickled content.
    - All three loader validations (schema version, feature list version, library majors) have named negative tests; the minor-mismatch path warns without refusing.
    - test_score_single_row_matches_training_frame fails if categorical restoration is removed (executor mutation-check once during development) — proving Pitfall 7's fix is load-bearing.
    - scorer.py has no MetaTrader5 import and no I/O beyond reading the artifact files; `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>SC4 complete: model + calibrator + feature metadata versioned under data/models/pooled/v{N} with manifest + LATEST pointer, atomic writes, three-layer loader validation, and a loadable scorer whose score and contributor surfaces Phase 5/6 consume without any backtest-only branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| label/feature frames → train.py | training inputs cross from storage into model fitting; single-class and starvation frames must be refused or skipped |
| model.joblib → load_artifact/scorer | pickle deserialization boundary — only this repo's own runs are trusted; validation gates the load |
| config.toml → train.py | ml knobs steer training semantics; typos must fail fast |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering/Elevation | ml/artifact.py load path | high | mitigate | Loader validates artifact_schema_version, feature_list_version, and installed sklearn/lightgbm majors before use; module contract documents own-runs-only trust boundary; every refusal names expected vs found |
| T-04-02 | Repudiation (evidence integrity) | ml/purge.py + ml/folds.py | critical | mitigate | OQ5 purge pinned at both boundary sides by named tests; folds derive exclusively from build_windows (SC3); AST test proves no default-cv calibration call site; embargo knob semantics pinned |
| T-04-05 | Repudiation | ml/train.py determinism | high | mitigate | deterministic=True + force_row_wise=True + num_threads=1 + fixed random_state pinned by retrain-identical test; seeds recorded in the bundle; no dart boosting |
| T-04-08 | Tampering | probability distortion via reweighting | medium | mitigate | imbalance flags structurally absent from lgbm_params (named test); omission rationale documented in the docstring |
| T-04-09 | Repudiation | starved/single-class windows | medium | mitigate | Pattern 5 preflight returns stable reason prefixes (skip+record upstream); log_loss-style metric guards handled in plan 03; never fabricates a model |
| T-04-SC | Tampering | (inherited) pinned lightgbm/scikit-learn | low | accept | Installed and version-verified in plan 01 Task 1 against the Package Legitimacy Audit; no new installs in this plan |
</threat_model>

<verification>
- `uv run pytest tests/unit -q` — full suite green
- `uv run pytest tests/unit/test_ml_purge.py tests/unit/test_ml_folds.py tests/unit/test_ml_train.py tests/unit/test_ml_artifact.py tests/unit/test_ml_scorer.py -q` — AI-02/AI-03/SC2/SC3/SC4 named tests green
- `uv run ruff check .` — clean
- Vendor purity: no MetaTrader5 import under src/ai_trading/ml/ (existing plan-01 static test continues to pass over the new modules)
- File-count mitigation (accepted, as in Phase 3): per-task commit discipline — one commit per task with the unit suite green before each commit — keeps every review/rollback scope to a single task
</verification>

<success_criteria>
- Purge + embargo semantics pinned at both boundaries; folds derive from build_windows only; no shuffled splits anywhere including inside calibration (SC3/AI-04)
- Deterministic decided-only training with sigmoid-default calibration and recorded reliability helper (AI-02/AI-03/SC2, D-01)
- Versioned, atomically-written, load-validated artifact bundle + MT5-free scorer with contributors (SC4)
- All five new test modules green within the full suite
</success_criteria>

## Artifacts this phase produces

**New files:** `src/ai_trading/ml/purge.py`, `src/ai_trading/ml/folds.py`, `src/ai_trading/ml/train.py`, `src/ai_trading/ml/artifact.py`, `src/ai_trading/ml/scorer.py`, plus `tests/unit/test_ml_purge.py`, `test_ml_folds.py`, `test_ml_train.py`, `test_ml_artifact.py`, `test_ml_scorer.py`

**Symbols in ml/purge.py:** `purged_train_mask(train_mask, exit_times, test_start, embargo_bars=0, bar_minutes=15) -> pd.Series`

**Symbols in ml/folds.py:** `calibration_folds(entry_times, exit_times, test_days, train_days, embargo_bars=0) -> list[tuple[list[int], list[int]]]` (positional sklearn cv pairs derived exclusively from `walkforward.build_windows`)

**Symbols in ml/train.py:** `decided_mask(outcomes)`, `preflight_reason(y_decided, min_labels, folds=None) -> str | None` (stable prefixes: insufficient_decided_labels / single_class / insufficient_calibration_folds), `lgbm_params(cfg) -> dict`, `fit_lightgbm(X, y, cfg)`, `fit_calibrated(X, y, folds, method, cfg)` (folds is required-positional, ensemble=True), `reliability_curve(y_true, p_pred, n_bins=5) -> pd.DataFrame`

**Symbols in ml/artifact.py:** `ARTIFACT_SCHEMA_VERSION = 1`, `build_bundle(...) -> dict`, `save_artifact(bundle, models_dir) -> Path` (writes `data/models/pooled/v{N}/model.joblib`, `manifest.json`, `data/models/LATEST.json`), `load_artifact(path) -> dict`

**Symbols in ml/scorer.py:** `load_scorer(models_root_or_path) -> Scorer`; `Scorer.score(features) -> pd.DataFrame` (columns p_win, score_source="ml", artifact_version); `Scorer.contributors(features) -> pd.DataFrame` (feature_names + bias via pred_contrib)

**On-disk layout created:** `data/models/pooled/v{N}/`, `data/models/LATEST.json` (eval reports and the audit artifact are written by plan 03's runner)

<output>
Create `.planning/phases/04-ml-scoring/04-02-SUMMARY.md` when done
</output>
