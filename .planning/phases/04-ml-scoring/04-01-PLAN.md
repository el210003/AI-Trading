---
phase: 04-ml-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - config.toml
  - src/ai_trading/config.py
  - src/ai_trading/ml/__init__.py
  - src/ai_trading/ml/features.py
  - src/ai_trading/ml/audit.py
  - tests/unit/_ml_fixtures.py
  - tests/unit/test_ml_features.py
  - tests/unit/test_ml_feature_audit.py
autonomous: true
requirements: [AI-01]
user_setup: []

must_haves:
  truths:
    - "Feature rows for every labeled trade are built only from decision-bar-visible state: appending future bars to the store never changes an already-built feature row (L1 prefix-equivalence, check_exact=True)"
    - "The R:R feature is recomputed at the decision-bar close, never read from the label frame's fill-based column (named test pins the distinction)"
    - "FEATURE_SPEC declares (name, dtype, source_tier, source_columns, stamp_kind) for every produced feature column and a test proves the spec covers exactly the produced columns"
    - "The three-layer audit runs green on the synthetic world and its artifact payload records max_abs_diff 0.0 with n_labels_checked > 0"
    - "lightgbm + scikit-learn are installed and importable (lightgbm 4.7.0, scikit-learn 1.9.0, joblib 1.6.x) and the frozen Config carries fail-fast-validated ml_* keys"
  artifacts:
    - path: "src/ai_trading/ml/features.py"
      provides: "FEATURE_SPEC, features_at_decision, build_feature_frame — pure, MT5-free, per D-03/D-15 point-in-time discipline"
    - path: "src/ai_trading/ml/audit.py"
      provides: "FORBIDDEN_LABEL_COLUMNS, run_feature_audit, write_feature_audit — the SC1 three-layer proof + atomic audit artifact"
    - path: "src/ai_trading/config.py"
      provides: "Config extended with ml_* knobs (feature list version, calibration method, embargo, gate, calibration window days, LightGBM params, retrain schedule) with fail-fast validation"
    - path: "tests/unit/_ml_fixtures.py"
      provides: "ml_cfg, make_labels, synthetic_feature_frame, sculpted_label_world — Phase 4 fixture helpers over _backtest_fixtures (never mutating conftest)"
    - path: "tests/unit/test_ml_features.py + tests/unit/test_ml_feature_audit.py"
      provides: "Named tests: decision-close R:R, as-of correctness per source tier, L1/L2/L3 audit layers, ml/ vendor purity"
  key_links:
    - from: "ml/features.py"
      to: "backtest/asof.py visible_mask + STAMP_CLOSE/STAMP_BAR"
      via: "every state slice in build_feature_frame goes through the single anti-lookahead choke point"
      why: "AI-01 point-in-time assembly inherits the Phase 2/3 prefix-stability guarantees"
    - from: "ml/features.py"
      to: "backtest/chain.py run_chain"
      via: "build_feature_frame consumes run_chain tier outputs (swings15/zones15/pools15/events15/payload) — detection never re-implemented"
      why: "BT-01 one shared code path; live scoring in Phase 6 uses the same builder"
    - from: "ml/audit.py"
      to: "data/reports/feature_audit.json"
      via: "atomic tmp+os.replace write of the audit payload on every training run"
      why: "SC1 'a feature audit confirms' becomes a checkable persisted artifact"
    - from: "ml/features.py FEATURE_SPEC"
      to: "Phase 4 plan 02 artifact metadata"
      via: "feature_names + categorical_specs + feature_list_version derive from FEATURE_SPEC"
      why: "SC4 feature metadata travels inside the versioned model artifact"
---

<objective>
Deliver the point-in-time feature builder from SMC state and the three-layer feature audit that CONFIRMS no post-decision-bar information is used (AI-01, ROADMAP SC1), plus the Wave-0 foundation every later Phase 4 plan consumes: lightgbm/scikit-learn install, frozen-Config ml_* extension, and the Phase 4 fixture module.

Purpose: features are the trust root of the ML tier — the project's core value (transparent, verifiable evidence) dies if features leak future information. This plan proves leak-freedom mechanically (L1 prefix-equivalence runtime proof, L2 provenance manifest, L3 forbidden-column static guard) instead of asserting it, per D-05's pipeline-first recommendation on synthetic data.

Output: src/ai_trading/ml/{__init__,features,audit}.py, extended config.py + config.toml, tests/unit/_ml_fixtures.py + two test modules, updated pyproject.toml/uv.lock with lightgbm 4.7.0 + scikit-learn 1.9.0.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ml-scoring/04-CONTEXT.md
@.planning/phases/04-ml-scoring/04-RESEARCH.md

# Phase 3 contracts the builder consumes
@src/ai_trading/backtest/asof.py
@src/ai_trading/backtest/candidates.py
@src/ai_trading/backtest/chain.py
@src/ai_trading/backtest/replay.py
@src/ai_trading/backtest/barriers.py
@src/ai_trading/config.py

# Pattern files
@tests/unit/test_replay_repaint.py
@tests/unit/_backtest_fixtures.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 foundation — ML dependencies, ml_* Config extension, package marker, fixtures</name>
  <files>pyproject.toml, uv.lock, config.toml, src/ai_trading/config.py, src/ai_trading/ml/__init__.py, tests/unit/_ml_fixtures.py</files>
  <read_first>
    - pyproject.toml (current dependency list + pytest markers)
    - src/ai_trading/config.py (frozen Config + _REQUIRED_KEYS + _validate pattern — extend, never fork)
    - config.toml (flat key layout + comment conventions for knob documentation)
    - src/ai_trading/backtest/__init__.py (bare package marker to mirror)
    - tests/unit/_backtest_fixtures.py (direct-import fixture convention this module extends)
    - tests/conftest.py (_make_cfg signature — ml fields get dataclass defaults so _make_cfg keeps working)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Standard Stack versions; Wave 0 Gaps list; Package Legitimacy Audit)
  </read_first>
  <action>
    1. Install the locked stack: run `uv add lightgbm scikit-learn` (joblib arrives transitively — never add it as a direct dependency). Pin nothing by hand beyond what uv resolves; research verified 4.7.0 / 1.9.0 / 1.6.0 resolve together on this environment. Package legitimacy: the RESEARCH.md Package Legitimacy Audit table covers both packages (Approved — locked stack; the project-init stack lock satisfies the human legitimacy checkpoint per the Phase 1 metatrader5 precedent), so this is a normal install task, not a blocking checkpoint.
    2. Extend src/ai_trading/config.py exactly per the established pattern: add to _REQUIRED_KEYS the thirteen ml keys (ml_feature_list_version, ml_calibration_method, ml_embargo_bars, ml_min_train_labels, ml_cal_train_days, ml_cal_test_days, ml_random_state, ml_n_estimators, ml_num_leaves, ml_min_data_in_leaf, ml_learning_rate, ml_retrain_enabled, ml_retrain_interval_hours); add matching frozen-Config fields WITH defaults so direct construction (tests/conftest.py _make_cfg) keeps working unchanged: ml_feature_list_version int 1, ml_calibration_method "sigmoid", ml_embargo_bars 0, ml_min_train_labels 30, ml_cal_train_days 2, ml_cal_test_days 1, ml_random_state 42, ml_n_estimators 200, ml_num_leaves 7, ml_min_data_in_leaf 5, ml_learning_rate 0.1, ml_retrain_enabled False, ml_retrain_interval_hours 24. Extend _validate fail-fast: feature_list_version positive int; calibration_method must be sigmoid or isotonic (reject anything else naming the allowed set); embargo_bars int >= 0; min_train_labels int >= 1; cal_train_days/cal_test_days positive ints; random_state int >= 0; n_estimators/num_leaves/min_data_in_leaf positive ints; learning_rate number > 0; retrain_enabled bool; retrain_interval_hours positive int. Use the module's existing _is_int/_is_number helpers and error-message style. Thread each key through load_config exactly like the Phase 3 keys. Document in the Config field comments that ml_retrain_enabled/ml_retrain_interval_hours are consumed by the Phase 6 scheduler (Phase 4 only validates them).
    3. Append an ML section to config.toml after the Phase 3 knobs, following the existing comment style: one comment block explaining each ml_* key (feature list version for artifact compatibility, calibration method sigmoid-default because isotonic overfits tiny calibration samples, embargo bars default 0 meaning purge-only, min decided labels gate, calibration window days for fold derivation inside the training window, deterministic LightGBM capacity params, retrain schedule keys for Phase 6) followed by the thirteen flat keys with the default values above.
    4. Create src/ai_trading/ml/__init__.py as a bare package marker (module docstring only, no imports — mirrors backtest/__init__.py).
    5. Create tests/unit/_ml_fixtures.py as the Phase 4 local helper following the _backtest_fixtures direct-import convention (imports from _backtest_fixtures and conftest, never modifies them): provide ml_cfg(**overrides) wrapping bt_cfg so tests get a frozen Config carrying the ml defaults without load_config or files on disk; make_labels(rows) building a label frame with exactly replay.LABEL_COLUMNS and replay's dtype pinning (string columns as pd.StringDtype, entry_time/exit_time as datetime64[us], exit_idx int64, remaining columns float64) from a list of row dicts; synthetic_feature_frame(n_rows, seed) producing a deterministic feature frame whose columns are exactly the FEATURE_SPEC names with categorical columns as pd.category dtype and numeric columns float64 (values seeded via numpy default_rng(seed), scaled to plausible ranges) — consumed by train/scorer tests in later plans; sculpted_label_world() returning (m15_bars, h1_bars, h4_bars, labels) by adapting the sculpt from tests/unit/test_replay_repaint.py: the same equal-low pool / zone-leg / H1-bullish-bias world, replayed with replay_symbol using an end-of-data barrier stub so at least one WIN and one LOSS label exist with known sl_price/tp_price. All helpers return new frames and never mutate inputs.
  </action>
  <verify>
    <automated>uv run python -c "import lightgbm, sklearn, joblib; print(lightgbm.__version__, sklearn.__version__, joblib.__version__)" && uv run pytest tests/unit -q</automated>
  </verify>
  <acceptance_criteria>
    - The import check prints 4.7.0, 1.9.0, and a 1.6.x joblib version in that order; lightgbm and scikit-learn appear in pyproject.toml dependencies; joblib does NOT appear as a direct dependency.
    - `uv run python -c "from ai_trading.config import Config; import dataclasses; names = {f.name for f in dataclasses.fields(Config)}; assert {'ml_feature_list_version','ml_calibration_method','ml_embargo_bars','ml_min_train_labels','ml_cal_train_days','ml_cal_test_days','ml_random_state','ml_n_estimators','ml_num_leaves','ml_min_data_in_leaf','ml_learning_rate','ml_retrain_enabled','ml_retrain_interval_hours'} <= names"` exits 0.
    - `uv run python -c "from ai_trading.config import load_config; import tomllib"` plus a pytest check in tests/unit/test_normalize_and_config.py or test_backtest_config.py covering at least: ml_calibration_method value outside the allowed set raises ValueError naming the allowed set; ml_embargo_bars = -1 raises; ml_min_train_labels = 0 raises; ml_feature_list_version = 0 raises; missing ml key in TOML raises naming the key. Add these to the existing config test module (extend, do not fork).
    - config.toml contains all thirteen ml keys; `uv run python -c "from pathlib import Path; from ai_trading.config import load_config; load_config(Path('config.toml'))"` exits 0.
    - tests/unit/_ml_fixtures.py exists; `uv run pytest tests/unit -q` is green with the full prior suite intact (research baseline: 358 passing).
    - `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>ML stack installed and version-verified; frozen Config validates thirteen ml_* keys fail-fast with defaults preserving direct construction; config.toml documents them; ml package marker + Phase 4 fixtures exist; full unit suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ml/features.py — FEATURE_SPEC, features_at_decision, build_feature_frame</name>
  <files>src/ai_trading/ml/features.py, tests/unit/test_ml_features.py</files>
  <read_first>
    - src/ai_trading/backtest/candidates.py (CandidateState fields, compute_rr signature, bias_agrees, the D-02 semantics)
    - src/ai_trading/backtest/asof.py (STAMP_CLOSE/STAMP_BAR anchor table, visible_mask contract, close_time_of)
    - src/ai_trading/backtest/replay.py (LABEL_COLUMNS — the single label schema; the exact visible_mask slicing per tier to mirror; _validate_bars single-symbol precedent)
    - src/ai_trading/backtest/chain.py (run_chain output dict keys)
    - src/ai_trading/detectors/mtf.py (PAYLOAD_COLUMNS 14-column lean payload consumed as-is per D-15)
    - src/ai_trading/detectors/atr.py (wilders_atr signature + NaN warmup semantics)
    - src/ai_trading/normalize.py (TIMEFRAME_MINUTES)
    - tests/unit/_ml_fixtures.py, tests/unit/test_replay_repaint.py (world sculpt conventions)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Pattern 1 feature builder; Pitfall 1 the fill-based R:R leak; A7 session encoding)
  </read_first>
  <action>
    1. Define FEATURE_SPEC as the ordered source of truth for the feature list version (cfg.ml_feature_list_version): an ordered structure whose entries carry name, dtype (categorical or float64), source_tier, source_columns, and stamp_kind (one of the two as-of anchors from backtest.asof, or the payload-as-is kind for D-14 payload fields). The spec must cover EXACTLY this 18-feature set. Categorical (5): symbol, timeframe, direction — decision-time identity fields read from the label row's structural columns; bias_h1, bias_h4 — consumed as-is from the CandidateState payload_row (missing when payload_row is None, per D-15). Numeric (13): rr_at_decision — compute_rr(direction, decision_close, sl_price, tp_price) using the label row's structural sl_price/tp_price (decision-time detector outputs per Phase 3 D-08/D-09/D-13) and the decision bar's close; sl_dist_atr — absolute distance from decision close to sl_price divided by the decision-bar atr14; tp_dist_atr — same toward tp_price; atr14 — wilders_atr over bars up to and including the decision bar (period 14, NaN warmup preserved); zone_position — (decision_close − zone range_low) / (zone range_high − zone range_low) for the tapped zone located by the label row's zone_id within the visible zones15 slice; bars_since_sweep — decision bar index minus the bar index of the sweep event located by the label row's event_id (resolved_at is a bar-time anchor); bars_since_zone_created — decision bar index minus the bar index whose time_utc is the latest bar at or before the tapped zone's created_at (created_at is a close-time stamp); htf_bias_agreement — int 0/1/2 per the D-02 bias_agrees semantics; htf_dist_to_eq_atr_h1 and htf_dist_to_eq_atr_h4 — payload-as-is; spread_points — the decision bar's recorded spread; utc_hour_sin and utc_hour_cos — cyclical encoding of the decision bar time_utc hour (A7: keyed on UTC, not broker-local).
    2. Implement features_at_decision(state, label_row, cfg) -> dict: pure function of the already-visibility-filtered CandidateState plus the label row's decision-time structural fields (direction, sl_price, tp_price, zone_id, event_id, symbol, timeframe). The decision close is the last row's close of state.m15_bars. Never read the label frame's post-decision columns — the fill-based R:R column, the filled entry price, the four exit/result columns, and the three R variants are forbidden inputs (Task 3's L3 layer enforces this mechanically; the module docstring explains the rule and why the structural sl/tp and the entry-time join key ARE decision-time legitimate: the fill lands at the next bar's open per D-04, so entry_time equals the decision bar's close time except across session gaps, and sl/tp are frozen detector outputs from pre-decision state). NA policy: payload-as-is fields propagate NA/None as missing; atr14 warmup NaN propagates; zone/event lookups that find no visible row yield missing (never raise) — LightGBM handles missing natively.
    3. Implement build_feature_frame(labels, chain, bars) -> pd.DataFrame: processes ONE symbol's labels (mirror replay._validate_bars: required columns, exactly one symbol, time_utc strictly increasing and unique; raise ValueError naming the violation otherwise). For each label row: locate the decision bar as the positional predecessor of the fill bar — searchsorted over the bars' time_utc for entry_time with side left, minus one — NOT a naive entry_time minus one timeframe (weekend/session gaps make the time-arithmetic form land on the wrong bar; pin this with a named test); raise a ValueError invariant naming the entry_time when no decision bar exists (searchsorted result 0). Derive bar_t, close_t, and build the CandidateState by slicing each tier through visible_mask with the exact per-tier anchors replay_symbol uses (events15/zones15 via the bar-time anchor, pools15/swings15 via the close-time anchor, payload row where time_utc equals bar_t consumed as-is), then call features_at_decision. Return a frame with the 18 feature columns plus the alignment columns symbol, timeframe, entry_time, and decision_close_time (close_time_of of the decision bar's time_utc — across session gaps this differs from entry_time; the audit's prefix filter consumes decision_close_time). Assert the output row count equals the input label row count and row order matches (invariant, raised on mismatch). Inputs never mutated; empty labels yield a schema-correct empty frame with pinned dtypes.
    4. Write tests/unit/test_ml_features.py with named tests: test_rr_feature_uses_decision_close_not_fill_open (the world's decision close differs from the fill open; assert rr_at_decision equals compute_rr at the decision close and differs from the label's fill-based value); test_decision_bar_located_across_session_gap (insert a multi-day gap between the decision bar and the fill bar; the builder still finds the positional predecessor); test_categorical_features_from_payload_as_is (bias fields missing when payload_row is None); test_atr_warmup_propagates_missing (a decision bar inside atr warmup yields missing atr14/sl_dist_atr/tp_dist_atr); test_zone_and_sweep_recency_features (hand-computed bar counts); test_feature_frame_schema_and_row_alignment (exact column set = 18 features + 4 alignment columns; row order equals label order); test_empty_labels_schema_correct_empty_frame; test_build_feature_frame_rejects_multi_symbol_labels. Use _ml_fixtures sculpted_label_world / make_labels.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_features.py -q` all green, including the eight named tests above.
    - test_rr_feature_uses_decision_close_not_fill_open fails if features_at_decision is switched to read the label's fill-based R:R column (mutation-check by the executor: temporarily sourcing the fill value must turn the test red — verified once during development).
    - FEATURE_SPEC covers exactly the 18 named features with dtype/source_tier/source_columns/stamp_kind populated on every entry.
    - features.py contains no MetaTrader5 import, no file I/O, and never mutates inputs (mirrors detector-tier purity; Task 3's static layer re-asserts).
    - `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>18-feature point-in-time builder exists as pure functions with the decision-close R:R rule pinned by test; session-gap-robust decision-bar location pinned by test; schema, alignment, NA policy, and empty-frame contracts all tested green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ml/audit.py — three-layer point-in-time audit + feature_audit.json artifact</name>
  <files>src/ai_trading/ml/audit.py, tests/unit/test_ml_feature_audit.py</files>
  <read_first>
    - src/ai_trading/ml/features.py (Task 2 output — the audit target)
    - tests/unit/test_replay_repaint.py (THE prefix-equivalence pattern L1 extends: 1-by-1 + chunked appends, check_exact=True, strict vs non-strict visibility horizons)
    - tests/unit/_detector_fixtures.py (assert_point_in_time_prefix_equality + prefix_close_time helpers)
    - src/ai_trading/backtest/asof.py (close_time_of for prefix close computation)
    - src/ai_trading/backtest/reports.py (_atomic_json pattern — tmp + os.replace, tmp unlinked on failure; copy the pattern, do not import the private helper)
    - src/ai_trading/ml/features.py FEATURE_SPEC (L2 manifest source)
    - .planning/phases/04-ml-scoring/04-RESEARCH.md (Pattern 2 three-layer audit spec; Security Domain lookahead-leakage threat row)
  </read_first>
  <action>
    1. Implement L1 prefix-equivalence as a reusable function audit_prefix_equivalence(labels, m15_bars, h1_bars, h4_bars, prefix_points) -> dict: for each prefix length k in prefix_points, run run_chain over the truncated bar prefixes and build_feature_frame over them; compare against the full-range run restricted to labels whose decision_close_time is at or before the prefix close time (non-strict — features need only bars up to and including the decision bar S, unlike the replay repaint suite which uses a strict horizon because the fill needs bar S+1; document this distinction in the docstring). Compare with pandas assert_frame_equal check_exact=True after sorting both sides by (symbol, entry_time). Return the audit payload fragment {max_abs_diff: 0.0 when equal, n_labels_checked: count of compared rows, prefix_points}. Reuse or adapt the _detector_fixtures helpers via direct import rather than duplicating their logic.
    2. Implement L2 spec coverage as audit_spec_coverage(feature_frame, feature_spec) -> dict: assert the set of feature columns produced equals the spec's names exactly (missing and extra both fail, naming the difference), every declared categorical dtype matches the produced column dtype, and every stamp_kind value is one of the three legal kinds. Return {feature_spec_hash: sha256 over the sorted spec table serialization, spec_table: the spec as records}.
    3. Implement L3 as a static source guard: define FORBIDDEN_LABEL_COLUMNS as the tuple of label-side post-decision columns taken from replay.LABEL_COLUMNS — the fill-based R:R column, the filled entry price, the four exit/result columns (exit_time, exit_price, exit_idx, outcome), and the three R variants (r_gross, r_raw, r_net). The test (not production code) parses features.py with the ast module, collects identifier names (Name and Attribute nodes) and string constants EXCLUDING module/class/function docstrings, and asserts the intersection with FORBIDDEN_LABEL_COLUMNS is empty, naming any offender. Docstrings are excluded because features.py legitimately documents the leak rule in prose; code identifiers and column-access strings are not. Also assert in the same test that features.py has no MetaTrader5 import and no open/to_parquet/to_csv/os.replace calls (vendor purity + purity-of-transform extension of the Phase 2 detector assertion).
    4. Implement run_feature_audit(labels, m15_bars, h1_bars, h4_bars, cfg) -> dict composing the three layers into the artifact payload {max_abs_diff, n_labels_checked, feature_spec_hash, spec_table, suite: "test_ml_feature_audit", embargo_note: "purge obligations live in plan 04-02"} — any layer failure raises ValueError naming the layer. Implement write_feature_audit(payload, reports_dir) -> Path writing data/reports/feature_audit.json through a local atomic tmp + os.replace helper following the reports.py pattern exactly (tmp sibling in the same directory, unlinked on failure).
    5. Write tests/unit/test_ml_feature_audit.py with named tests: test_l1_prefix_equivalence_single_and_chunked_appends (sculpted world; 1-by-1 and chunked prefix points; check_exact equality); test_l1_detects_runtime_leak_mutation (monkeypatch/monkeybuild a variant of the feature builder that reads one bar past the decision index — e.g., a local wrapper passing bars.iloc[:S+2] as m15_bars — and assert the audit raises naming layer L1, proving the layer can fail); test_l2_spec_covers_every_column (green path + a mutated spec missing one column raises naming it); test_l3_forbidden_columns_absent_from_source (green path + a tmp file containing a quoted forbidden column access raises naming it); test_l3_vendor_purity_no_mt5_import; test_run_feature_audit_payload_shape (max_abs_diff 0.0, n_labels_checked > 0, spec hash stable across two calls); test_write_feature_audit_atomic (file written, no .tmp sibling left, malformed payload raises before writing).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ml_feature_audit.py tests/unit/test_ml_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_ml_feature_audit.py tests/unit/test_ml_features.py -q` all green, including the seven named audit tests.
    - test_l1_detects_runtime_leak_mutation demonstrates L1 can fail (the audit is a real proof, not a tautology).
    - test_l3_forbidden_columns_absent_from_source passes against the Task 2 features.py as written (docstring prose does not trip the guard).
    - The audit payload dict carries exactly the keys max_abs_diff, n_labels_checked, feature_spec_hash, spec_table, suite; write_feature_audit returns the path data/reports/feature_audit.json under the given reports dir and leaves no .tmp file.
    - `uv run pytest tests/unit -q` full suite green; `uv run ruff check .` passes.
  </acceptance_criteria>
  <done>SC1's "audit confirms point-in-time assembly" is mechanical: L1 runtime prefix proof (proven falsifiable), L2 spec-coverage manifest, L3 static forbidden-column + vendor-purity guard, all green with a persisted-artifact writer following the atomic-write discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → Config | ml_* knobs cross from human-edited TOML into training semantics |
| label/store frames → feature builder | Phase 3 label frames are inputs; post-decision columns must not cross into features |
| bar frames → run_chain | stored Parquet bars feed detection; prefix stability is the guarantee being proven |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Repudiation (evidence integrity) | ml/features.py + ml/audit.py | critical | mitigate | Three-layer audit: L1 prefix-equivalence (falsifiable named test), L2 provenance manifest with spec hash, L3 AST-based forbidden-column + vendor-purity guard; audit payload persisted per training run (plan 03 runner) |
| T-04-04 | Tampering | config.py ml_* keys | high | mitigate | Frozen-Config fail-fast validation: typo'd/missing/out-of-domain ml keys refuse load naming the field and allowed set; bool-as-int rejected via existing _is_int discipline |
| T-04-SC | Tampering | uv add lightgbm scikit-learn | high | mitigate | RESEARCH.md Package Legitimacy Audit table present (both packages Approved via the project-init stack lock, official repos confirmed); versions verified post-install (4.7.0 / 1.9.0 / 1.6.x) before any downstream task; no [SLOP]/[SUS] residuals |
| T-04-07 | Information disclosure | _ml_fixtures sculpted world | low | accept | Test fixtures use synthetic prices only; no credentials or real account data exist in Phase 4 paths |
</threat_model>

<verification>
- `uv run pytest tests/unit -q` — full suite green (baseline 358 + new ML tests)
- `uv run pytest tests/unit/test_ml_features.py tests/unit/test_ml_feature_audit.py -q` — AI-01/SC1 named tests green
- `uv run ruff check .` — clean
- `uv run python -c "import lightgbm, sklearn, joblib"` — imports succeed (Wave 0 gap closed)
- Grep gate: no MetaTrader5 import under src/ai_trading/ml/ (asserted by test_l3_vendor_purity_no_mt5_import, not by a shell grep)
</verification>

<success_criteria>
- Features for every label are assembled through visible_mask anchors and run_chain exclusively (BT-01 preserved); decision-close R:R rule proven by named test
- The three-layer audit passes on the synthetic world with max_abs_diff 0.0 and is demonstrably falsifiable (L1 mutation test)
- ml_* config knobs load fail-fast; thirteen keys documented in config.toml
- lightgbm 4.7.0 + scikit-learn 1.9.0 + joblib 1.6.x installed and verified
- AI-01 satisfied at the unit level; SC1 evidence exists as a persisted-artifact writer ready for plan 03's runner
</success_criteria>

## Artifacts this phase produces

**New files:** `src/ai_trading/ml/__init__.py` (bare marker), `src/ai_trading/ml/features.py`, `src/ai_trading/ml/audit.py`, `tests/unit/_ml_fixtures.py`, `tests/unit/test_ml_features.py`, `tests/unit/test_ml_feature_audit.py`

**Symbols in ml/features.py:** `FEATURE_SPEC` (ordered spec table: name, dtype, source_tier, source_columns, stamp_kind per entry — 18 features); `features_at_decision(state: CandidateState, label_row, cfg) -> dict`; `build_feature_frame(labels, chain, bars) -> pd.DataFrame` (single-symbol contract; emits the 18 feature columns + alignment columns symbol, timeframe, entry_time, decision_close_time)

**Symbols in ml/audit.py:** `FORBIDDEN_LABEL_COLUMNS` (tuple), `audit_prefix_equivalence(...)`, `audit_spec_coverage(...)`, `run_feature_audit(labels, m15_bars, h1_bars, h4_bars, cfg) -> dict`, `write_feature_audit(payload, reports_dir) -> Path` (writes `data/reports/feature_audit.json`)

**Config fields added (frozen Config + config.toml):** `ml_feature_list_version`, `ml_calibration_method`, `ml_embargo_bars`, `ml_min_train_labels`, `ml_cal_train_days`, `ml_cal_test_days`, `ml_random_state`, `ml_n_estimators`, `ml_num_leaves`, `ml_min_data_in_leaf`, `ml_learning_rate`, `ml_retrain_enabled`, `ml_retrain_interval_hours`

**Dependency changes:** pyproject.toml + uv.lock gain `lightgbm` (4.7.0) and `scikit-learn` (1.9.0); joblib 1.6.x arrives transitively (never a direct dependency)

**Fixture helpers (tests/unit/_ml_fixtures.py):** `ml_cfg(**overrides)`, `make_labels(rows)`, `synthetic_feature_frame(n_rows, seed)`, `sculpted_label_world()`

<output>
Create `.planning/phases/04-ml-scoring/04-01-SUMMARY.md` when done
</output>
