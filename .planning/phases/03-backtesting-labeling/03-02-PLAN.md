---
phase: 03-backtesting-labeling
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/ai_trading/backtest/barriers.py
  - src/ai_trading/backtest/stats.py
  - src/ai_trading/backtest/reports.py
  - tests/unit/test_barriers.py
  - tests/unit/test_stats.py
  - tests/unit/test_reports.py
autonomous: true
requirements: [BT-03, BT-04]
must_haves:
  truths:
    - "The triple-barrier walk evaluates exactly bars [E, E+95] (96 bars inclusive of the entry bar), checks SL before TP on EVERY bar including the entry bar (D-10), and fills gap crossings at the bar's open (D-11, both directions)."
    - "Outcomes are exactly WIN (TP hit) / LOSS (SL hit) / TIMEOUT; TIMEOUT exits at the final window bar's close (D-17/D-18)."
    - "Every label carries entry_time, exit_time, outcome, R_raw and R_net so Phase 4 can purge boundary-overlapping labels (AFML ch.7 purge contract); R_net = R_raw minus slippage on both fills (raw = spread only, net = spread + 2x slippage)."
    - "Canonical stats (BT-04) compute win rate (TIMEOUT excluded from the denominator, share reported separately), profit factor (zero-loss -> inf guard, zero-trade -> nan guard), expectancy and avg R as the mean signed R over ALL trades including TIMEOUT, max drawdown from the per-trade cumulative R curve, and trade count — per (symbol, timeframe) and in BOTH raw and net variants with the cost delta visible (D-16)."
    - "Label/canonical artifacts are written atomically (tmp + os.replace, zstd Parquet), keyed by (symbol, entry_time), byte-identical on same-input re-runs, with run metadata (config hash, range) in a SEPARATE JSON manifest so label bytes never contain timestamps or run ids."
  artifacts:
    - src/ai_trading/backtest/barriers.py (walk_barriers, OUTCOME_WIN/OUTCOME_LOSS/OUTCOME_TIMEOUT)
    - src/ai_trading/backtest/stats.py (canonical_stats, stats_by_symbol_timeframe, profit_factor, max_drawdown)
    - src/ai_trading/backtest/reports.py (write_labels, write_canonical_stats, write_run_manifest, _atomic_parquet, _atomic_json, config_hash)
    - tests/unit/test_barriers.py, test_stats.py, test_reports.py
  key_links:
    - barriers.walk_barriers consumes the Position contract from replay.py (03-01): entry_open, entry_spread_points, entry_price, sl_price, tp_price, entry_idx — the resolver seam signature matches replay_symbol's resolver parameter.
    - stats functions consume barrier label records (labels DataFrame with LABEL_COLUMNS from 03-01); groupby (symbol, timeframe) only, never global.
    - reports writes reuse bar_store.merge_and_write's atomic tmp+os.replace discipline (bar_store.py L67-75); labels keyed (symbol, entry_time) for idempotent overwrite.
---

<objective>
Implement the triple-barrier outcome walk (BT-03: SL-first intrabar tie rule, gap-open fills, 96-bar inclusive time barrier, TIMEOUT), the canonical statistics functions (BT-04: win rate, profit factor, expectancy, max DD, avg R, trade count, raw + net), and the atomic label/canonical-stat artifact writers.

Purpose: converts replay positions into truthful outcomes and reports. The tie rule, gap handling, and cost accounting are the phase's integrity surface — every number a human will later trust comes from these pure functions.

Output: backtest/barriers.py, backtest/stats.py, backtest/reports.py, and their three test modules.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtesting-labeling/03-CONTEXT.md
@.planning/phases/03-backtesting-labeling/03-RESEARCH.md
@.planning/phases/03-backtesting-labeling/03-PATTERNS.md

# Depends on 03-01 (Position/LABEL_COLUMNS/replay_resolver contract). 03-01-SUMMARY
# only needed if the Position field names changed; otherwise skip.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triple-barrier walk (BT-03) — SL-first tie, gap=open, 96-bar window</name>
  <files>src/ai_trading/backtest/barriers.py, tests/unit/test_barriers.py</files>
  <read_first>
    src/ai_trading/detectors/zones.py (_zones_for_symbol L159-176 — the per-bar sequential walk + first-event stamping style this module mirrors)
    .planning/phases/03-backtesting-labeling/03-RESEARCH.md (Pattern 3 L277-296 with the tie-rule grounding; Code Example 1 L440-467 — the exact window/gap/SL-first shape)
    src/ai_trading/backtest/costs.py (exit_fill_price / entry_fill_price signatures from plan 03-01 — raw vs net via apply_slippage)
    src/ai_trading/backtest/replay.py (Position fields, LABEL_COLUMNS — the resolver return-dict keys must match)
  </read_first>
  <action>
    Create src/ai_trading/backtest/barriers.py — pure triple-barrier outcome walk (D-10/D-11/D-17/D-18):
    - Module constants OUTCOME_WIN = "WIN", OUTCOME_LOSS = "LOSS", OUTCOME_TIMEOUT = "TIMEOUT".
    - walk_barriers(position, bars, cfg) -> dict with keys: outcome, exit_price, exit_idx, exit_time, r_gross, r_raw, r_net. This signature IS the resolver contract declared by 03-01's replay_symbol (position: Position, bars: DataFrame of bar rows, cfg: Config; time_barrier_bars from cfg.time_barrier_bars, default 96).
    - Window semantics (D-17, off-by-one pinned): evaluate bars[E : E + time_barrier_bars] where E = position.entry_idx; the window is INCLUSIVE of the entry bar (96 bars = 24h in detector-timeframe bars). For idx in range(entry_idx, min(entry_idx + time_barrier_bars, len(bars))):
      1. Gap check on the bar's OPEN before any high/low check, entry bar included (D-11): long — if open <= sl_price -> LOSS, exit at open; if open >= tp_price -> WIN, exit at open. short — if open >= sl_price -> LOSS at open; if open <= tp_price -> WIN at open. (Strict comparison per the locked convention; gapped fills never ignore gaps.)
      2. SL-first on ALL bars (D-10): long — if low <= sl_price -> LOSS, exit at sl_price; elif high >= tp_price -> WIN, exit at tp_price. short — if high >= sl_price -> LOSS at sl_price; elif low <= tp_price -> WIN at tp_price. The intrabar path is unknowable from OHLC; SL-first is the locked conservative convention — document it INSIDE the module docstring and never add close-direction/open-proximity/TP-first branches.
    - TIMEOUT (D-17): after the window with neither barrier hit — outcome TIMEOUT, exit_idx = min(entry_idx + time_barrier_bars, len(bars)) - 1, exit_price = that bar's close, exit_time = that bar's time_utc. (End-of-data lens: if len(bars) < E + time_barrier_bars, the last available bar is the timeout bar.)
    - Exit-time/price: for WIN/LOSS exit_time = bars.iloc[exit_idx]["time_utc"]; exit_price = the structural level (tp/sl) or the open for gap fills.
    - R computation, three variants per label record (D-16 raw = spread only, net = spread + 2x slippage; both must be visible):
      - r_gross: exit at structural level vs the structural entry (position.entry_open): long (gross_exit - entry_open)/(entry_open - sl_price); short (entry_open - gross_exit)/(sl_price - entry_open). For gap fills use the open as the exit price for gross as well (the bar opened beyond the level — the fill IS the open).
      - r_raw: entry_raw = costs.entry_fill_price(direction, entry_open, entry_spread_points, cfg, symbol, apply_slippage=False); exit_raw = costs.exit_fill_price(direction, exit_price_or_open, exit_bar_spread, cfg, symbol, apply_slippage=False); r_raw = signed(exit_raw - entry_raw)/sign_units where the denominator is the raw risk |entry_raw - sl_raw| (long: (exit_raw - entry_raw)/(entry_raw - sl_raw); short mirrored).
      - r_net: entry_net = position.entry_price (already net-filled by replay); exit_net = costs.exit_fill_price(direction, exit_price, bar_spread, cfg, symbol, apply_slippage=True); r_net = signed(exit_net - entry_net)/|entry_net - sl_net| where sl_net = costs.exit_fill_price(direction, sl_price, sl_bar_spread, cfg, symbol, True) treated as the net exit-at-SL level (document: net R normalizes by the NET risk distance so raw/net R are directly comparable; a -1.0 net LOSS with cost means slightly worse than -1).
      - bar_spread for the exit bar = effective_spread_points(cfg, symbol, bars.iloc[exit_idx]["spread"]) (D-15 fallback applies here too).
      - Guards: sl_price == entry_open / zero risk distance -> raise ValueError("walk_barriers invariant violated: zero risk distance").
    - Validators: position must carry non-None entry_open/sl_price/tp_price/entry_idx; missing columns -> ValueError "walk_barriers invariant violated"; input frames never mutated; pure (zero I/O, zero MetaTrader5).

    Create tests/unit/test_barriers.py (@pytest.mark.unit, direct imports from _backtest_fixtures / _detector_fixtures, hand-derivable bars via make_bars + sculpt helpers):
    - test_tie_sl_first_including_entry_bar: a single bar where BOTH low <= sl and high >= tp (engineered window of 1-2 bars) -> outcome LOSS; also the same construction on a mid-window bar. This test's expected value is LOSS — it FAILS if TP-first or close-direction logic sneaks into the walk (D-10 pin).
    - test_sl_hit_on_entry_bar: SL touched on the entry bar -> LOSS at sl_price (window includes entry bar; pitfall 6 pin).
    - Gap tests (D-11, both directions): long with next open <= sl -> LOSS, exit_price == open (not sl); long open >= tp -> WIN at open; short mirrors (open >= sl -> LOSS at open; open <= tp -> WIN at open).
    - test_timeout_after_exactly_96_bars: engineered never-touch bars -> TIMEOUT, exit_idx == E + 95 (NOT E+96), exit_price == close of bar E+95 (off-by-one pin, pitfall 6). Also a 2-bar window with cfg.time_barrier_bars=2 variant.
    - test_timeout_near_end_of_data: len(bars) < E + time_barrier_bars -> TIMEOUT at the last available bar close.
    - R math literals: e.g. long entry_open 1.10000, sl 1.09900, tp 1.10200 (win 2:1) -> r_gross == 2.0; LOSS -> r_gross == -1.0; gap-fill WIN at open above tp -> r_gross computed from the open fill; raw vs net delta for a full round trip == exactly 2x slippage_pips (D-16); NET LOSS r_net < -1.0 when costs present.
    - Zero-risk invariant: sl == entry_open -> ValueError matching "invariant violated".
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_barriers.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest/barriers.py tests/unit/test_barriers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - walk_barriers(position, bars, cfg) returns exactly {outcome, exit_price, exit_idx, exit_time, r_gross, r_raw, r_net} and is lifecycle-consistent with replay.py's resolver contract (03-01).
    - test_tie_sl_first_including_entry_bar passes (LOSS on the both-touched bar) and there is NO TP-first/close-direction/other tie heuristic branch in the module source.
    - test_timeout_after_exactly_96_bars proves exactly 96 inclusive bars with the timeout at bar E+95 close.
    - Gap-open fills (both sides), entry-bar inclusion, end-of-data timeout, and the raw/net R delta of exactly 2x slippage are all asserted by passing tests.
    - Full unit suite green; ruff clean.
  </acceptance_criteria>
  <done>
    BT-03 is implemented and pinned: SL-first tie rule (entry bar included), gap fills at open, inclusive 96-bar window with TIMEOUT at final-bar close, and per-label R in gross/raw/net variants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Canonical stats (BT-04) — win rate, PF, expectancy, max DD, avg R, counts, raw + net</name>
  <files>src/ai_trading/backtest/stats.py, tests/unit/test_stats.py</files>
  <read_first>
    src/ai_trading/history_report.py (compute_gaps/classify_gap L144-202 — pure function with explicit edge guards, never-crash report style)
    .planning/phases/03-backtesting-labeling/03-RESEARCH.md (Pattern 6 L322-333 with the formula + guard policy; Code Example 3 L485-500; Assumptions A4/A5: win-rate denominator excludes TIMEOUT, expectancy/avg R include TIMEOUT)
  </read_first>
  <action>
    Create src/ai_trading/backtest/stats.py — pure pandas statistics with explicit edge guards (BT-04, D-16, A4/A5):
    - canonical_stats(labels, r_col) -> dict reading labels["outcome"] + labels[r_col] (r_col in {"r_raw", "r_net"}): returns keys trades, wins, losses, timeouts, win_rate, timeouts_share, profit_factor, expectancy, avg_r, max_dd, r_col.
      - trades = len(labels); wins = count outcome == "WIN"; losses = count outcome == "LOSS"; timeouts = count outcome == "TIMEOUT" (D-18 classes).
      - win_rate = wins / (wins + losses); if wins + losses == 0 -> float("nan") (TIMEOUT excluded from the DENOMINATOR per A4; TIMEOUT share reported separately as timeouts/trades).
      - profit_factor = sum(r > 0) / abs(sum(r < 0)); zero loss-sum with positive win-sum -> float("inf"); both zero -> float("nan") (guards pinned).
      - expectancy = float(r.mean()) over ALL trades (TIMEOUTs included, A5 — usually slightly negative ties to the final-close exit); avg_r = same value (both keys kept for report compatibility).
      - max_dd = float((eq / eq.cummax() - 1).min()) over eq = r.cumsum(); empty -> 0.0 (never crash on empty).
      - All on R units (not currency); NaN propagation never raises.
    - stats_by_symbol_timeframe(labels) -> pd.DataFrame: groupby ("symbol", "timeframe") -> canonical_stats per group for r_raw and r_net; output columns: symbol, timeframe, trades, wins, losses, timeouts, win_rate, profit_factor, expectancy, avg_r, max_dd, plus raw_/net_ prefixed variants (e.g. net_expectancy, raw_expectancy) and cost_delta_expectancy = net expectancy - raw expectancy (D-16: both variants with the cost delta visible). Empty labels -> schema-correct empty DataFrame (pinned dtypes like detector frames); sort by (symbol, timeframe).
    - Docstring pin: win-rate denominator convention (A4), TIMEOUT inclusion in expectancy (A5), R-units, per-symbol grouping ONLY (never global).

    Create tests/unit/test_stats.py (report-dict assertion style like test_history_report.py; hand-computed R series):
    - Hand series: wins 2 (+1.0, +0.5), losses 1 (-1.0), timeouts 1 (-0.1) -> win_rate = 2/3; timeouts_share = 1/4; expectancy = (1.0+0.5-1.0-0.1)/4 = 0.1; profit_factor = 1.5/1.0 = 1.5; max_dd from cumsum (0.1,-0.9,-0.5... exact values).
    - Edge cases: empty labels -> nan win rates, trades 0, max_dd 0.0, no raise; all-win -> profit_factor inf; all-timeout -> win_rate nan, expectancies included; single trade.
    - Zero-loss PF guard (inf) and zero-both guard (nan) parametrized.
    - win_rate denominator: TIMEOUT count does not change the denominator (2 wins / 2 losses / 3 timeouts -> win_rate 0.5).
    - stats_by_symbol_timeframe: two symbols + two timeframes -> 4 group rows, no cross-symbol leakage (per-symbol aggregation; Pitfall "global" guard); raw vs net delta row == 2x slippage per trade average.
    - Empty labels -> empty frame with the exact output columns.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_stats.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest/stats.py tests/unit/test_stats.py -q</automated>
  </verify>
  <acceptance_criteria>
    - canonical_stats(labels, "r_net" / "r_raw") returns the documented key set; every hand-computed assertion in test_stats.py holds exactly.
    - Guards: empty, all-win, all-loss, zero-loss PF, zero-both PF all behave per the pinned table (nan/inf/0.0, never raise).
    - stats_by_symbol_timeframe groups per (symbol, timeframe); both raw and net variants present with visible cost delta; empty -> schema-correct empty frame.
    - Full unit suite green; ruff clean.
  </acceptance_criteria>
  <done>
    BT-04 canonical statistics are implemented as pure functions with pinned edge-case policy (TIMEOUT excluded from win-rate denominator, included in expectancy; PF inf/nan guards; R-curve max DD), computed per (symbol, timeframe) in raw + net variants.
  </done>
</task>

<task type="auto">
  <name>Task 3: Label + canonical-stat artifact writers (atomic, deterministic, idempotent)</name>
  <files>src/ai_trading/backtest/reports.py, tests/unit/test_reports.py</files>
  <read_first>
    src/ai_trading/stores/bar_store.py (merge_and_write L36-75 — tmp + os.replace + cleanup discipline to copy verbatim)
    tests/unit/test_idempotent_store.py (test_no_tmp_file_remains_after_successful_merge L85 and idempotency conventions to mirror)
    src/ai_trading/backtest/replay.py (LABEL_COLUMNS — the label frame schema reports.py validates)
  </read_first>
  <action>
    Create src/ai_trading/backtest/reports.py — artifact writers following the bar_store atomic-write pattern of record (Pitfall 11):
    - _atomic_json(obj, path) and _atomic_parquet(df, path) helpers: mkdir parents, write to path.with_name(path.name + ".tmp") (JSON: indent=2 sort_keys=True; Parquet: engine="pyarrow", compression="zstd", index=False), os.replace(tmp, path), unlink tmp on exception (copy bar_store.py L67-75 shape exactly; never delete-then-write).
    - write_labels(labels, labels_dir, symbol, timeframe) -> Path: validates the frame has LABEL_COLUMNS (else ValueError "write_labels invariant violated"); dedup on ("symbol", "entry_time") keep="last"; sort by entry_time; write to labels_dir / f"{symbol}_{timeframe}.parquet" (keyed by symbol+entry_time -> idempotent overwrite on re-run).
    - write_canonical_stats(stats_by_sym_tf: pd.DataFrame, reports_dir) -> Path: canonical_stats.json via _atomic_json (records keyed by symbol/timeframe; includes raw_/net_ columns and cost_delta; NEVER a timestamp/run-id inside — determinism).
    - write_run_manifest(meta: dict, reports_dir) -> Path: run_manifest.json via _atomic_json carrying run metadata ONLY (run_id, config hash, range start/end, min_history_days, time_barrier_bars, wf_train_days, wf_test_days, created_at) — explicitly separate from label/canon files so label bytes stay identical across re-runs.
    - config_hash(cfg) -> str: sha256 over the sorted Config dataclass fields (repr-safe: paths as strings, tuples sorted) — used by the manifest and later by Phase 4 content-versioning.
    - Path-safety (ASVS V4): all writers receive explicit directories under a caller-supplied data root; no user-supplied filenames; document "paths resolve under the data/ root only".

    Create tests/unit/test_reports.py (mirror test_idempotent_store.py conventions; tmp_path):
    - Atomicity: after a successful write, no .tmp file remains next to the artifact (exact assertion style of test_no_tmp_file_remains_after_successful_merge); a failing write (e.g. unwritable dir or mocked to_parquet raising) leaves no .tmp residue and re-raises.
    - Determinism: write the same labels frame twice to two paths -> byte-identical parquet bytes (read both as bytes); canonical JSON identical strings (sort_keys).
    - Idempotent overwrite: writing a frame twice with the same (symbol, entry_time) rows -> no duplicates in the final parquet; revised row with the same key keeps the LAST value.
    - Schema: write_labels with a frame missing LABEL_COLUMNS column -> ValueError "invariant violated".
    - Manifest separation: regenerating run_manifest does not change label bytes; manifest contains config_hash and range keys; config_hash is stable across equal Config instances and differs when a knob changes.
    - Empty labels -> valid empty parquet round-trips with LABEL_COLUMNS preserved.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_reports.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest/reports.py tests/unit/test_reports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - write_labels/write_canonical_stats/write_run_manifest/config_hash exist with the stated signatures; every artifact write goes through the tmp + os.replace path (no delete-then-write anywhere in the module).
    - Byte determinism, idempotent (symbol, entry_time) overwrite, manifest/label separation, and no-.tmp-residue all pass their named tests.
    - Full unit suite green; ruff clean.
  </acceptance_criteria>
  <done>
    BT-03/BT-04 artifacts are persistable: labels Parquet (keyed symbol+entry_time), canonical stats JSON with raw+net and cost delta, and a separate run-manifest carrying config hash — all atomic, deterministic, and idempotent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| positions -> barrier walk -> labels | outcome assignment is the integrity center; a tie/heuristic bug flatters win rates |
| labels -> stats | aggregation mistakes (global vs per-symbol) silently skew every report |
| reports -> data/labels, data/reports | artifact corruption or non-determinism breaks Phase 4 caching and human trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | barrier walk tie/gap handling | critical | mitigate | locked SL-first only (D-10); named test test_tie_sl_first_including_entry_bar fails on TP-first/close-direction heuristics; gap=open pinned both directions (D-11); 96-bar inclusive window pinned by test_timeout_after_exactly_96_bars (off-by-one) |
| T-03-02 | Repudiation | stats formulas / R units | high | mitigate | pure pandas + explicit guard table (nan/inf/0.0); hand-computed literal assertions; raw vs net delta pinned to exactly 2x slippage (D-16); TIMEOUT denominator policy documented in-module (A4/A5) |
| T-03-03 | Tampering | artifact corruption / partial writes | high | mitigate | tmp + os.replace atomic writes (bar_store pattern); no .tmp residue test; determinism (byte-identical re-runs); manifest separation so label bytes never carry run ids/timestamps |
| T-03-04 | Tampering | cross-symbol stats contamination | medium | mitigate | stats_by_symbol_timeframe groupby ("symbol","timeframe") only; per-symbol test with 2 symbols asserting no leakage; no global aggregation anywhere in stats.py |
| T-03-05 | Elevation | report path injection | low | mitigate | writers take caller-supplied dirs under data/; no user-supplied filenames; validated config-driven paths only (ASVS V4 partial) |
| T-03-06 | Denial of Service (of the evidence) | empty/partial label sets reported as success | medium | mitigate | empty-input stats never raise and return schema-correct defaults; D-21 gate (03-01) prevents silent low-data runs from reaching reports |
</threat_model>

<verification>
- Per task commit: `uv run pytest tests/unit -q` (green; no regressions).
- Per wave merge: full `uv run pytest -q` + `uv run ruff check .`.
- Integrity gates: test_barriers.py (tie + gap + off-by-one), test_stats.py (guards + raw/net delta), test_reports.py (no .tmp residue + byte determinism) all green before this plan is done.
- Deferred item note: Phase 4 must purge train labels whose [entry_time, exit_time] interval overlaps a test window (AFML ch.7) — labels carry entry_time/exit_time for that purpose; documented here, owned by Phase 4.
</verification>

<success_criteria>
- Triple-barrier labels are reproducible from raw setup + bar data alone (pure functions over the Position + bars) — BT-03.
- Canonical stats deliver win rate, PF, expectancy, max DD, avg R, trade count per symbol/timeframe with raw + net variants and visible cost delta — BT-04.
- Label + canonical artifacts are atomic, deterministic, and re-run idempotent; manifest holds run metadata only.
</success_criteria>

## Artifacts this phase produces

**New modules (src/ai_trading/backtest/):**
- `barriers.py` — `OUTCOME_WIN`, `OUTCOME_LOSS`, `OUTCOME_TIMEOUT`, `walk_barriers(position, bars, cfg) -> dict{outcome, exit_price, exit_idx, exit_time, r_gross, r_raw, r_net}`
- `stats.py` — `canonical_stats(labels, r_col) -> dict` (keys: trades, wins, losses, timeouts, win_rate, timeouts_share, profit_factor, expectancy, avg_r, max_dd, r_col), `stats_by_symbol_timeframe(labels) -> pd.DataFrame` (raw_/net_ prefixed columns incl. cost_delta_expectancy)
- `reports.py` — `write_labels(labels, labels_dir, symbol, timeframe)`, `write_canonical_stats(stats_df, reports_dir)`, `write_run_manifest(meta, reports_dir)`, `config_hash(cfg)`, `_atomic_json(obj, path)`, `_atomic_parquet(df, path)`

**New test files:** `tests/unit/test_barriers.py`, `test_stats.py`, `test_reports.py`

**Named tests added (pin the locked contracts):** `test_tie_sl_first_including_entry_bar`, `test_timeout_after_exactly_96_bars`, `test_long_short_cost_symmetry` (03-01), `test_no_tmp_file_remains_after_successful_merge` (style copy)

**New artifact file layout (written at runtime, data/ gitignored):** `data/labels/{SYMBOL}_{M15}.parquet`, `data/labels/canonical_stats.json`, `data/labels/run_manifest.json` — walk-forward writer ships in plan 03-03.

<output>
Create `.planning/phases/03-backtesting-labeling/03-02-SUMMARY.md` when done
</output>
