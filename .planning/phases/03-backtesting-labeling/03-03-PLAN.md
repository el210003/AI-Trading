---
phase: 03-backtesting-labeling
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/ai_trading/backtest/walkforward.py
  - src/ai_trading/backtest/reports.py
  - src/ai_trading/backtest/runner.py
  - tests/unit/test_walkforward.py
  - tests/unit/test_reports.py
  - tests/unit/test_runner.py
autonomous: true
requirements: [BT-05]
user_setup:
  - service: "human-decision"
    why: "Real-data demo run (Phase gate, Open Question 1): M15 stored history is ~9 days (below D-21's 30-day gate). Decide at verification: deepen stored history via Phase 1 purge+backfill, or run the demo with --min-history-days override / H4-range. Engine is fully synthetic-tested either way."
must_haves:
  truths:
    - "Walk-forward windows are derived strictly chronologically: expanding train (every label with entry_time strictly before the test window start), fixed-length rolling test windows that slide forward by exactly the test length, zero overlap, no shuffles (D-19 / AI-04)."
    - "Window sizes are configurable in days (defaults 180 train / 30 test) AND work with small day-windows (e.g. 2/1) so the harness is usable on the ~90-day store today (D-20)."
    - "Every label is assigned to exactly one window by its entry bar time; per-window reports contain full canonical stats per (symbol, timeframe) AND an aggregate across symbols per window (D-22 / BT-05)."
    - "The runner CLI wires gate -> load (M15 + HTF warmup lead-in) -> chain -> replay -> barriers -> stats -> walk-forward -> reports; exit codes 2 (config error) / 1 (runtime refusal incl. D-21 gate and mixed-offset detection) / 0 (success — INCLUDING zero-candidate runs, which write schema-correct empty artifacts and never escape the exit-code contract); MT5-free, no mt5-marked tests."
    - "Walk-forward artifacts are deterministic and atomic: walkforward.parquet is byte-identical across same-input re-runs, and window boundaries/config hash live in a separate JSON manifest (no timestamps baked into data files)."
  artifacts:
    - src/ai_trading/backtest/walkforward.py (Window, build_windows, label_window_assignment, window_stats_table, window_aggregate)
    - src/ai_trading/backtest/runner.py (main, run_backtest, resolve_range)
    - src/ai_trading/backtest/reports.py (extended: write_walkforward, write_window_manifest)
    - tests/unit/test_walkforward.py, test_runner.py + test_reports.py extension
  key_links:
    - build_windows consumes label entry_time/exit_time stamps emitted by 03-02 barriers; window boundaries split on entry TIME (not exit time) — the Phase-4 purge contract depends on this.
    - runner wires walk_barriers (03-02) as replay_symbol's resolver — the interface-first seam from 03-01 closes here.
    - runner gate reuses check_history_gate/assert_offset_uniform (03-01) and reads bars via bar_store.read_bars; HTF lead-in loaded [start - htf_warmup_days, end] (A8).
    - Artifact layout aligned with 03-02 (INFO-side consistency): label-derived artifacts (labels parquet, canonical_stats.json, run_manifest.json) under data/labels/; window/report artifacts (walkforward.parquet, walkforward_manifest.json) under data/reports/ — no canonical/manifest file is ever written to data/reports/.
---

<objective>
Build the walk-forward harness (BT-05: expanding-train/rolling-test windows, per-window canonical stats, cross-symbol aggregate) and the runner CLI that runs the complete MT5-free backtest pipeline end-to-end with the D-21 history gate and offset-uniformity guard.

Purpose: closes the phase. Delivers regime-aware per-window reporting (BT-05) and the executable artifact the human runs; Phase 4 reuses the exact same window harness (AI-04, D-19).

Output: backtest/walkforward.py, backtest/runner.py, reports.py walk-forward writer extension, test_walkforward.py, test_runner.py, test_reports.py extension.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtesting-labeling/03-CONTEXT.md
@.planning/phases/03-backtesting-labeling/03-RESEARCH.md
@.planning/phases/03-backtesting-labeling/03-PATTERNS.md
@.planning/phases/03-backtesting-labeling/03-VALIDATION.md

# Depends on 03-02 (label records/LABEL_COLUMNS, walk_barriers, reports writers).
# 03-02-SUMMARY only needed if the label field names changed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk-forward window derivation + per-window stats (BT-05, D-19/D-20)</name>
  <files>src/ai_trading/backtest/walkforward.py, tests/unit/test_walkforward.py</files>
  <read_first>
    src/ai_trading/history_report.py (compute_gaps L144-178 — timestamp-grid/iterator window-derivation style to mirror)
    .planning/phases/03-backtesting-labeling/03-RESEARCH.md (Pattern 7 L334-350 with the sklearn TimeSeriesSplit semantics and small-window requirement; Code Example 4 L502-515; Pitfall 7 leakage/overlap rules)
    src/ai_trading/backtest/stats.py (canonical_stats + the raw/net column contract from plan 03-02 — per-window stats reuse these)
  </read_first>
  <action>
    Create src/ai_trading/backtest/walkforward.py — pure window derivation (D-19/D-20/D-22, Pattern 7):
    - Window frozen dataclass: test_start, test_end, window_id (int), train_mask (pd.Series bool over the label index), test_mask (pd.Series bool). Train labels = entry_time < test_start (STRICTLY before, expanding — D-19); test labels = test_start <= entry_time < test_end.
    - build_windows(entry_times: pd.Series, test_days: int, train_days: int, start: Timestamp | None = None, end: Timestamp | None = None) -> list[Window]:
      - Domain: start/end default to min/max of entry_times; boundaries b_0 = floor(start to day) (or start as given), b_{k+1} = b_k + test_days days; while b_k < end.
      - Chronological + zero overlap (D-19): next test_start == previous test_end (no gaps, no overlaps — pitfall 7).
      - train_days is the documented/validated knob retained in the Window (metadata) — the harness exposes it for Phase 4 even though Phase 3 computes train masks from expanding accumulation; docstring states D-19's expanding-train semantics and that the split is the SAME harness Phase 4 uses (AI-04, no shuffled splits anywhere).
      - Small-window support (D-20): test_days as small as 1 and train_days as small as 1/2 must produce valid windows (no zero-length); e.g. 10 labels over 5 days with test_days=1 -> 5 windows.
      - If test_days <= 0 or train_days <= 0 -> ValueError (window sizes positive ints).
    - ZERO-LABEL CONTRACT (supported state, never an error): build_windows on an empty entry_times series returns [] (no windows, no raise); label_window_assignment on an empty labels frame returns a schema-correct empty DataFrame with the window_id column added (same columns as the input + window_id); window_stats_table and window_aggregate on empty labels return schema-correct empty frames (pinned dtypes, no raise) — a legitimate no-candidate range must never crash the harness or escape the runner exit-code contract.
    - label_window_assignment(labels, windows) -> pd.DataFrame: adds window_id column, asserting EVERY label falls in EXACTLY ONE window (raise ValueError naming a label entry_time if zero or 2+ windows match — pitfall 7 pin; labels outside the domain are an error, never dropped silently). Insertion: window_id = max window with test_start <= entry_time < test_end; use searchsorted for O(n log n).
    - window_stats_table(labels_w, r_col="r_net") -> pd.DataFrame: per (window_id, symbol, timeframe) canonical_stats (reuse stats.canonical_stats per group), columns: window_id, window_start, window_end, symbol, timeframe, + all canonical metrics; empty -> schema-correct empty frame.
    - window_aggregate(labels_w, r_col="r_net") -> pd.DataFrame: per window_id ACROSS symbols (D-22 aggregate; groupby window_id only), columns window_id, window_start, window_end, + canonical metrics (trades/wins/losses/timeouts/win_rate/profit_factor/expectancy/avg_r/max_dd).
    - Both stats helpers compute raw AND net variants (call canonical_stats with r_raw and r_net; prefix columns raw_/net_ with cost_delta_expectancy) — consistency with 03-02 D-16.
    - Docstrings pin: labels are split by ENTRY bar time (not exit time — Phase 4 must purge [entry_time, exit_time] overlaps separately; AFML ch.7 obligation documented); strictly chronological; no shuffles; per-symbol grouping everywhere.

    Create tests/unit/test_walkforward.py (@pytest.mark.unit; literal timestamp sets):
    - Chronological + zero overlap (Pitfall 7): consecutive windows satisfy next.test_start == prev.test_end; intersection of test masks empty; windows sorted ascending.
    - Expanding train strictly before test: every train label entry_time < test_start; a label AT test_start is in the test mask, not the train mask (boundary pin).
    - No shuffle: input order preserved; masks are boolean views over the same index.
    - Small windows (D-20): 10 labels/day over 5 days with test_days=1/train_days=2 -> 5 windows, each test mask has exactly 2 labels; test_days=2 variant -> 2-3 windows.
    - Exactly-one-window assignment: every label has a non-empty window_id; labels spanning domain edges assign correctly; duplicate entry times (two labels same bar) both assign.
    - Outside-domain label -> ValueError naming the entry_time.
    - window_stats_table: per (window_id, symbol, timeframe) canonical numbers match stats.canonical_stats applied to the subset; raw+net columns present with cost delta; empty labels -> correct empty frame.
    - window_aggregate: per-window totals equal the sum of per-(symbol, TF) rows for the same window (counts), and win_rate computed on the pooled R series; 2-symbol fixture proving cross-symbol aggregate.
    - Invalid window sizes (0 or negative) -> ValueError.
    - Zero-label path: build_windows(pd.Series(dtype="datetime64[us]")) == []; label_window_assignment(empty_label_frame, []) -> empty frame with input schema + window_id column, no raise; window_stats_table/window_aggregate on empty labels -> schema-correct empty frames (no raise).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_walkforward.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest/walkforward.py tests/unit/test_walkforward.py -q</automated>
  </verify>
  <acceptance_criteria>
    - build_windows returns validated, chronologically-ordered, non-overlapping windows; test_days/train_days at 1-2 days produce valid windows; EMPTY entry_times -> [] (no raise).
    - label_window_assignment gives every label exactly one window_id and raises on outside-domain/fuzzy matches; empty labels -> schema-correct empty frame.
    - window_stats_table and window_aggregate match hand-recomputed canonical stats per (window_id, symbol, timeframe) and pooled per window; both raw_/net_ variants present; empty labels -> schema-correct empty frames (zero-candidate ranges are supported, not errors).
    - Full unit suite green; ruff clean.
  </acceptance_criteria>
  <done>
    BT-05 windowing exists: expanding train / rolling fixed-length test, day-configurable (supports the current ~90-day store), per-window canonical stats per (symbol, TF) plus cross-symbol aggregate, exactly-one-window assignment, all pure and reusable by Phase 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk-forward artifact writer (deterministic, atomic) + reports extension</name>
  <files>src/ai_trading/backtest/reports.py, tests/unit/test_reports.py</files>
  <read_first>
    src/ai_trading/backtest/reports.py (the _atomic_parquet/_atomic_json + manifest discipline created in plan 03-02 — extend, do not rewrite)
    src/ai_trading/backtest/walkforward.py (Window fields, window_stats_table/window_aggregate output schemas from Task 1)
    src/ai_trading/stores/bar_store.py (L67-75 atomic write to keep the copy verbatim)
  </read_first>
  <action>
    Extend src/ai_trading/backtest/reports.py (same module; no new module):
    - write_walkforward(window_stats: pd.DataFrame, reports_dir) -> Path: atomic Parquet to reports_dir / "walkforward.parquet" (zstd, index=False, tmp+os.replace via the existing _atomic_parquet). The FILENAME is deterministic — no timestamp/run-id in the filename (Pitfall 11); sorting by (window_id, symbol, timeframe) before write so re-runs are byte-identical.
    - write_window_manifest(windows_meta: dict, reports_dir) -> Path: JSON atomic to reports_dir / "walkforward_manifest.json" carrying window boundaries list (window_id, test_start, test_end, train_days, test_days), config hash, range, min_history_days and created_at — run metadata ONLY, never merged into data files.
    - Ensure both writers take caller-supplied dirs (path-safety under data/ root, ASVS V4) and reuse _atomic_parquet/_atomic_json so no second write path exists.
    - Docstring addition: per-window artifacts must stay byte-identical across same-input re-runs for Phase 4 content-versioning.

    Extend tests/unit/test_reports.py (same module; append cases, keep 03-02 tests intact):
    - After write_walkforward: file exists at data/reports/walkforward.parquet; no .tmp residue.
    - Determinism: same window_stats written twice -> byte-identical parquet; sort order ignored (writer sorts internally).
    - write_window_manifest: JSON parses; contains all window boundaries + config_hash; regenerating it does not change walkforward.parquet bytes (manifest/labels separation).
    - Window stats frame missing required columns -> ValueError "invariant violated".
    - Empty window_stats -> valid empty parquet round-trip with the pinned schema.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_reports.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest/reports.py tests/unit/test_reports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - reports.py exposes write_walkforward + write_window_manifest; both use the existing atomic helpers only.
    - walkforward.parquet is byte-deterministic, no .tmp residue, manifest carries windows + config hash and does not perturb data bytes.
    - Full unit suite green; ruff clean.
  </acceptance_criteria>
  <done>
    Per-window artifacts persist deterministically under data/reports/ with a separate manifest, ready for the dashboard (Phase 6) and Phase 4 re-use.
  </done>
</task>

<task type="auto">
  <name>Task 3: Runner CLI — full pipeline wiring, history gate, offset uniformity, exit codes</name>
  <files>src/ai_trading/backtest/runner.py, tests/unit/test_runner.py</files>
  <read_first>
    src/ai_trading/history_report.py (main L406-467 — argparse/--config/logging/exit-code CLI pattern of record)
    src/ai_trading/backtest/replay.py (check_history_gate, assert_offset_uniform, replay_symbol + the resolver param from plan 03-01)
    src/ai_trading/backtest/barriers.py (walk_barriers — passed as the resolver)
    src/ai_trading/backtest/walkforward.py + stats.py (window_stats_table/window_aggregate/stats_by_symbol_timeframe)
    src/ai_trading/backtest/reports.py (all writers + config_hash)
    src/ai_trading/stores/bar_store.py (bar_path, read_bars)
    src/ai_trading/config.py (load_config — exit 2 on ValueError)
    src/ai_trading/backtest/chain.py (run_chain) + src/ai_trading/normalize.py (TIMEFRAME_MINUTES)
  </read_first>
  <action>
    Create src/ai_trading/backtest/runner.py — the executable backtest CLI (history_report.py pattern):
    - resolve_range(range_arg: str | None, bars: pd.DataFrame) -> tuple[Timestamp, Timestamp]: supports "YYYY-MM-DD:YYYY-MM-DD" (inclusive start day to end day, clamped to the stored frame bounds) and "last-ND" (last N DAYS of stored bars); None -> (min time_utc, max time_utc); invalid format -> ValueError with the accepted formats listed.
    - run_backtest(cfg, symbols, range_arg, write) -> dict: per symbol (M15 execution only, D-06):
      1. m15 = read_bars(bar_path(cfg.bars_dir, symbol, "M15")); stored_days = (max(time_utc) - min(time_utc)) days; check_history_gate(stored_days, cfg.min_history_days, symbol, "M15") (D-21 — RuntimeError propagates to exit 1 with the actionable message; NEVER an empty 0-trade report).
      2. assert_offset_uniform(m15) (Pitfall 10); same check for H1/H4 frames.
      3. Load HTF lead-in (A8): h1 = read_bars(... "H1"), h4 = read_bars(... "H4"), sliced to [start - cfg.htf_warmup_days days, end] so confirmed legs exist before the first decision bar.
      4. chain = run_chain(m15_range, h1_range, h4_range) (BT-01 — the SAME detector exports).
      5. labels = replay_symbol(m15_slice, chain, cfg, walk_barriers) (resolver seam from 03-01 closes here: walk_barriers implements the (Position, bars, Config) -> dict contract).
      6. If write: write_labels(labels, cfg labels dir, symbol, "M15").
      7. Per-symbol canonical: stats_by_symbol_timeframe(labels) (raw + net, D-16).
      8. Walk-forward: windows = build_windows(labels["entry_time"], cfg.wf_test_days, cfg.wf_train_days); labels_w = label_window_assignment(labels, windows); window_stats_table + window_aggregate; aggregate across symbols per window (D-22). ZERO-CANDIDATE PATH: an empty labels frame is SUPPORTED — build_windows returns [], assignment/stats return schema-correct empty frames, and the loop CONTINUES (per-symbol canonical + empty walk-forward artifacts written normally; no raise, no special exit code).
      Returns a dict {labels: dict[symbol->df], canonical: df, windows, window_stats, window_aggregate}.
    - main(argv=None) -> int: argparse prog="ai_trading.backtest"; --config (default config.toml), --symbols (comma-separated, default cfg.symbols; validated subset of cfg.symbols), --range (format above, default None = full stored range), --min-history-days (int override of cfg.min_history_days; validated > 0 at parse), --write (store_true; if absent, artifacts are NOT written — dry-run prints summary only). logging.basicConfig(level=INFO, format="%(asctime)s %(levelname)s %(name)s: %(message)s").
      - Override mechanism (frozen Config — no mutation allowed): after load_config succeeds, if args.min_history_days is not None, rebuild the config via `cfg = dataclasses.replace(cfg, min_history_days=args.min_history_days)` and pass the REPLACED cfg into run_backtest → check_history_gate reads the override with zero signature threading. Do NOT setattr on the frozen dataclass and do NOT add a separate parameter to run_backtest/check_history_gate — replace() keeps the Config contract intact (fail-fast validation already ran; the override is validated > 0 at parse time before replace).
      - load_config ValueError -> log.error("invalid configuration: %s", exc) -> return 2.
      - RuntimeError (gate, mixed offset, range validation, replay/label errors) -> log.error -> return 1.
      - Success -> if --write: write label-derived artifacts to data/labels/ (write_labels per symbol, write_canonical_stats, write_run_manifest with config_hash, range, window sizes — SAME layout as 03-02) and the per-window artifacts to data/reports/ (write_walkforward, write_window_manifest); print a summary block (trades, win rate, PF, expectancy raw/net, per-window count); return 0. ZERO-CANDIDATE RUNS ALSO return 0: log INFO "0 candidates for {symbol} in {range}: schema-correct empty artifacts written" — an empty label set is NOT a refusal (the D-21 gate already ran; a gate-passing run with no signals is a valid outcome), never raise, never exit outside {0, 1, 2}.
      - Guard: report/label output roots must resolve under cfg.bars_dir.parent (data/ root); ValueError otherwise (ASVS V4).
    - Docstring: MT5-free (no MetaTrader5 import anywhere); exit-code contract 2/1/0; D-21 gate refusal message must include the remedy (extend collection or --min-history-days).
    - Note: config.toml needs labels/reports dirs — the runner derives them from cfg.bars_dir.parent / "labels" and "reports" (no new config keys; data/ layout), documented in the module.

    Create tests/unit/test_runner.py (pytest.mark.unit; tmp_path + synthetic parquet written via bar_store.merge_and_write / _backtest_fixtures.write_bars_parquet; call main(argv) directly, NOT subprocess, unless exit-code capture demands it):
    - Config error: main(["--config", missing_or_invalid]) -> returns 2; invalid config message logged (no crash).
    - Gate refusal (D-21): tmp store with 5 days of M15 (make_bars), cfg.min_history_days=30 -> return 1 AND stderr/caplog message contains symbol, "M15", stored days, min days, and the remedy text (extend / --min-history-days); NO labels file written.
    - Gate override: same store + main([..., "--min-history-days", "3"]) -> return 0, artifacts exist.
    - Full happy path: write synthetic M15/H1/H4 parquet for one symbol (enough bars: > warmup + a sculpted sweep+mitigation so >= 1 label), run with --write -> return 0; data/labels/{SYMBOL}_M15.parquet exists with LABEL_COLUMNS; data/labels/canonical_stats.json + data/labels/run_manifest.json + data/reports/walkforward.parquet + data/reports/walkforward_manifest.json exist; manifests carry config_hash; per-window artifacts under data/reports/ only (label-derived under data/labels/ — INFO-side alignment).
    - Mixed-offset bars (write a frame with time - time_utc = 2h for a few rows) -> return 1 and the message names the symbol (Pitfall 10).
    - Invalid --range string -> return 1 (runtime validation) with accepted formats in the message.
    - --symbols with a symbol not in cfg.symbols -> return 2 (config).
    - Resolver wiring: assert run_backtest actually produces labels via walk_barriers (the produced label outcomes are one of WIN/LOSS/TIMEOUT).
    - Zero-candidates range: synthetic M15/H1/H4 with NO sweep+mitigation sculpted (flat bars), gate passes (>= min_history_days) -> main([... , "--write"]) returns 0 (NOT 1/2/exception); data/labels/{SYMBOL}_M15.parquet exists and round-trips as a schema-correct EMPTY frame (LABEL_COLUMNS, pinned dtypes); canonical_stats.json + run_manifest.json + walkforward.parquet + walkforward_manifest.json all exist and are schema-correct; caplog shows the symbol + "0 candidates" INFO message.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_runner.py tests/unit/test_walkforward.py -q</automated>
    <automated>uv run pytest tests/unit -q</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run ruff check src/ai_trading/backtest tests/unit/test_runner.py -q</automated>
  </verify>
  <acceptance_criteria>
    - runner.py exposes main(argv)->int and run_backtest(...); exit codes verified: 2 (config), 1 (gate/offset/range), 0 (success).
    - D-21 refusal message contains symbol + timeframe + both day counts + remedy; no artifact is written on refusal.
    - Happy-path end-to-end on synthetic parquet produces labels, canonical stats (raw+net), walk-forward parquet, and both manifests with config_hash; all via the atomic writers.
    - Zero-candidate ranges exit 0 with schema-correct empty artifacts (test_runner zero-candidates test); build_windows/label_window_assignment/stats/aggregate never raise on empty label sets (Task 1 test).
    - Full unit suite green (existing + new), ruff clean, no MetaTrader5 import in backtest/.
  </acceptance_criteria>
  <done>
    BT-05 + phase deliverables are executable: `python -m ai_trading.backtest --config config.toml --range last-30d --write` runs gate -> chain -> replay -> barriers -> stats -> walk-forward -> artifacts, MT5-free, with actionable refusals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored bars -> runner -> labels | past data only; gate and offset guards are the integrity sentinels |
| config/CLI args -> runner | untrusted-ish inputs (range strings, symbol lists, paths) must fail fast, never traverse |
| reports -> data/ | artifacts consumed by Phase 4 caching and the Phase 6 dashboard; determinism is trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | walk-forward leakage/overlap/shuffle | critical | mitigate | build_windows is the ONLY splitter (chronological, zero overlap, expanding train strictly before test); exactly-one-window assertion raises on ambiguity; test suite pins boundary/overlap/shuffle cases (Pitfall 7); same harness reused by Phase 4 (AI-04) |
| T-03-02 | Denial of Service (of the evidence) | history gate / mixed-offset ranges produce misleading reports | high | mitigate | check_history_gate refuses < min_history_days with actionable message; assert_offset_uniform refuses mixed offsets (Pitfall 3/10); runner never writes artifacts on refusal paths |
| T-03-03 | Tampering | non-deterministic / partial walk-forward artifacts | high | mitigate | deterministic filenames + stable sort; tmp + os.replace atomic writes; byte-identical re-run test; manifest separation (Pitfall 11) |
| T-03-04 | Elevation | CLI/config path traversal for report output | medium | mitigate | report roots resolved under the data/ root (cfg.bars_dir.parent); writer signatures take caller-supplied dirs only; --symbols validated against cfg.symbols; no user-supplied filenames (ASVS V4) |
| T-03-05 | Tampering | cross-symbol contamination in per-window stats | medium | mitigate | window_stats_table groups (window_id, symbol, timeframe); window_aggregate pools per window explicitly; per-symbol grouping everywhere (never a global aggregation in stats.py) |
| T-03-06 | Impact of log/error handling | sensitive path or config dumps in logs | low | mitigate | mirror history_report: log.error with message only, never repr(cfg); credentials never flow here (MT5-free) |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages; RESEARCH Package Legitimacy Audit shows no installs (vectorbt/quantstats rejected by design; backtesting.py/mlfinlab SLOP removed) |
</threat_model>

<verification>
- Per task commit: `uv run pytest tests/unit -q` (no regressions).
- Per wave merge: full `uv run pytest -q` + `uv run ruff check .` (all unit; mt5 marker never applies to backtest/). Note: the full-suite gate (~60s) is the documented wave-merge gate — per-task verifies remain under 30s.
- Anti-leakage gates: test_walkforward.py (overlap/chronological/exactly-one-window), test_runner.py (gate refusal + override), test_reply_repaint.py (03-01) all green.
- Phase-gate demo (human): one real-data backtest run with the human deciding history depth vs --min-history-days override (Open Question 1 — recorded in user_setup); DST live re-validation of broker offset stays a human-assisted, mt5-marked, out-of-unit-scope carried obligation (02-VERIFICATION deferred item).
- Deferred to Phase 4: purge train labels whose [entry_time, exit_time] overlaps a test window (AFML ch.7) — labels carry the stamps; harness exposes windows + assignments.
</verification>

<success_criteria>
- Per-window reports expose regime shifts: canonical stats per (symbol, timeframe) per window AND an aggregate across symbols (BT-05, D-22).
- The runner executes the identical detector pipeline over history with actionable refusals below the D-21 gate and on mixed offsets.
- All artifacts (labels, canonical, walk-forward, manifests) are deterministic and atomic; full suite green; ruff clean.
</success_criteria>

## Artifacts this phase produces

**New modules (src/ai_trading/backtest/):**
- `walkforward.py` — `Window` dataclass (test_start, test_end, window_id, train_mask, test_mask), `build_windows(entry_times, test_days, train_days, start=None, end=None) -> list[Window]`, `label_window_assignment(labels, windows) -> DataFrame` (+window_id col), `window_stats_table(labels_w, r_col="r_net") -> DataFrame`, `window_aggregate(labels_w, r_col="r_net") -> DataFrame` (raw_/net_ variants + cost_delta_expectancy)
- `runner.py` — `main(argv=None) -> int` (exit 0/1/2), `run_backtest(cfg, symbols, range_arg, write) -> dict`, `resolve_range(range_arg, bars) -> (start, end)`
- `reports.py` (extended) — `write_walkforward(window_stats, reports_dir)`, `write_window_manifest(windows_meta, reports_dir)`

**New CLI surface:** `python -m ai_trading.backtest` with flags `--config`, `--symbols`, `--range` (`YYYY-MM-DD:YYYY-MM-DD` or `last-ND`), `--min-history-days`, `--write`

**New test files:** `tests/unit/test_walkforward.py`, `tests/unit/test_runner.py`; `tests/unit/test_reports.py` (extended)

**New runtime artifact layout (data/ gitignored):** `data/labels/{SYMBOL}_M15.parquet`, `data/labels/canonical_stats.json`, `data/labels/run_manifest.json`, `data/reports/walkforward.parquet`, `data/reports/walkforward_manifest.json`

**New data-flow consumers (downstream):** Phase 4 ML (labels + windows + walkforward harness), Phase 6 dashboard (canonical + per-window stats artifacts)

<output>
Create `.planning/phases/03-backtesting-labeling/03-03-SUMMARY.md` when done
</output>
