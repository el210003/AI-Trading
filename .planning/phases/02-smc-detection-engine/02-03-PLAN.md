---
phase: 02-smc-detection-engine
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ai_trading/detectors/zones.py
  - tests/unit/test_zones.py
  - tests/unit/test_lifecycle.py
autonomous: true
requirements: [SMC-04, SMC-05]

must_haves:
  truths:
    - "Each completed zigzag leg spawns exactly one zone at the completing swing's confirmation: range_high = max(p1, p2), range_low = min(p1, p2), equilibrium = midpoint, leg_direction up (low->high) or down (high->low) (D-09)"
    - "No zone exists before the first completed leg (a zone requires two opposite-side zigzag points)"
    - "Mitigation = wick intersecting the range interior (closes not required, D-10/A7); invalidation = committed close beyond the far boundary; wick pokes beyond the boundary do NOT invalidate (D-11)"
    - "Lifecycle is monotone unmitigated -> mitigated -> invalidated; one bar may stamp both timestamps (mitigation check first, then invalidation check); timestamps are first-event and never overwritten (Pitfall 6)"
    - "All zones are tracked concurrently with full history retained — overlapping ranges from different legs coexist; zones are never deleted (D-12)"
    - "Lifecycle advancement starts at the bar opening at created_at (time_utc >= created_at); the creating confirmation bar itself never advances its own zone (planner pin)"
    - "Repaint tier-3 (zones): zones derived from completed legs are immutable under appended bars"
  artifacts:
    - "src/ai_trading/detectors/zones.py — derive_zones(zigzag, bars) -> pd.DataFrame"
    - "Zones frame schema: (zone_id, symbol, timeframe, leg_direction, range_high, range_low, equilibrium, state, created_at, mitigated_at, invalidated_at); state in {unmitigated, mitigated, invalidated}"
    - "Deterministic IDs: zone_id = {SYMBOL}-{TF}-Z{n:04d} assigned in creation order"
    - "tests/unit/test_zones.py — SMC-04 unit suite incl. zone tier-3 repaint assertions"
    - "tests/unit/test_lifecycle.py — SMC-05 zone lifecycle suite"
  key_links:
    - "derive_zones consumes build_zigzag output (with symbol/timeframe carried through) — never the raw swing list (D-04)"
    - "Zones carry created_at = completing zigzag point's confirmed_at — the confirmation-time axis plan 02-04's MTF join keys on"
    - "Zero adapter-tier imports, zero file I/O, input frames never mutated (SC5)"
---

<objective>
Implement premium/discount zone derivation and the monotone zone lifecycle state machine: one zone per completed zigzag leg (D-09), wick-touch mitigation (D-10), committed-close invalidation (D-11), all zones tracked concurrently with full history (D-12) — satisfying SMC-04 and the zone half of SMC-05.

Purpose: zones are the HTF bias source for SMC-06 (D-13 reuses this machinery) and the evidence backbone for Phase 4 features and Phase 6 traces; lifecycle timestamps must be first-event and point-in-time safe or every downstream probability is contaminated.
Output: src/ai_trading/detectors/zones.py + tests/unit/{test_zones,test_lifecycle}.py — all unit-green, ruff-clean, tier-3 repaint proven for zones.
</objective>

<execution_context>
@C:/Users/Eric/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Eric/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-smc-detection-engine/02-CONTEXT.md
@.planning/phases/02-smc-detection-engine/02-PATTERNS.md
@src/ai_trading/normalize.py
@tests/conftest.py
@src/ai_trading/detectors/swings.py
@src/ai_trading/detectors/zigzag.py
@tests/unit/_detector_fixtures.py
</context>

## Artifacts this phase produces (plan 03 scope)

- Function: `derive_zones(zigzag: pd.DataFrame, bars: pd.DataFrame) -> pd.DataFrame` in `src/ai_trading/detectors/zones.py`
- Pinned zones schema: `(zone_id, symbol, timeframe, leg_direction, range_high, range_low, equilibrium, state, created_at, mitigated_at, invalidated_at)`; `leg_direction` in {up, down}; `state` in {unmitigated, mitigated, invalidated}; deterministic zone IDs `{SYMBOL}-{TF}-Z{n:04d}` in creation order
- Test modules: `tests/unit/test_zones.py`, `tests/unit/test_lifecycle.py` with module-local sculptor helpers (do NOT modify tests/unit/_detector_fixtures.py or root conftest.py — 02-01 owns them; consume read-only)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: zones.py — per-leg zone derivation + monotone lifecycle state machine</name>
  <files>src/ai_trading/detectors/zones.py</files>
  <read_first>
    - .planning/phases/02-smc-detection-engine/02-PATTERNS.md — zones.py section: copy-ready advance_zone core (Pattern 5 excerpt) + pinned copy rules
    - .planning/phases/02-smc-detection-engine/02-RESEARCH.md — Pattern 5 (lines 326–342), Pitfall 6 (same-bar double-stamp order), Assumption A7 (wick-interior mitigation), Anti-Patterns (no full-frame statistics)
    - .planning/phases/02-smc-detection-engine/02-CONTEXT.md — locked decisions D-09, D-10, D-11, D-12
    - src/ai_trading/detectors/zigzag.py — input contract from plan 02-01 (zigzag schema carries symbol/timeframe; confirmed_at per point)
    - src/ai_trading/normalize.py — style-of-record for guards; TIMEFRAME_MINUTES if needed
    - src/ai_trading/detectors/pools.py — sibling loop-style analog for sequential state machines
  </read_first>
  <behavior>
    - A completed up-leg (low point then high point) spawns one zone: range_low = leg low, range_high = leg high, leg_direction = up, created_at = the high point's confirmed_at
    - A completed down-leg mirrors: leg_direction = down, created_at = the low point's confirmed_at
    - equilibrium equals (range_high + range_low) / 2 exactly on every zone record
    - A frame whose zigzag has fewer than 2 opposite-side points produces zero zones
    - First bar (opening at created_at) whose wick enters the range interior (low strictly below range_high AND high strictly above range_low) sets state mitigated, mitigated_at = that bar's time_utc — no close required
    - A bar whose wick pokes beyond the far boundary but closes inside does NOT invalidate (mitigated at most)
    - A bar whose close is strictly beyond the far boundary (close above range_high or below range_low) sets state invalidated, invalidated_at = that bar's time_utc — valid from unmitigated or mitigated
    - One bar that both wick-enters the range and closes beyond it stamps BOTH timestamps with mitigated_at <= invalidated_at and final state invalidated
    - Lifecycle never regresses; mitigated_at/invalidated_at are first-event (later qualifying bars do not overwrite)
    - Two consecutive legs produce two distinct zones with independent lifecycles (full history retained)
    - Bars strictly before created_at never advance a zone
    - Empty zigzag or empty bars -> schema-correct empty zones frame; zigzag/bars inputs bit-identical before/after derive_zones
  </behavior>
  <action>
    Create src/ai_trading/detectors/zones.py with the module docstring citing D-09..D-12 and RESEARCH Pattern 5; from __future__ import annotations; import pandas. Zero adapter-tier vendor imports, zero file I/O, no grid re-flooring of stored times.

    Public function `derive_zones(zigzag, bars) -> pd.DataFrame`. Guards: zigzag must match the pinned zigzag schema (symbol, timeframe, bar_time, price, side, confirmed_at) with sides alternating under the plan 02-01 contract; bars must contain symbol, time_utc, open, high, low, close with strictly-increasing unique time_utc and finite OHLC; violations raise ValueError naming the invariant. Empty inputs return an empty frame with the exact pinned schema (Pitfall 10).

    Implementation (explicit sequential loop per leg + per-bar advancement; do not vectorize the state machine):
    Zone creation: group zigzag points per symbol (they arrive sorted by confirmed_at, bar_time, side under the plan 02-01 contract). Each consecutive opposite-side pair (p1, p2) defines one zone at the moment p2 confirms: range_high = max of the two prices, range_low = min, equilibrium = midpoint, leg_direction = up when p1 is a low and p2 is a high, down otherwise, created_at = p2's confirmed_at, state = unmitigated, mitigated_at/invalidated_at = NA. zone_id assigned sequentially in creation order per symbol as f"{SYMBOL}-{TF}-Z{n:04d}" (zero-padded, deterministic under recompute). Track ALL zones concurrently — never delete or supersede (D-12).
    Lifecycle advancement: iterate bars in time_utc order; evaluate every live (not invalidated) zone whose created_at is at or before the bar's time_utc — the first evaluated bar is the one opening at created_at (time_utc == created_at); the creating confirmation bar itself (time_utc == created_at minus TF) is excluded so zones are never born mitigated (planner pin). Per bar per zone, mitigation check then invalidation check (Pitfall 6 pin): mitigation = bar low strictly below range_high AND bar high strictly above range_low (wick intersects the range interior — operationalizes D-10's near-boundary touch for both approach directions, A7); invalidation = bar close strictly above range_high or strictly below range_low (committed closes only — wick pokes beyond do not count, D-11). One bar may stamp both timestamps (mitigated_at first, then invalidated_at; final state invalidated). State transitions are monotone; timestamps are first-event and never overwritten; invalidated zones stop being evaluated.
    Return a fresh zones frame with the exact pinned column order, sorted by (symbol, created_at); naive datetimes; never assign into zigzag or bars.
  </action>
  <verify>
    <automated>uv run python -c "from ai_trading.detectors.zones import derive_zones; print('zones-import-ok')" && uv run pytest tests/unit/test_zones.py tests/unit/test_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - [source] src/ai_trading/detectors/zones.py defines derive_zones(zigzag, bars) -> pd.DataFrame; columns exactly zone_id, symbol, timeframe, leg_direction, range_high, range_low, equilibrium, state, created_at, mitigated_at, invalidated_at
    - [source] rg -c "floor_to_timeframe" src/ai_trading/detectors/zones.py returns no matches
    - [test-command] uv run pytest tests/unit/test_zones.py tests/unit/test_lifecycle.py -q exits 0
    - [behavior] wick-poke-beyond test proves invalidation requires a committed close (D-11)
    - [behavior] same-bar double-stamp test proves mitigated_at <= invalidated_at with final state invalidated
  </acceptance_criteria>
  <done>derive_zones implements D-09..D-12: per-completed-leg ranges with equilibrium, wick-touch mitigation, committed-close invalidation, monotone first-event lifecycles, concurrent full-history tracking, deterministic IDs, guards, and empty-frame contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_zones.py — per-leg ranges, equilibrium, creation timing + tier-3 zone repaint</name>
  <files>tests/unit/test_zones.py</files>
  <read_first>
    - tests/unit/_detector_fixtures.py — reuse swing_spec_bars / assert_point_in_time_prefix_equality (read-only)
    - src/ai_trading/detectors/swings.py + src/ai_trading/detectors/zigzag.py — upstream chain (tests build zones via build_zigzag(detect_swings(...)))
    - src/ai_trading/detectors/zones.py — implementation under test (Task 1)
    - tests/unit/test_swings.py + tests/unit/test_pools.py — named-test style from plans 02-01/02-02
    - .planning/phases/02-smc-detection-engine/02-RESEARCH.md — Pattern 5, D-09 range semantics
  </read_first>
  <behavior>
    - Sculpted low->high leg yields one up zone with range_low/range_high at the sculpted extremes and equilibrium at the exact midpoint
    - Sculpted high->low leg yields one down zone (mirror)
    - created_at equals the completing swing's confirmed_at (the confirmation-bar close), not the extreme bar's time
    - Zigzag with a single point -> zero zones; two same-side points absorbed by the zigzag -> zero zones
    - Two completed legs -> two zones, both present simultaneously (full history), each with its own state/timestamps
    - Zones frame sorted by (symbol, created_at); IDs sequential Z0001, Z0002, ... per symbol
    - Empty zigzag -> schema-correct empty frame; empty bars -> schema-correct empty frame
    - zigzag/bars bit-identical before/after derive_zones
    - Tier-3 repaint: zones derived from completed legs are identical between every prefix run and the visible subset of the full-frame run (created_at <= prefix close); chunked appends preserve this
  </behavior>
  <action>
    tests/unit/test_zones.py — module docstring cites SMC-04 + D-04 (zones consume the zigzag, never the raw swing list) + SC1 tier-3 and states no adapter-tier imports anywhere; @pytest.mark.unit on every test; module-local sculptors composing swing_spec_bars -> detect_swings -> build_zigzag -> derive_zones (the real upstream chain — do not hand-build zigzag frames in tests except where a unit edge needs one). One named test per behavior bullet (names indicative): test_up_leg_zone_range_and_equilibrium, test_down_leg_zone_mirror, test_created_at_is_completing_swing_confirmation, test_incomplete_leg_yields_no_zone, test_two_legs_two_coexisting_zones, test_zone_ids_sequential_and_sorted, test_empty_frame_contracts, test_input_frames_not_mutated, test_completed_leg_zones_immutable_under_appended_bars. Write tests first, then align zones.py to GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_zones.py -q && git diff --exit-code tests/conftest.py tests/unit/_detector_fixtures.py</automated>
  </verify>
  <acceptance_criteria>
    - [test-command] uv run pytest tests/unit/test_zones.py -q exits 0 (>= 9 named tests)
    - [behavior] test_completed_leg_zones_immutable_under_appended_bars proves tier-3 repaint immutability for zones
    - [behavior] test_incomplete_leg_yields_no_zone proves no zone exists before the first completed leg
    - [source] every test carries @pytest.mark.unit; docstring cites SMC-04 and the zigzag-consumption rule (D-04)
    - [source] git diff --exit-code tests/conftest.py tests/unit/_detector_fixtures.py passes
  </acceptance_criteria>
  <done>SMC-04 derivation semantics and the zone tier of the repaint contract are locked by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_lifecycle.py — bar-by-bar monotone zone state transitions</name>
  <files>tests/unit/test_lifecycle.py</files>
  <read_first>
    - src/ai_trading/detectors/zones.py — lifecycle logic under test
    - .planning/phases/02-smc-detection-engine/02-RESEARCH.md — Pattern 5, Pitfall 6 (pinned order: mitigation check then invalidation check), A7
    - .planning/phases/02-smc-detection-engine/02-CONTEXT.md — D-10, D-11, D-12
    - tests/unit/test_zones.py — chain-fixture style to mirror
  </read_first>
  <behavior>
    - Wick into the range with close outside -> state mitigated only (mitigated_at stamped, invalidated_at NA)
    - Wick pokes beyond the far boundary, closes inside -> mitigated but NOT invalidated (D-11 wick-poke rule)
    - Close beyond the far boundary from unmitigated -> direct unmitigated -> invalidated transition (invalidated_at stamped, mitigated_at NA)
    - Close beyond the far boundary from mitigated -> invalidated (mitigated_at preserved)
    - One bar wick-enters AND closes beyond -> both timestamps stamped, mitigated_at <= invalidated_at, final state invalidated
    - Monotonicity: no test fixture path produces a state regression or a timestamp overwrite (later qualifying bars leave first-event timestamps intact)
    - The creating confirmation bar never advances its own zone; advancement starts at the bar opening at created_at
    - Bars after invalidation never touch the zone again
    - Empty frames -> schema-correct empty output
  </behavior>
  <action>
    tests/unit/test_lifecycle.py — module docstring cites SMC-05 + D-10/D-11/D-12 + Pitfall 6 pinned order and states no adapter-tier imports anywhere; @pytest.mark.unit on every test; module-local sculptors building full detector chains with bars engineered bar-by-bar for each transition (name the sculpting intent in each test's docstring so the price path is auditable). One named test per behavior bullet (names indicative): test_wick_touch_mitigates_without_close, test_wick_poke_beyond_does_not_invalidate, test_close_beyond_invalidates_from_unmitigated, test_close_beyond_invalidates_from_mitigated, test_same_bar_double_stamp_order, test_lifecycle_monotone_first_event_timestamps, test_zone_not_advanced_before_creation_bar, test_zone_frozen_after_invalidation, test_empty_frame_contract. Write tests first, then align zones.py to GREEN. Cross-reference (docstring note only) that pool terminal-state one-and-done coverage lives in tests/unit/test_sweeps.py (plan 02-02) — do not duplicate pool tests here.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_lifecycle.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - [test-command] uv run pytest tests/unit/test_lifecycle.py -q exits 0 (>= 9 named tests)
    - [behavior] test_wick_poke_beyond_does_not_invalidate proves D-11's committed-close-only invalidation
    - [behavior] test_same_bar_double_stamp_order proves the pinned mitigation-then-invalidation same-bar order
    - [test-command] uv run pytest -q (full unit suite incl. plans 02-01..02-03) exits 0
    - [test-command] uv run ruff check . exits 0
  </acceptance_criteria>
  <done>SMC-05 zone lifecycle is locked: monotone unmitigated -> mitigated -> invalidated with first-event timestamps, wick-touch mitigation, committed-close invalidation, and pinned same-bar ordering. The full unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zigzag + bar frames -> zones detector | Malformed/untrusted frames cross into the zone state machine |
| zone timestamps -> downstream consumers | First-event lifecycle timestamps feed Phase 4 features and Phase 6 evidence; any overwrite/regression would tamper with historical evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering/Repudiation (lookahead bias) | derive_zones | high | mitigate | Zones spawn only at confirmed leg completions; lifecycle evaluated only on bars at/after created_at; tier-3 repaint test proves completed-leg zone immutability under appended bars |
| T-2-02 | Tampering (NaN/inf poisoning) | derive_zones entry | medium | mitigate | V5 guards (schemas, monotonic unique time_utc, finite OHLC); empty/short-frame contracts tested |
| T-2-03 | Tampering (schema drift) | zones frame | medium | mitigate | Schema pinned in this plan incl. state vocabulary; schema assertions in test_zones.py; plan 02-04 and Phase 4/6 consume the pinned columns |
| T-2-04 | Tampering (input-frame mutation) | derive_zones | medium | mitigate | Fresh output frames; test_input_frames_not_mutated |
| T-2-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No packages installed in Phase 2 (Package Legitimacy Audit: zero adoptions) |
</threat_model>

<verification>
- uv run pytest -q — full unit suite green
- uv run pytest -q -m "unit or mt5" — full suite (no terminal needed)
- uv run ruff check . — clean
- rg -c "import MetaTrader5|from MetaTrader5" src/ai_trading/detectors/ — no matches
</verification>

<success_criteria>
- Every completed zigzag leg spawns a zone with range high/low, equilibrium, leg direction, and confirmation-time created_at (SMC-04, D-09)
- Zones carry monotone lifecycle state updated bar-by-bar: wick-touch mitigation, committed-close invalidation, first-event timestamps, full concurrent history (SMC-05, D-10/D-11/D-12)
- Tier-3 repaint immutability proven for completed-leg zones (SC1 extension)
- Zero vendor-package imports; conftest.py and _detector_fixtures.py untouched
</success_criteria>

<output>
Create `.planning/phases/02-smc-detection-engine/02-03-SUMMARY.md` when done
</output>
